<script setup lang="ts">
useHead({
  title: "Silicon Nitride Shaft Rods | High Precision & Lightweight",
  meta: [
    {
      name: "description",
      content:
        "High-precision Si₃N₄ ceramic shaft rods for CNC spindles, pump shafts, and turbo-machinery, offering low weight, high hardness, and non-magnetic performance.",
    },
  ],
});
</script>

<template>
  <div>
    <div class="breadcrumb">
      <div class="wrap">
        <a href="/">Home</a>
        <span class="sep">/</span>
        <a href="/products">Products</a>
        <span class="sep">/</span>
        <a href="/products/ceramic-igniters">Ceramic Igniters</a>
        <span class="sep">/</span>
        <span>Si₃N₄ Shaft Rods</span>
      </div>
    </div>

    <section class="prod-hero">
      <div class="wrap">
        <div>
          <span class="ph-eyebrow">★ Flagship · Ceramic Structural Parts</span>
          <h1>
            Silicon nitride shaft rods,<br /><span class="hl"
              >precision to ±0.01mm.</span
            >
          </h1>
          <p class="ph-sub">
            High-precision Si₃N₄ ceramic shaft rods for
            <b>high-speed CNC spindles, pump shafts, and turbo-machinery</b>.
            60% lighter than steel, hardness 78 HRC, zero magnetic signature,
            and lubricant-free capable. Standard Ø3–100mm; custom dimensions to
            OEM drawing.
          </p>
          <div class="ph-statbar">
            <div>
              <div class="num">±0.01mm</div>
              <div class="lbl">Ground Tolerance</div>
            </div>
            <div>
              <div class="num">HRC 78</div>
              <div class="lbl">Hardness</div>
            </div>
            <div>
              <div class="num">60%</div>
              <div class="lbl">Lighter than Steel</div>
            </div>
            <div>
              <div class="num">Non-magnetic</div>
              <div class="lbl">Zero Mag. Sig.</div>
            </div>
          </div>
          <div class="hero-ctas">
            <a class="btn-primary" href="#quote">Request a Quote →</a>
            <a
              class="btn-ghost"
              href="#downloads"
              style="color: #fff; border-color: #fff"
              >Download Datasheet ↓</a
            >
          </div>
        </div>
        <div class="spotlight">
          <div class="spotlight-tag">Si₃N₄ Shaft Rods · At a Glance</div>
          <div class="spotlight-row">
            <span class="k">Material</span><span class="v">Si₃N₄</span>
          </div>
          <div class="spotlight-row">
            <span class="k">Density</span><span class="v">3.2 g/cm³</span>
          </div>
          <div class="spotlight-row">
            <span class="k">Hardness</span><span class="v">HRC 78</span>
          </div>
          <div class="spotlight-row">
            <span class="k">Diameter</span><span class="v">Ø 3–100mm</span>
          </div>
          <div class="spotlight-row">
            <span class="k">Tolerance</span
            ><span class="v">±0.01mm ground</span>
          </div>
          <div class="spotlight-row">
            <span class="k">Magnetic</span><span class="v">Non-magnetic</span>
          </div>
          <div class="spotlight-row">
            <span class="k">Sample Lead Time</span><span class="v">14 d</span>
          </div>
        </div>
      </div>
      <div class="prod-hero-stripe"></div>
    </section>
    <section class="cert-strip">
      <div class="wrap">
        <div class="cs-label">Certifications &amp; Standards</div>
        <div class="cs-badges">
          <span>ISO 9001</span><span>IATF 16949</span><span>CE</span
          ><span>RoHS · REACH</span><span>UL Recognized</span>
        </div>
      </div>
    </section>
    <section class="s">
      <div class="wrap">
        <div class="sec-num">01 · Size Ranges</div>
        <h2>Three shaft rod <span class="accent">diameter ranges</span>.</h2>
        <p class="sec-lead">
          All three ranges share the same Si₃N₄ material. Choose based on your
          bore diameter and bearing interface requirement.
        </p>
        <div class="b-grid">
          <div class="b-card featured">
            <div class="b-badge">★ Highest Volume</div>
            <div
              class="b-head"
              style="
                background: linear-gradient(
                  135deg,
                  var(--navy) 0%,
                  var(--navy-3) 100%
                );
              "
            >
              <div class="b-model">Small Diameter</div>
              <div class="b-volt">Ø 3–20 mm · precision class</div>
            </div>
            <div class="b-img-slot">
              <div class="b-img-ph">
                <div class="bph-icon">🔩</div>
                <div class="bph-lbl">Small diameter shaft — close-up</div>
              </div>
            </div>
            <div class="b-body">
              <div class="b-row">
                <span class="b-k">Diameter</span
                ><span class="b-v">Ø 3–20<small>mm</small></span>
              </div>
              <div class="b-row">
                <span class="b-k">Tolerance</span
                ><span class="b-v hot">±0.01<small>mm</small></span>
              </div>
              <div class="b-row">
                <span class="b-k">Surface Finish</span
                ><span class="b-v">Ra ≤0.2<small>µm</small></span>
              </div>
              <div class="b-row">
                <span class="b-k">Straightness</span
                ><span class="b-v">≤0.01/100<small>mm</small></span>
              </div>
              <div class="b-row">
                <span class="b-k">Hardness</span
                ><span class="b-v">HRC 78<small></small></span>
              </div>
            </div>
            <div class="b-foot">
              <b>Use:</b> CNC spindle shafts · dental turbines · precision
              instruments
            </div>
          </div>
          <div class="b-card">
            <div class="b-head">
              <div class="b-model">Medium Diameter</div>
              <div class="b-volt">Ø 20–50 mm · industrial class</div>
            </div>
            <div class="b-img-slot">
              <div class="b-img-ph">
                <div class="bph-icon">⚙️</div>
                <div class="bph-lbl">Medium diameter shaft</div>
              </div>
            </div>
            <div class="b-body">
              <div class="b-row">
                <span class="b-k">Diameter</span
                ><span class="b-v">Ø 20–50<small>mm</small></span>
              </div>
              <div class="b-row">
                <span class="b-k">Tolerance</span
                ><span class="b-v">±0.02<small>mm</small></span>
              </div>
              <div class="b-row">
                <span class="b-k">Surface Finish</span
                ><span class="b-v">Ra ≤0.4<small>µm</small></span>
              </div>
              <div class="b-row">
                <span class="b-k">Straightness</span
                ><span class="b-v">≤0.02/100<small>mm</small></span>
              </div>
              <div class="b-row">
                <span class="b-k">Hardness</span
                ><span class="b-v">HRC 78<small></small></span>
              </div>
            </div>
            <div class="b-foot">
              <b>Use:</b> Pump shafts · compressor shafts · chemical equipment
            </div>
          </div>
          <div class="b-card">
            <div class="b-head">
              <div class="b-model">Large Diameter</div>
              <div class="b-volt">Ø 50–100 mm · heavy duty</div>
            </div>
            <div class="b-img-slot">
              <div class="b-img-ph">
                <div class="bph-icon">🏭</div>
                <div class="bph-lbl">Large diameter shaft</div>
              </div>
            </div>
            <div class="b-body">
              <div class="b-row">
                <span class="b-k">Diameter</span
                ><span class="b-v">Ø 50–100<small>mm</small></span>
              </div>
              <div class="b-row">
                <span class="b-k">Tolerance</span
                ><span class="b-v">±0.05<small>mm</small></span>
              </div>
              <div class="b-row">
                <span class="b-k">Surface Finish</span
                ><span class="b-v">Ra ≤0.8<small>µm</small></span>
              </div>
              <div class="b-row">
                <span class="b-k">Length</span
                ><span class="b-v">Up to 600<small>mm</small></span>
              </div>
              <div class="b-row">
                <span class="b-k">Application</span
                ><span class="b-v">Turbo-machinery<small></small></span>
              </div>
            </div>
            <div class="b-foot">
              <b>Use:</b> Turbo-machinery · large pump shafts · custom
              structural
            </div>
          </div>
        </div>
        <div class="common-note">
          All Si₃N₄ shaft rods: density 3.2 g/cm³ · non-magnetic ·
          lubricant-free · operating temp −40 to +800°C.
        </div>
      </div>
    </section>
    <section class="s" id="downloads" style="background: var(--bg-soft)">
      <div class="wrap">
        <div class="sec-num">02 · Datasheet &amp; Downloads</div>
        <h2>Drawings, datasheets, <span class="accent">and certs</span>.</h2>
        <p class="sec-lead">
          Everything your engineering and procurement teams need. Email
          <CmsEmail /> for STEP/DWG files.
        </p>
        <div class="dl-grid">
          <a href="#quote" class="dl-card"
            ><div class="dl-icon">📄</div>
            <div class="dl-name">Product Datasheet</div>
            <div class="dl-meta">PDF · full specifications</div>
            <div class="dl-btn">Download PDF →</div></a
          >
          <a href="#quote" class="dl-card"
            ><div class="dl-icon">📐</div>
            <div class="dl-name">2D / 3D Drawings</div>
            <div class="dl-meta">STEP · DWG · PDF · on request</div>
            <div class="dl-btn">Request files →</div></a
          >
          <a href="#quote" class="dl-card"
            ><div class="dl-icon">🛡️</div>
            <div class="dl-name">Certification Files</div>
            <div class="dl-meta">ISO 9001 · CE · RoHS · REACH</div>
            <div class="dl-btn">Request scans →</div></a
          >
        </div>
      </div>
    </section>
    <section class="s">
      <div class="wrap">
        <div class="sec-num">03 · Certifications &amp; Compliance</div>
        <h2>Certified for <span class="accent">global markets</span>.</h2>
        <p class="sec-lead">
          All products shipped with certificate of conformity. Additional
          certification documentation available for OEM qualification programs.
        </p>
        <div class="cert-grid">
          <div class="cert-card">
            <div class="cc-mark">ISO</div>
            <div class="cc-scope">
              ISO 9001 quality management system. Full lot traceability and
              PPAP-level documentation for OEM programs.
            </div>
            <div class="cc-tag">Quality System</div>
          </div>
          <div class="cert-card">
            <div class="cc-mark">CE</div>
            <div class="cc-scope">
              CE marking for European Union shipments. Declaration of Conformity
              provided with CE-marked products.
            </div>
            <div class="cc-tag">European Union</div>
          </div>
          <div class="cert-card">
            <div class="cc-mark">RoHS</div>
            <div class="cc-scope">
              RoHS and REACH compliant. Material declarations available for
              restricted-substance compliance screening.
            </div>
            <div class="cc-tag">EU · Global</div>
          </div>
          <div class="cert-card">
            <div class="cc-mark">IATF</div>
            <div class="cc-scope">
              Produced in an IATF 16949 / ISO 9001 facility. PPAP documentation
              available for automotive-adjacent applications.
            </div>
            <div class="cc-tag">Quality System</div>
          </div>
        </div>
      </div>
    </section>
    <section class="s quote-sec" id="quote">
      <div class="wrap">
        <div>
          <div class="sec-num">04 · Get a Quote</div>
          <h2>
            Request a quote for <span class="accent">Si₃N₄ shaft rods</span>.
          </h2>
          <p class="sec-lead" style="color: rgba(255, 255, 255, 0.8)">
            Tell us your specification, dimensions, grade, and annual volume.
            Our engineers reply within one business day with a quote and matched
            datasheet.
          </p>
          <ul class="quote-why">
            <li>
              <b>Free samples, 48-hour dispatch</b> on stock items — no MOQ.
            </li>
            <li>
              <b>Custom to drawing:</b> DFM review in 48h, prototype in 14 days.
            </li>
            <li><b>Direct manufacturer pricing</b> — no trading layer.</li>
            <li>
              <b>ISO 9001 certified</b> — full material certs with every
              shipment.
            </li>
          </ul>
        </div>
        <div class="quote-form-card">
          <div class="qquote-tag">Free Quote · Reply in 24h</div>
          <div class="qquote-title">Request a quote</div>
          <div class="qquote-note">
            Specify diameter (OD), length, tolerance, and annual volume.
          </div>
          <QuoteInquiryForm variant="card" submit-label="Request a Quote →" />
        </div>
      </div>
    </section>
    <section class="s" style="background: var(--bg-soft)">
      <div class="wrap">
        <div class="sec-num">05 · Engineering FAQ</div>
        <h2>The questions <span class="accent">we get most</span>.</h2>
        <div class="faq-grid">
          <div class="faq-item">
            <div class="faq-q">Why Si₃N₄ over steel for shaft rods?</div>
            <div class="faq-a">
              Si₃N₄ is 60% lighter than steel, has zero magnetic signature, and
              can run lubricant-free. At high speeds (>40,000 RPM), the lighter
              shaft reduces centrifugal loads on bearings by 60%, extending
              bearing life 3× or more.
            </div>
          </div>
          <div class="faq-item">
            <div class="faq-q">What is the tightest tolerance?</div>
            <div class="faq-a">
              Ground ceramic: ±0.01mm on diameter and straightness. Ra ≤0.2µm
              surface finish on precision class. CMM inspection report with
              every production shipment.
            </div>
          </div>
          <div class="faq-item">
            <div class="faq-q">
              Can you match my existing steel shaft OD and length?
            </div>
            <div class="faq-a">
              Yes — send drawing (STEP/DWG) or just specify OD, length, and
              tolerance. We confirm material capability and quote within 48
              hours.
            </div>
          </div>
          <div class="faq-item">
            <div class="faq-q">What is the minimum order quantity?</div>
            <div class="faq-a">
              Prototypes: no MOQ (single-unit quotes available). Production: 50
              units minimum for standard dimensions. Volume pricing from 500
              units.
            </div>
          </div>
          <div class="faq-item">
            <div class="faq-q">Operating temperature range?</div>
            <div class="faq-a">
              Si₃N₄ shaft rods operate continuously to 800°C. Ambient range −40
              to +85°C. Suitable for high-temperature furnace, autoclave, and
              process equipment applications.
            </div>
          </div>
          <div class="faq-item">
            <div class="faq-q">Do you stock standard sizes?</div>
            <div class="faq-a">
              Common OD sizes (Ø5, Ø6, Ø8, Ø10, Ø12, Ø16, Ø20mm) in lengths to
              300mm are held in stock. Non-stock: 14-day lead time.
            </div>
          </div>
        </div>
      </div>
    </section>
    <section class="s">
      <div class="wrap">
        <div class="sec-num">06 · Related</div>
        <h2>Explore <span class="accent">further</span>.</h2>
        <div class="rel-grid">
          <a href="/products/ceramic-structural" class="rel-card"
            ><div class="rel-eyebrow">Product Category</div>
            <div class="rel-title">Ceramic Structural Parts</div>
            <div class="rel-desc">
              All 12 sub-types: shafts, turbines, wear parts, seals, and custom
              machined components.
            </div>
            <div class="rel-link">View →</div></a
          ><a href="/materials/silicon-nitride" class="rel-card"
            ><div class="rel-eyebrow">Material</div>
            <div class="rel-title">Silicon Nitride (Si₃N₄)</div>
            <div class="rel-desc">
              Density, hardness, thermal shock resistance, and machinability of
              Si₃N₄ — the engineering data.
            </div>
            <div class="rel-link">View →</div></a
          ><a href="/products/precision-bearings" class="rel-card"
            ><div class="rel-eyebrow">Related Product</div>
            <div class="rel-title">Precision Ceramic Bearings</div>
            <div class="rel-desc">
              Full Si₃N₄ and hybrid ceramic bearings for high-speed spindles —
              paired with Si₃N₄ shaft rods.
            </div>
            <div class="rel-link">View →</div></a
          >
        </div>
      </div>
    </section>
  </div>
</template>

<style>
:root {
  --navy: #0a2647;
  --navy-2: #103a6b;
  --navy-3: #1b4d8a;
  --bg: #ffffff;
  --bg-soft: #f1f5fa;
  --bg-soft-2: #e6eef7;
  --ink: #14233a;
  --ink-soft: #4a5a70;
  --ink-faint: #8794a6;
  --orange: #f26419;
  --orange-deep: #d8530f;
  --gold: #f0a500;
  --gold-soft: #ffd466;
  --line: #dce5ef;
  --line-strong: #c2d0e0;
  --sans: "Barlow", sans-serif;
  --cond: "Barlow Condensed", sans-serif;
  --mono: "IBM Plex Mono", monospace;
}
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}
body {
  background: var(--bg);
  color: var(--ink);
  font-family: var(--sans);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}
a {
  color: inherit;
  text-decoration: none;
  cursor: pointer;
}
.wrap {
  max-width: 1260px;
  margin: 0 auto;
  padding: 0 40px;
}

/* CHROME */
.topbar {
  background: var(--navy);
  color: #c5d4e6;
  font-family: var(--mono);
  font-size: 12px;
}
.topbar .wrap {
  display: flex;
  justify-content: space-between;
  align-items: center;
  height: 38px;
}
.topbar .left {
  display: flex;
  gap: 26px;
}
.topbar .left span {
  display: flex;
  align-items: center;
  gap: 7px;
}
.dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--gold);
}
.topbar .right {
  letter-spacing: 0.05em;
  color: var(--gold);
}
nav {
  position: sticky;
  top: 0;
  z-index: 100;
  background: #fff;
  border-bottom: 3px solid var(--navy);
  box-shadow: 0 2px 12px rgba(10, 38, 71, 0.06);
}
nav .wrap {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 78px;
}
.logo {
  display: flex;
  align-items: center;
  gap: 12px;
}
.logo-mark {
  width: 42px;
  height: 42px;
}
.logo-text {
  font-weight: 800;
  font-size: 24px;
  letter-spacing: -0.02em;
  color: var(--navy);
}
.logo-text b {
  color: var(--orange);
}
.logo-text small {
  display: block;
  font-family: var(--mono);
  font-size: 9px;
  letter-spacing: 0.16em;
  color: var(--ink-faint);
  font-weight: 400;
  margin-top: -2px;
}
.nav-links {
  display: flex;
  gap: 32px;
  align-items: center;
}
.nav-links a {
  font-size: 14.5px;
  font-weight: 700;
  color: var(--navy);
  padding: 6px 0;
  letter-spacing: 0.2px;
}
.nav-links a.active {
  color: var(--orange);
  border-bottom: 2px solid var(--orange);
}
.nav-links a:hover {
  color: var(--orange);
}
.nav-cta {
  background: var(--orange);
  color: #fff !important;
  padding: 12px 24px;
  font-weight: 800;
  font-size: 13.5px;
  letter-spacing: 0.5px;
  transition: background 0.15s;
}
.nav-cta:hover {
  background: var(--orange-deep);
}

.breadcrumb {
  background: var(--bg-soft);
  padding: 14px 0;
  border-bottom: 1px solid var(--line);
}
.breadcrumb .wrap {
  display: flex;
  gap: 10px;
  align-items: center;
  font-family: var(--mono);
  font-size: 12px;
  color: var(--ink-soft);
  letter-spacing: 0.05em;
}
.breadcrumb a {
  color: var(--navy);
  font-weight: 600;
}
.breadcrumb a:hover {
  color: var(--orange);
}
.breadcrumb .sep {
  color: var(--ink-faint);
}

/* HERO */
.page-hero {
  background: linear-gradient(135deg, var(--navy) 0%, var(--navy-2) 100%);
  color: #fff;
  padding: 70px 0 90px;
  position: relative;
  overflow: hidden;
}
.page-hero-stripe {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(
    90deg,
    var(--orange) 0%,
    var(--gold) 50%,
    var(--orange) 100%
  );
}
.page-hero .wrap {
  display: grid;
  grid-template-columns: 1.4fr 1fr;
  gap: 60px;
  align-items: end;
  position: relative;
}
.ph-eyebrow {
  display: inline-block;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.15em;
  padding: 8px 14px;
  margin-bottom: 24px;
  text-transform: uppercase;
  font-weight: 500;
}
.formula-display {
  font-family: var(--cond);
  font-size: 92px;
  font-weight: 800;
  line-height: 0.95;
  letter-spacing: -0.025em;
  margin-bottom: 12px;
}
.formula-display sub {
  font-size: 56px;
  font-weight: 600;
}
.page-hero h1 {
  font-family: var(--cond);
  font-size: 52px;
  line-height: 1.05;
  font-weight: 700;
  letter-spacing: -0.02em;
  margin-bottom: 22px;
  color: var(--gold);
}
.ph-sub {
  font-size: 18px;
  color: rgba(255, 255, 255, 0.85);
  line-height: 1.55;
  max-width: 620px;
  margin-bottom: 32px;
}
.ph-sub b {
  color: var(--gold-soft);
  font-weight: 700;
}
.ph-quickstats {
  display: flex;
  gap: 40px;
}
.ph-stat .num {
  font-family: var(--cond);
  font-size: 44px;
  font-weight: 800;
  line-height: 1;
  color: var(--gold);
}
.ph-stat .lbl {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.13em;
  color: rgba(255, 255, 255, 0.7);
  margin-top: 6px;
  text-transform: uppercase;
}
.ph-side {
  padding-bottom: 14px;
}
.grade-cards {
  display: flex;
  flex-direction: column;
  gap: 14px;
}
.grade-card {
  background: rgba(255, 255, 255, 0.06);
  border-left: 3px solid var(--orange);
  padding: 18px 22px;
  transition:
    background 0.15s,
    border-color 0.15s;
  cursor: pointer;
}
.grade-card:hover {
  background: rgba(255, 255, 255, 0.1);
  border-left-color: var(--gold);
}
.grade-card .grade-label {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.15em;
  color: var(--gold);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 4px;
}
.grade-card .grade-title {
  font-family: var(--cond);
  font-size: 22px;
  font-weight: 700;
  color: #fff;
  letter-spacing: -0.005em;
}
.grade-card .grade-use {
  font-family: var(--mono);
  font-size: 10.5px;
  color: rgba(255, 255, 255, 0.6);
  margin-top: 4px;
  letter-spacing: 0.05em;
}

/* CERT STRIP */
.cert-strip {
  background: var(--bg-soft);
  padding: 24px 0;
  border-bottom: 1px solid var(--line);
}
.cert-strip .wrap {
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 22px;
}
.cs-label {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.15em;
  color: var(--navy);
  text-transform: uppercase;
  font-weight: 500;
}
.cs-badges {
  display: flex;
  gap: 28px;
  flex-wrap: wrap;
}
.cs-badges span {
  font-family: var(--cond);
  font-size: 16px;
  font-weight: 700;
  color: var(--navy);
  display: flex;
  align-items: center;
  gap: 7px;
  letter-spacing: 0.3px;
  text-transform: uppercase;
}
.cs-badges span::before {
  content: "✓";
  color: var(--orange);
  font-weight: 700;
}

/* SECTION */
section.s {
  padding: 80px 0;
}
.sec-num {
  font-family: var(--mono);
  font-size: 11.5px;
  letter-spacing: 0.2em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 500;
  margin-bottom: 14px;
}
h2 {
  font-family: var(--cond);
  font-size: 48px;
  font-weight: 800;
  line-height: 1.05;
  letter-spacing: -0.02em;
  color: var(--ink);
  margin-bottom: 16px;
}
h2 .accent {
  color: var(--orange);
}
.sec-lead {
  color: var(--ink-soft);
  font-size: 16.5px;
  max-width: 720px;
  line-height: 1.6;
  margin-bottom: 44px;
}
.sec-lead b {
  color: var(--navy);
  font-weight: 700;
}

/* PROPERTIES TABLE */
.props-wrap {
  background: var(--navy);
  color: #fff;
  padding: 8px;
  overflow-x: auto;
}
table.props {
  width: 100%;
  border-collapse: collapse;
}
table.props th,
table.props td {
  padding: 16px 18px;
  text-align: left;
  font-size: 14px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
}
table.props thead th {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.13em;
  color: var(--gold);
  text-transform: uppercase;
  font-weight: 500;
  padding-top: 22px;
  padding-bottom: 14px;
  border-bottom: 2px solid rgba(255, 255, 255, 0.15);
  background: var(--navy-2);
}
table.props tbody th {
  font-family: var(--cond);
  font-size: 18px;
  font-weight: 700;
  color: #fff;
}
table.props tbody td {
  color: rgba(255, 255, 255, 0.85);
  font-family: var(--cond);
  font-size: 18px;
  font-weight: 700;
}
table.props tbody td.unit {
  font-family: var(--mono);
  font-size: 11px;
  color: rgba(255, 255, 255, 0.55);
  font-weight: 400;
  letter-spacing: 0.05em;
}
table.props tbody tr:hover {
  background: rgba(255, 255, 255, 0.03);
}
.props-legend {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--ink-soft);
  margin-top: 16px;
  letter-spacing: 0.05em;
}

/* OPTION B — card per voltage variant */
.b-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 22px;
}
.b-card {
  background: #fff;
  border: 1px solid var(--line);
  position: relative;
  display: flex;
  flex-direction: column;
  transition:
    border-color 0.15s,
    transform 0.15s,
    box-shadow 0.15s;
}
.b-card:hover {
  border-color: var(--orange);
  transform: translateY(-4px);
  box-shadow: 0 26px 50px -26px rgba(10, 38, 71, 0.22);
}
.b-card.featured {
  border-color: var(--orange);
  border-width: 2px;
}
.b-badge {
  position: absolute;
  top: 0;
  right: 0;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 9.5px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  font-weight: 600;
  padding: 6px 12px;
}
.b-head {
  background: var(--navy);
  color: #fff;
  padding: 24px 26px 22px;
}
.b-card.featured .b-head {
  background: linear-gradient(135deg, var(--navy) 0%, var(--navy-3) 100%);
}
.b-model {
  font-family: var(--cond);
  font-size: 28px;
  font-weight: 800;
  letter-spacing: -0.01em;
  line-height: 1;
}
.b-volt {
  font-family: var(--mono);
  font-size: 12px;
  color: var(--gold);
  letter-spacing: 0.1em;
  margin-top: 8px;
  text-transform: uppercase;
}
.b-body {
  padding: 8px 26px 24px;
  flex: 1;
}
.b-row {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  padding: 14px 0;
  border-bottom: 1px solid var(--line);
}
.b-row:last-child {
  border-bottom: none;
}
.b-k {
  font-family: var(--mono);
  font-size: 10.5px;
  color: var(--ink-faint);
  letter-spacing: 0.05em;
  text-transform: uppercase;
}
.b-v {
  font-family: var(--cond);
  font-size: 21px;
  font-weight: 700;
  color: var(--navy);
}
.b-v small {
  font-family: var(--mono);
  font-size: 10px;
  color: var(--ink-faint);
  font-weight: 400;
  margin-left: 3px;
}
.b-v.hot {
  color: var(--orange);
}
.b-foot {
  padding: 18px 26px;
  background: var(--bg-soft);
  border-top: 1px solid var(--line);
  font-family: var(--mono);
  font-size: 11px;
  color: var(--ink-soft);
  letter-spacing: 0.04em;
}
.b-foot b {
  color: var(--navy);
}
.common-note {
  font-family: var(--mono);
  font-size: 11.5px;
  color: var(--ink-soft);
  margin-top: 16px;
  letter-spacing: 0.03em;
}

/* OPTION C — borderless minimal */
.c-table {
  width: 100%;
  border-collapse: collapse;
}
.c-table thead th {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.13em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 500;
  text-align: left;
  padding: 0 24px 18px 0;
  border-bottom: 2px solid var(--ink);
}
.c-table thead th.num {
  text-align: right;
}
.c-table tbody th {
  font-family: var(--sans);
  font-size: 15px;
  font-weight: 600;
  color: var(--ink);
  text-align: left;
  padding: 18px 24px 18px 0;
  border-bottom: 1px solid var(--line);
  white-space: nowrap;
}
.c-table tbody td {
  font-family: var(--mono);
  font-size: 15px;
  color: var(--ink);
  padding: 18px 24px 18px 0;
  border-bottom: 1px solid var(--line);
  text-align: right;
}
.c-table tbody td.unit {
  color: var(--ink-faint);
  font-size: 12px;
  text-align: left;
  letter-spacing: 0.03em;
}
.c-table tbody tr:hover th,
.c-table tbody tr:hover td {
  background: var(--bg-soft);
}
.c-table tbody td b {
  color: var(--orange);
  font-weight: 500;
}

@media (max-width: 1024px) {
  .b-grid {
    grid-template-columns: 1fr;
  }
}

/* APPLICATIONS GRID */
.app-uses {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 22px;
}
.app-use {
  background: #fff;
  border: 1px solid var(--line);
  padding: 28px;
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 14px;
  transition:
    border-color 0.15s,
    transform 0.15s;
}
.app-use:hover {
  border-color: var(--orange);
  transform: translateY(-3px);
  box-shadow: 0 20px 40px -20px rgba(10, 38, 71, 0.15);
}
.app-use-bar {
  position: absolute;
  top: -1px;
  left: 28px;
  width: 40px;
  height: 3px;
  background: var(--orange);
}
.app-use-icon {
  font-size: 32px;
  line-height: 1;
  margin-top: 8px;
}
.app-use-title {
  font-family: var(--cond);
  font-size: 22px;
  font-weight: 700;
  color: var(--navy);
  line-height: 1.15;
  letter-spacing: -0.005em;
}
.app-use-desc {
  font-size: 13.5px;
  color: var(--ink-soft);
  line-height: 1.55;
  flex: 1;
}
.app-use-grade {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--orange);
  letter-spacing: 0.05em;
  padding-top: 12px;
  border-top: 1px solid var(--line);
  font-weight: 600;
}
.app-use-grade b {
  color: var(--navy);
}

/* COMPARISON CARDS */
.vs-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 28px;
}
.vs-card {
  background: #fff;
  border: 1px solid var(--line);
  padding: 32px;
}
.vs-card.win {
  border-color: var(--orange);
  border-width: 2px;
  background: linear-gradient(180deg, #fff 0%, var(--bg-soft) 100%);
}
.vs-card-head {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  margin-bottom: 16px;
  padding-bottom: 16px;
  border-bottom: 1px solid var(--line);
}
.vs-card-head h3 {
  font-family: var(--cond);
  font-size: 26px;
  font-weight: 800;
  color: var(--navy);
  letter-spacing: -0.01em;
}
.vs-tag {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.15em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 700;
  background: rgba(242, 100, 25, 0.08);
  padding: 4px 10px;
}
.vs-tag.alumina-tag {
  background: var(--orange);
  color: #fff;
}
.vs-points {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.vs-points li {
  display: flex;
  gap: 10px;
  align-items: baseline;
  font-size: 14px;
  color: var(--ink-soft);
  line-height: 1.5;
}
.vs-points li::before {
  content: "→";
  color: var(--orange);
  flex-shrink: 0;
  font-weight: 700;
}
.vs-points li b {
  color: var(--navy);
  font-weight: 700;
}

/* CTA BAND */
.casecta {
  background: var(--bg-soft);
  padding: 60px 0;
  border-top: 1px solid var(--line);
  border-bottom: 1px solid var(--line);
}
.casecta .wrap {
  display: grid;
  grid-template-columns: 1.3fr 1fr;
  gap: 50px;
  align-items: center;
}
.casecta-eyebrow {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.18em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 14px;
}
.casecta h2 {
  font-size: 38px;
  line-height: 1.08;
  margin-bottom: 18px;
}
.casecta p {
  font-size: 16px;
  color: var(--ink-soft);
  margin-bottom: 26px;
  line-height: 1.6;
}
.casecta-ctas {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}
.btn-primary {
  background: var(--orange);
  color: #fff;
  padding: 16px 28px;
  font-weight: 800;
  font-size: 14px;
  letter-spacing: 0.2px;
  border: none;
  cursor: pointer;
  transition: background 0.15s;
  display: inline-flex;
  align-items: center;
  gap: 8px;
}
.btn-primary:hover {
  background: var(--orange-deep);
}
.btn-ghost {
  background: transparent;
  color: var(--navy);
  padding: 16px 26px;
  font-weight: 700;
  font-size: 14px;
  letter-spacing: 0.2px;
  border: 2px solid var(--navy);
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 8px;
}
.btn-ghost:hover {
  border-color: var(--orange);
  color: var(--orange);
}
.casecta-side {
  background: #fff;
  border: 1px solid var(--line);
  padding: 28px;
  position: relative;
}
.casecta-side::before {
  content: "";
  position: absolute;
  top: -1px;
  left: 0;
  width: 70px;
  height: 4px;
  background: var(--orange);
}
.casecta-side-tag {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.15em;
  color: var(--orange);
  text-transform: uppercase;
  margin-bottom: 12px;
  font-weight: 600;
}
.casecta-side-title {
  font-family: var(--cond);
  font-size: 22px;
  font-weight: 700;
  color: var(--navy);
  margin-bottom: 16px;
  line-height: 1.2;
}
.casecta-side-list {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.casecta-side-list li {
  font-size: 13.5px;
  color: var(--ink-soft);
  display: flex;
  gap: 10px;
  align-items: baseline;
  padding-bottom: 10px;
  border-bottom: 1px dashed var(--line);
}
.casecta-side-list li:last-child {
  border-bottom: none;
  padding-bottom: 0;
}
.casecta-side-list li b {
  font-family: var(--cond);
  font-size: 15px;
  font-weight: 700;
  color: var(--navy);
  min-width: 70px;
}

/* FAQ */
.faq-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 18px 50px;
}
.faq-item {
  padding: 18px 0;
  border-bottom: 1px solid var(--line);
}
.faq-item:last-child,
.faq-item:nth-last-child(2) {
  border-bottom: none;
}
.faq-q {
  font-family: var(--cond);
  font-size: 19px;
  font-weight: 700;
  color: var(--navy);
  margin-bottom: 10px;
  letter-spacing: -0.005em;
  line-height: 1.25;
}
.faq-q::before {
  content: "Q";
  display: inline-block;
  width: 24px;
  height: 24px;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 12px;
  line-height: 24px;
  text-align: center;
  margin-right: 10px;
  vertical-align: middle;
  font-weight: 700;
}
.faq-a {
  font-size: 14px;
  color: var(--ink-soft);
  line-height: 1.6;
  padding-left: 34px;
}

footer {
  background: var(--navy);
  color: #c5d4e6;
  padding: 70px 0 30px;
}
.foot-grid {
  display: grid;
  grid-template-columns: 1.5fr 1fr 1fr 1fr 1.2fr;
  gap: 40px;
  margin-bottom: 50px;
}
.foot-col h5 {
  font-family: var(--cond);
  font-size: 16px;
  font-weight: 700;
  color: #fff;
  letter-spacing: 0.05em;
  margin-bottom: 16px;
  text-transform: uppercase;
}
.foot-col a {
  display: block;
  font-size: 13.5px;
  color: rgba(255, 255, 255, 0.65);
  margin-bottom: 9px;
  transition: color 0.15s;
}
.foot-col a:hover {
  color: var(--gold);
}
.fb-right p {
  font-size: 13.5px;
  color: rgba(255, 255, 255, 0.65);
  margin-bottom: 10px;
  line-height: 1.55;
}
.fb-right p b {
  color: #fff;
  font-weight: 600;
}
.foot-bottom {
  padding-top: 24px;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  font-family: var(--mono);
  font-size: 11.5px;
  color: rgba(255, 255, 255, 0.5);
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 14px;
}
.foot-bottom span {
  display: flex;
  gap: 22px;
}

@media (max-width: 1024px) {
  .page-hero .wrap {
    grid-template-columns: 1fr;
    gap: 40px;
  }
  .app-uses {
    grid-template-columns: 1fr 1fr;
  }
  .vs-grid {
    grid-template-columns: 1fr;
  }
  .casecta .wrap {
    grid-template-columns: 1fr;
  }
  .faq-grid {
    grid-template-columns: 1fr;
    gap: 0;
  }
  .foot-grid {
    grid-template-columns: 1fr 1fr;
  }
}
@media (max-width: 640px) {
  .wrap {
    padding: 0 20px;
  }
  .formula-display {
    font-size: 72px;
  }
  .page-hero h1 {
    font-size: 38px;
  }
  .app-uses {
    grid-template-columns: 1fr;
  }
  .topbar .left,
  .nav-links {
    display: none;
  }
}

/* ============================================================
     PRODUCT-PAGE TEMPLATE (distinct from material-page template)
     - hero carries an inline Quick-Quote form (core CTR)
     - sub-product catalog lineup replaces the spec datasheet table
     - benefit checklist replaces the vs-comparison grid
     ============================================================ */

/* PRODUCT HERO (quote-led) */
.prod-hero {
  background: linear-gradient(135deg, var(--navy) 0%, var(--navy-2) 100%);
  color: #fff;
  padding: 64px 0 78px;
  position: relative;
  overflow: hidden;
}
.prod-hero-stripe {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(
    90deg,
    var(--orange) 0%,
    var(--gold) 50%,
    var(--orange) 100%
  );
}
.prod-hero .wrap {
  display: grid;
  grid-template-columns: 1.15fr 0.85fr;
  gap: 56px;
  align-items: start;
  position: relative;
}
.ph-eyebrow {
  display: inline-block;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.15em;
  padding: 8px 14px;
  margin-bottom: 22px;
  text-transform: uppercase;
  font-weight: 500;
}
.prod-hero h1 {
  font-family: var(--cond);
  font-size: 56px;
  line-height: 1.02;
  font-weight: 800;
  letter-spacing: -0.02em;
  margin-bottom: 20px;
  color: #fff;
}
.prod-hero h1 .hl {
  color: var(--gold);
}
.prod-hero .ph-sub {
  font-size: 17px;
  color: rgba(255, 255, 255, 0.85);
  line-height: 1.55;
  max-width: 600px;
  margin-bottom: 30px;
}
.prod-hero .ph-sub b {
  color: var(--gold-soft);
  font-weight: 700;
}
.ph-statbar {
  display: flex;
  gap: 36px;
  flex-wrap: wrap;
  padding-top: 26px;
  border-top: 1px solid rgba(255, 255, 255, 0.15);
}
.ph-statbar .num {
  font-family: var(--cond);
  font-size: 38px;
  font-weight: 800;
  line-height: 1;
  color: var(--gold);
}
.ph-statbar .lbl {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.12em;
  color: rgba(255, 255, 255, 0.7);
  margin-top: 6px;
  text-transform: uppercase;
}

/* QUICK-QUOTE FORM CARD (the conversion engine) */
.qquote {
  background: #fff;
  padding: 30px 30px 32px;
  position: relative;
  box-shadow: 0 30px 60px -25px rgba(0, 0, 0, 0.5);
}
.qquote::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: var(--orange);
}
.qquote-tag {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.16em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 8px;
}
.qquote-title {
  font-family: var(--cond);
  font-size: 27px;
  font-weight: 800;
  color: var(--navy);
  line-height: 1.08;
  letter-spacing: -0.01em;
  margin-bottom: 6px;
}
.qquote-note {
  font-size: 12.5px;
  color: var(--ink-soft);
  line-height: 1.5;
  margin-bottom: 20px;
}
.qq-field {
  margin-bottom: 13px;
}
.qq-field label {
  display: block;
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--ink-faint);
  font-weight: 500;
  margin-bottom: 5px;
}
.qq-field input,
.qq-field select,
.qq-field textarea {
  width: 100%;
  font-family: var(--sans);
  font-size: 14px;
  color: var(--ink);
  border: 1px solid var(--line-strong);
  padding: 11px 13px;
  background: var(--bg-soft);
  transition:
    border-color 0.15s,
    background 0.15s;
}
.qq-field input:focus,
.qq-field select:focus,
.qq-field textarea:focus {
  outline: none;
  border-color: var(--orange);
  background: #fff;
}
.qq-field textarea {
  resize: vertical;
  min-height: 64px;
}
.qq-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 13px;
}
.qq-submit {
  width: 100%;
  background: var(--orange);
  color: #fff;
  border: none;
  padding: 15px;
  font-family: var(--sans);
  font-weight: 800;
  font-size: 14.5px;
  letter-spacing: 0.3px;
  cursor: pointer;
  transition: background 0.15s;
  margin-top: 6px;
}
.qq-submit:hover {
  background: var(--orange-deep);
}
.qq-trust {
  font-family: var(--mono);
  font-size: 10.5px;
  color: var(--ink-faint);
  text-align: center;
  margin-top: 12px;
  letter-spacing: 0.03em;
}

/* SUB-PRODUCT LINEUP (catalog — replaces datasheet table) */
.lineup {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24px;
}
.lineup-card {
  background: #fff;
  border: 1px solid var(--line);
  padding: 30px;
  position: relative;
  display: flex;
  flex-direction: column;
  transition:
    border-color 0.15s,
    transform 0.15s,
    box-shadow 0.15s;
}
.lineup-card:hover {
  border-color: var(--orange);
  transform: translateY(-3px);
  box-shadow: 0 24px 46px -24px rgba(10, 38, 71, 0.18);
}
.lineup-card.flagship {
  border-color: var(--orange);
  border-width: 2px;
  background: linear-gradient(180deg, #fff 0%, var(--bg-soft) 100%);
}
.lineup-badge {
  position: absolute;
  top: 0;
  right: 0;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 9.5px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  font-weight: 600;
  padding: 6px 12px;
}
.lineup-head {
  display: flex;
  align-items: flex-start;
  gap: 14px;
  margin-bottom: 14px;
}
.lineup-icon {
  font-size: 30px;
  line-height: 1;
  flex-shrink: 0;
  margin-top: 2px;
}
.lineup-name {
  font-family: var(--cond);
  font-size: 25px;
  font-weight: 800;
  color: var(--navy);
  line-height: 1.08;
  letter-spacing: -0.01em;
}
.lineup-cn {
  font-family: var(--sans);
  font-size: 12.5px;
  color: var(--ink-faint);
  font-weight: 600;
  margin-top: 2px;
}
.lineup-desc {
  font-size: 13.5px;
  color: var(--ink-soft);
  line-height: 1.55;
  margin-bottom: 16px;
  flex: 1;
}
.lineup-specs {
  display: flex;
  flex-wrap: wrap;
  gap: 7px;
  margin-bottom: 18px;
}
.spec-chip {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.03em;
  color: var(--navy);
  background: var(--bg-soft-2);
  padding: 5px 9px;
  font-weight: 500;
}
.spec-chip b {
  color: var(--orange);
  font-weight: 600;
}
.lineup-foot {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 15px;
  border-top: 1px solid var(--line);
}
.lineup-mat {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--ink-soft);
  letter-spacing: 0.04em;
}
.lineup-mat b {
  color: var(--navy);
  font-weight: 600;
}
.lineup-mat a {
  color: var(--orange);
  font-weight: 600;
}
.lineup-link {
  font-family: var(--sans);
  font-size: 13px;
  font-weight: 800;
  color: var(--orange);
  letter-spacing: 0.2px;
  white-space: nowrap;
}
.lineup-link:hover {
  color: var(--orange-deep);
}

/* BENEFIT CHECKLIST (replaces vs-comparison grid) */
.benefits {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 22px 56px;
}
.benefit {
  display: flex;
  gap: 16px;
  align-items: flex-start;
  padding-bottom: 20px;
  border-bottom: 1px solid var(--line);
}
.benefit-ico {
  flex-shrink: 0;
  width: 30px;
  height: 30px;
  background: var(--orange);
  color: #fff;
  font-weight: 800;
  font-size: 15px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 2px;
}
.benefit-ico::before {
  content: "✓";
}
.benefit-body h4 {
  font-family: var(--cond);
  font-size: 20px;
  font-weight: 700;
  color: var(--navy);
  letter-spacing: -0.005em;
  margin-bottom: 5px;
  line-height: 1.15;
}
.benefit-body p {
  font-size: 13.5px;
  color: var(--ink-soft);
  line-height: 1.55;
}

@media (max-width: 1024px) {
  .prod-hero .wrap {
    grid-template-columns: 1fr;
    gap: 40px;
  }
  .lineup {
    grid-template-columns: 1fr;
  }
  .benefits {
    grid-template-columns: 1fr;
    gap: 0;
  }
}
@media (max-width: 640px) {
  .prod-hero h1 {
    font-size: 38px;
  }
  .qq-row {
    grid-template-columns: 1fr;
  }
  .lineup-foot {
    flex-direction: column;
    align-items: flex-start;
    gap: 10px;
  }
}

/* ============================================================
     PRODUCT DETAIL PAGE (BLK Si3N4) — extends product template
     ============================================================ */

/* HERO SPOTLIGHT CARD (right column on detail hero) */
.spotlight-tag {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.16em;
  color: var(--gold);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 18px;
}
.spotlight-row {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  padding: 13px 0;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}
.spotlight-row:last-child {
  border-bottom: none;
}
.spotlight-row .k {
  font-family: var(--mono);
  font-size: 11px;
  color: rgba(255, 255, 255, 0.6);
  letter-spacing: 0.05em;
}
.spotlight-row .v {
  font-family: var(--cond);
  font-size: 20px;
  font-weight: 700;
  color: #fff;
}
.spotlight-row .v b {
  color: var(--gold);
}
.hero-ctas {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  margin-top: 30px;
}

/* DOWNLOAD CARDS */
.dl-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20px;
}
.dl-card {
  background: #fff;
  border: 1px solid var(--line);
  padding: 26px;
  display: flex;
  flex-direction: column;
  gap: 12px;
  transition:
    border-color 0.15s,
    transform 0.15s;
  position: relative;
}
.dl-card:hover {
  border-color: var(--orange);
  transform: translateY(-3px);
  box-shadow: 0 20px 40px -22px rgba(10, 38, 71, 0.18);
}
.dl-icon {
  font-size: 30px;
  line-height: 1;
}
.dl-name {
  font-family: var(--cond);
  font-size: 21px;
  font-weight: 700;
  color: var(--navy);
  line-height: 1.12;
}
.dl-meta {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--ink-faint);
  letter-spacing: 0.05em;
}
.dl-btn {
  margin-top: auto;
  font-family: var(--sans);
  font-size: 13px;
  font-weight: 800;
  color: var(--orange);
  letter-spacing: 0.2px;
  padding-top: 10px;
  border-top: 1px solid var(--line);
}
.dl-btn:hover {
  color: var(--orange-deep);
}

/* CERTIFICATION DETAIL CARDS */
.cert-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20px;
}
.cert-card {
  background: #fff;
  border: 1px solid var(--line);
  padding: 24px;
  position: relative;
}
.cert-card::before {
  content: "";
  position: absolute;
  top: -1px;
  left: 0;
  width: 50px;
  height: 4px;
  background: var(--orange);
}
.cert-card .cc-mark {
  font-family: var(--cond);
  font-size: 26px;
  font-weight: 800;
  color: var(--navy);
  letter-spacing: -0.01em;
  margin-bottom: 6px;
}
.cert-card .cc-scope {
  font-size: 13px;
  color: var(--ink-soft);
  line-height: 1.5;
}
.cert-card .cc-tag {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.12em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 600;
  margin-top: 12px;
}

/* RELATED INTERNAL LINKS (Product -> Material + Application) */
.rel-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 22px;
}
.rel-card {
  background: #fff;
  border: 1px solid var(--line);
  padding: 28px;
  display: flex;
  flex-direction: column;
  gap: 10px;
  transition:
    border-color 0.15s,
    transform 0.15s;
}
.rel-card:hover {
  border-color: var(--orange);
  transform: translateY(-3px);
  box-shadow: 0 20px 40px -22px rgba(10, 38, 71, 0.15);
}
.rel-eyebrow {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.14em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 600;
}
.rel-title {
  font-family: var(--cond);
  font-size: 22px;
  font-weight: 700;
  color: var(--navy);
  line-height: 1.15;
}
.rel-desc {
  font-size: 13px;
  color: var(--ink-soft);
  line-height: 1.5;
}
.rel-link {
  font-family: var(--sans);
  font-size: 13px;
  font-weight: 800;
  color: var(--orange);
  margin-top: 4px;
}

/* QUOTE SECTION (full-width, 2-col) */
.quote-sec {
  background: var(--navy);
  color: #fff;
}
.quote-sec .wrap {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 60px;
  align-items: start;
}
.quote-sec .sec-num {
  color: var(--gold);
}
.quote-sec h2 {
  color: #fff;
}
.quote-sec h2 .accent {
  color: var(--gold);
}
.quote-why {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 16px;
  margin-top: 24px;
}
.quote-why li {
  display: flex;
  gap: 13px;
  align-items: flex-start;
  font-size: 15px;
  color: rgba(255, 255, 255, 0.88);
  line-height: 1.5;
}
.quote-why li::before {
  content: "✓";
  color: var(--gold);
  font-weight: 800;
  flex-shrink: 0;
}
.quote-why li b {
  color: #fff;
  font-weight: 700;
}
.quote-form-card {
  background: #fff;
  padding: 32px;
}
.quote-form-card .qquote-title {
  color: var(--navy);
}

@media (max-width: 1024px) {
  .dl-grid {
    grid-template-columns: 1fr;
  }
  .cert-grid {
    grid-template-columns: 1fr 1fr;
  }
  .rel-grid {
    grid-template-columns: 1fr;
  }
  .quote-sec .wrap {
    grid-template-columns: 1fr;
    gap: 36px;
  }
}
@media (max-width: 640px) {
  .cert-grid {
    grid-template-columns: 1fr;
  }
  .spotlight {
    margin-top: 0;
  }
}

.b-img-slot img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center 25%;
  display: block;
  transition: transform 0.3s ease;
}
.b-card:hover .b-img-slot img {
  transform: scale(1.05);
}
@media (max-width: 1024px) {
  .b-grid-6 {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* === Section 01 — 6-model card grid === */
.b-grid-6 {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
}
.b-img-slot {
  height: 200px;
  overflow: hidden;
  border-bottom: 1px solid var(--line);
  background: #f4f5f7;
}
.b-img-slot img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center 20%;
  display: block;
  transition: transform 0.35s ease;
}
.b-card:hover .b-img-slot img {
  transform: scale(1.06);
}
@media (max-width: 1024px) {
  .b-grid-6 {
    grid-template-columns: repeat(2, 1fr);
  }
}
@media (max-width: 580px) {
  .b-grid-6 {
    grid-template-columns: 1fr;
  }
}
</style>
