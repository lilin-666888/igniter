<script setup lang="ts">
useHead({
  title:
    "Biomass & Pellet Stove Igniters — Si₃N₄ CN-300 for High-Cycle Ignition | Ceramitell",
  meta: [
    {
      name: "description",
      content:
        "Silicon nitride (Si₃N₄) hot surface igniters for biomass and pellet stoves. 100K+ cycle life, 3-second heat-up, UL/CE certified. Italian OEM case: 180,000 units/year, −38% warranty returns. Free samples, 48h dispatch.",
    },
  ],
});
</script>

<template>
  <div>
    <div class="breadcrumb">
      <div class="wrap">
        <a href="/">Home</a>
        <span class="sep">/</span>
        <a href="/applications">Applications</a>
        <span class="sep">/</span>
        <a href="/applications/advanced-combustion-fixed"
          >Advanced Combustion</a
        >
        <span class="sep">/</span>
        <span>AlN Substrates</span>
      </div>
    </div>

    <!-- HERO -->
    <section
      class="bio-hero"
      style="
        background: linear-gradient(
          135deg,
          #05101a 0%,
          #0a1e30 50%,
          var(--navy) 100%
        );
      "
    >
      <div class="bio-hero-stripe"></div>
      <div class="wrap">
        <div>
          <div class="bio-hero-eyebrow">
            Semiconductor & Electronics · AlN Substrates
          </div>
          <h1>
            AlN ceramic substrates for<br /><span class="hl"
              >power &amp; RF packaging.</span
            >
          </h1>
          <p class="bio-hero-sub">
            Aluminum nitride substrates for
            <b
              >power module packaging, RF device packaging, and high-frequency
              electronics</b
            >. Thermal conductivity 170 W/m·K — 8× Al₂O₃ — CTE 4.5 ppm/°C
            closely matched to SiC and GaN-on-SiC. The substrate material for
            next-generation EV power modules.
          </p>
          <div class="bio-statbar">
            <div>
              <div class="num">170 W/m·K</div>
              <div class="lbl">Thermal Conductivity</div>
            </div>
            <div>
              <div class="num">4.5 ppm/°C</div>
              <div class="lbl">CTE — matches SiC/GaN</div>
            </div>
            <div>
              <div class="num">15 kV/mm</div>
              <div class="lbl">Dielectric Strength</div>
            </div>
            <div>
              <div class="num">DBC/AMB</div>
              <div class="lbl">Metallisation Ready</div>
            </div>
          </div>
          <div class="bio-hero-ctas">
            <a class="btn-primary" href="#quote">Request a Quote →</a>
            <a
              class="btn-ghost"
              style="color: #fff; border-color: rgba(255, 255, 255, 0.4)"
              href="/products/custom-ceramic"
              >View Custom DFM →</a
            >
          </div>
          <div class="bio-hero-nav-bottom">
            <span class="nav-back"
              >←
              <a href="/applications/app-semiconductor"
                >Back to Semiconductor & Electronics</a
              ></span
            >
            <span class="nav-back"
              >↑ <a href="/applications">All 8 Application Verticals</a></span
            >
          </div>
        </div>
        <div>
          <div class="bio-proof">
            <div class="bio-proof-eyebrow">
              ★ Why AlN enables higher power density
            </div>
            <div class="bio-proof-title">
              AlN substrate thermal resistance vs alternatives
            </div>
            <div class="bio-proof-grid">
              <div class="bio-proof-stat">
                <div class="bn">8×</div>
                <div class="bl">AlN conductivity vs Al₂O₃</div>
              </div>
              <div class="bio-proof-stat">
                <div class="bn">4.5 ppm/°C</div>
                <div class="bl">CTE vs SiC 4.0 ppm/°C</div>
              </div>
              <div class="bio-proof-stat">
                <div class="bn">1.8 kW/cm²</div>
                <div class="bl">Modern SiC module power density</div>
              </div>
              <div class="bio-proof-stat">
                <div class="bn">1 kW/cm²</div>
                <div class="bl">Max density with Al₂O₃ substrate</div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>
    <section class="cert-strip">
      <div class="wrap">
        <div class="cs-label">Certifications &amp; Standards</div>
        <div class="cs-badges">
          <span>ISO 9001</span><span>IATF 16949</span><span>CE</span
          ><span>RoHS · REACH</span><span>UL Recognized</span>
        </div>
      </div>
    </section>
    <section class="mkt-band">
      <div class="wrap">
        <div class="mkt-grid">
          <div class="mkt-item">
            <div class="mkt-num">1.8 kW/cm²</div>
            <div class="mkt-label">
              Power density of modern SiC power modules — AlN substrate thermal
              resistance is the limiting factor
            </div>
          </div>
          <div class="mkt-item">
            <div class="mkt-num">8×</div>
            <div class="mkt-label">
              AlN thermal conductivity vs Al₂O₃ — decisive for
              high-power-density packaging
            </div>
          </div>
          <div class="mkt-item">
            <div class="mkt-num">4.5 ppm/°C</div>
            <div class="mkt-label">
              AlN CTE — closest match to SiC (4.0) and GaN-on-SiC (4.2) of any
              ceramic substrate
            </div>
          </div>
          <div class="mkt-item">
            <div class="mkt-num">EV</div>
            <div class="mkt-label">
              Electric vehicle power modules — the fastest-growing AlN substrate
              application
            </div>
          </div>
        </div>
      </div>
    </section>
    <section class="s" style="padding: 72px 0">
      <div class="wrap">
        <div class="sec-num">01 · The Application</div>
        <h2>
          AlN substrates enable
          <span class="accent">higher power density</span>.
        </h2>
        <div class="depth-grid">
          <div>
            <div
              class="depth-img"
              style="
                background: linear-gradient(
                  145deg,
                  #05101a 0%,
                  #0a1e30 40%,
                  #0d2640 100%
                );
              "
            >
              <div class="di-icon">📐</div>
              <div class="di-label">
                AlN substrate — DBC metallised for SiC power module
              </div>
              <div class="di-replace">Replace with product photo</div>
            </div>
          </div>
          <div>
            <p
              style="
                font-size: 16px;
                color: var(--ink-soft);
                line-height: 1.65;
                margin-bottom: 24px;
              "
            >
              Power module substrates must simultaneously provide: electrical
              isolation (dielectric strength ≥15 kV/mm), maximum thermal
              conductivity to minimise junction temperature rise, and CTE
              matched to the die to prevent solder fatigue. AlN achieves all
              three — making it the substrate of choice for SiC and GaN power
              modules.
            </p>
            <p
              style="font-size: 16px; color: var(--ink-soft); line-height: 1.65"
            >
              AlN CTE of 4.5 ppm/°C is the closest match to SiC (4.0 ppm/°C) and
              GaN-on-SiC (4.2 ppm/°C) of any ceramic substrate — virtually
              eliminating CTE-mismatch solder fatigue in SiC and GaN power
              modules.
            </p>
            <div class="tech-req-label" style="margin-top: 24px">
              Technical Requirements
            </div>
            <div class="tech-row">
              <span class="tech-k">Thermal conductivity</span
              ><span class="tech-v">170 W/m·K</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">CTE</span
              ><span class="tech-v">4.5 ppm/°C — matches SiC/GaN</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Dielectric strength</span
              ><span class="tech-v">≥15 kV/mm</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Surface finish</span
              ><span class="tech-v">Ra ≤0.4µm — metallisation ready</span>
            </div>
          </div>
        </div>
      </div>
    </section>
    <section class="comp-band" style="background: var(--bg-soft)">
      <div class="wrap">
        <div class="sec-num">02 · Material Selection</div>
        <h2>AlN vs Al₂O₃ for</h2>
        <p class="sec-lead">power module substrates</p>
        <div class="comp-grid">
          <div class="comp-card winner">
            <span class="comp-badge win">★ Recommended</span>
            <div class="comp-name">Aluminum Nitride (AlN)</div>
            <div class="comp-tagline">
              170 W/m·K · CTE 4.5 · next-gen SiC/GaN substrate
            </div>
            <ul class="comp-list">
              <li>
                <span class="ci">✓</span>
                <div>
                  <b>170 W/m·K thermal conductivity</b> — 7× lower thermal
                  resistance than Al₂O₃
                </div>
              </li>
              <li>
                <span class="ci">✓</span>
                <div>
                  <b>CTE 4.5 ppm/°C</b> — virtually eliminates CTE mismatch
                  solder fatigue with SiC/GaN
                </div>
              </li>
              <li>
                <span class="ci">✓</span>
                <div>
                  <b>Ra ≤0.4µm</b> — ready for DBC and AMB metallisation
                </div>
              </li>
              <li>
                <span class="ci">✓</span>
                <div>
                  <b>Dielectric strength ≥15 kV/mm</b> — qualified for 1,200V
                  SiC module isolation
                </div>
              </li>
              <li>
                <span class="ci">✓</span>
                <div>
                  <b>AEC-Q200 grade</b> material documentation for automotive
                  qualification
                </div>
              </li>
            </ul>
            <div class="comp-cta-row">
              <a class="btn-primary" href="/products/custom-ceramic"
                >View Full Specs →</a
              >
            </div>
          </div>
          <div class="comp-card">
            <span class="comp-badge alt">Also Available</span>
            <div class="comp-name">Alumina (Al₂O₃)</div>
            <div class="comp-tagline">
              25 W/m·K · CTE 7.4 · legacy substrate
            </div>
            <ul class="comp-list">
              <li>
                <span class="ci">→</span>
                <div>
                  Lower cost — standard ceramic substrate for &lt;600W modules
                </div>
              </li>
              <li>
                <span class="ci">→</span>
                <div>Established supply chain and processing</div>
              </li>
              <li>
                <span class="ci" style="color: var(--orange)">⚠</span>
                <div>
                  <b>CTE mismatch 7.4 vs SiC 4.0</b> — causes solder fatigue in
                  high-cycle power modules
                </div>
              </li>
              <li>
                <span class="ci" style="color: var(--orange)">⚠</span>
                <div>
                  <b>7× higher thermal resistance</b> — limits power density to
                  ~300 W/cm²
                </div>
              </li>
            </ul>
          </div>
        </div>
      </div>
    </section>
    <section class="casecta">
      <div class="wrap">
        <div>
          <div class="casecta-eyebrow">
            Power electronics substrate applications
          </div>
          <h2>
            Enabling next-generation<br /><span style="color: var(--gold)"
              >SiC and GaN power modules.</span
            >
          </h2>
          <div class="casecta-ctas">
            <a class="casecta-primary" href="#quote">Request a Quote →</a>
            <a class="casecta-secondary" href="/contact">Talk to an Engineer</a>
          </div>
        </div>
        <div class="casecta-side">
          <div class="casecta-side-tag">AlN substrate program</div>
          <ul class="casecta-side-list">
            <li>
              <b>CTE 4.5 ppm/°C:</b> closest match to SiC/GaN of any ceramic
            </li>
            <li><b>DBC/AMB ready:</b> Ra ≤0.4µm, semiconductor-grade clean</li>
            <li><b>Panel sizes:</b> 50×50mm to 150×200mm standard</li>
            <li><b>AEC-Q200:</b> automotive power electronics documentation</li>
            <li>
              <b>EV qualified:</b> supply to Tier-1 EV drivetrain suppliers
            </li>
          </ul>
        </div>
      </div>
    </section>
    <section class="failure-band">
      <div class="wrap">
        <div class="sec-num" style="color: var(--gold)">
          03 · Power Module Substrate Failures
        </div>
        <h2 style="color: #fff">
          Critical substrate failure modes in power cycling.
        </h2>
        <p
          style="
            color: rgba(255, 255, 255, 0.75);
            font-size: 16px;
            max-width: 680px;
            margin-top: 10px;
            line-height: 1.6;
          "
        >
          Two failure modes dominate — both are more severe with Al₂O₃
          substrates than with AlN.
        </p>
        <div class="failure-grid">
          <div class="fail-card worst">
            <div class="fail-num">01</div>
            <div class="fail-name">CTE mismatch solder fatigue</div>
            <div class="fail-desc">
              When substrate CTE differs from die CTE (Al₂O₃ 7.4 vs SiC 4.0
              ppm/°C), differential expansion during power cycling accumulates
              as solder joint fatigue. AlN's 4.5 ppm/°C virtually eliminates
              this with SiC and GaN dies.
            </div>
            <div class="fail-solution">
              AlN fix → CTE 4.5 ppm/°C — same order of magnitude as SiC/GaN.
              Solder fatigue life 5–10× longer than Al₂O₃ in SiC module power
              cycling.
            </div>
          </div>
          <div class="fail-card">
            <div class="fail-num">02</div>
            <div class="fail-name">Thermal resistance bottleneck</div>
            <div class="fail-desc">
              Al₂O₃ at 25 W/m·K creates 0.3°C·cm²/W substrate thermal resistance
              — limiting power density to ~300 W/cm². AlN at 170 W/m·K reduces
              this 7× to ~0.04°C·cm²/W, enabling &gt;1 kW/cm².
            </div>
            <div class="fail-solution">
              AlN fix → 7× lower thermal resistance enables the junction
              temperature margin required for reliable SiC module operation at
              1.8 kW/cm².
            </div>
          </div>
          <div class="fail-card">
            <div class="fail-num">03</div>
            <div class="fail-name">Metallisation delamination</div>
            <div class="fail-desc">
              Poor AlN surface finish or contamination prevents DBC copper
              adhesion. Our substrates are supplied Ra ≤0.4µm and
              semiconductor-grade clean — validated to 1,000 thermal cycles
              −55°C to +150°C.
            </div>
            <div class="fail-solution">
              AlN fix → Ra ≤0.4µm surface finish standard. 1,000-cycle
              delamination test data available for qualification.
            </div>
          </div>
        </div>
      </div>
    </section>
    <section class="s" style="background: var(--bg-soft)">
      <div class="wrap">
        <div class="sec-num">04 · Engineering FAQ</div>
        <h2>Questions from power module engineers.</h2>
        <div class="faq-grid">
          <div class="faq-item">
            <div class="faq-q">What CTE does AlN match?</div>
            <div class="faq-a">
              AlN CTE 4.5 ppm/°C. SiC: 4.0. GaN-on-SiC: 4.2. The closest CTE
              match of any ceramic substrate to next-generation power
              semiconductors. Al₂O₃ at 7.4 is significantly mismatched.
            </div>
          </div>
          <div class="faq-item">
            <div class="faq-q">DBC and AMB compatible?</div>
            <div class="faq-a">
              Yes — Ra ≤0.4µm surface finish standard. Both DBC and AMB
              metallisation processes validated. Bare substrates or referral to
              metallisation partners.
            </div>
          </div>
          <div class="faq-item">
            <div class="faq-q">Standard sizes?</div>
            <div class="faq-a">
              50×50mm, 100×100mm, 120×180mm, 150×200mm standard panels. Custom
              sizes and diced substrates to module footprint with 4-week lead
              time.
            </div>
          </div>
          <div class="faq-item">
            <div class="faq-q">AEC-Q200 certification for automotive?</div>
            <div class="faq-a">
              AEC-Q200 grade AlN with full lot traceability, PPAP documentation,
              and endurance test data for automotive power electronics
              qualification.
            </div>
          </div>
        </div>
      </div>
    </section>
    <section class="s quote-sec" id="quote">
      <div class="wrap">
        <div>
          <div class="sec-num">Request a Quote</div>
          <h2>
            Ready to spec
            <span class="accent">your AlN power module substrate</span>?
          </h2>
          <p class="sec-lead" style="color: rgba(255, 255, 255, 0.8)">
            Specify substrate size, thickness, metallisation requirements, and
            annual volume. DFM review in 48h.
          </p>
          <ul class="quote-why">
            <li><b>CTE 4.5 ppm/°C</b> — closest match to SiC/GaN.</li>
            <li><b>DBC/AMB metallisation ready</b> surface finish.</li>
            <li><b>AEC-Q200 documentation</b> for automotive.</li>
            <li><b>DFM review in 48h</b> from drawing receipt.</li>
          </ul>
        </div>
        <div class="quote-form-card">
          <div class="qquote-tag">Free Samples · Reply in 24h</div>
          <div class="qquote-title">Request AlN substrate samples or quote</div>
          <div class="qquote-note">
            Specify size, thickness, and metallisation requirements.
          </div>
          <QuoteInquiryForm variant="card" submit-label="Request Samples →" />
        </div>
      </div>
    </section>
    <section class="s">
      <div class="wrap">
        <div class="sec-num">Related</div>
        <h2>Explore <span class="accent">further</span>.</h2>
        <div class="rel-grid">
          <a href="/applications/sub-semiconductor-aln-heaters" class="rel-card"
            ><div class="rel-eyebrow">Application</div>
            <div class="rel-title">AlN Heaters for Semiconductor</div>
            <div class="rel-desc">
              AlN wafer chuck heaters — same material, different application.
            </div>
            <div class="rel-link">View →</div></a
          ><a href="/applications/app-semiconductor" class="rel-card"
            ><div class="rel-eyebrow">Application</div>
            <div class="rel-title">Semiconductor & Electronics</div>
            <div class="rel-desc">
              All ceramic applications in semiconductor manufacturing.
            </div>
            <div class="rel-link">View →</div></a
          ><a href="/products/custom-ceramic" class="rel-card"
            ><div class="rel-eyebrow">Product</div>
            <div class="rel-title">Custom Ceramic DFM</div>
            <div class="rel-desc">
              AlN and Al₂O₃ custom substrates to your drawing.
            </div>
            <div class="rel-link">View →</div></a
          >
        </div>
      </div>
    </section>
  </div>
</template>

<style>
:root {
  --navy: #0a2647;
  --navy-2: #103a6b;
  --navy-3: #1b4d8a;
  --bg: #ffffff;
  --bg-soft: #f1f5fa;
  --bg-soft-2: #e6eef7;
  --ink: #14233a;
  --ink-soft: #4a5a70;
  --ink-faint: #8794a6;
  --orange: #f26419;
  --orange-deep: #d8530f;
  --gold: #f0a500;
  --gold-soft: #ffd466;
  --line: #dce5ef;
  --line-strong: #c2d0e0;
  --sans: "Barlow", sans-serif;
  --cond: "Barlow Condensed", sans-serif;
  --mono: "IBM Plex Mono", monospace;
}
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}
body {
  background: var(--bg);
  color: var(--ink);
  font-family: var(--sans);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}
a {
  color: inherit;
  text-decoration: none;
  cursor: pointer;
}
.wrap {
  max-width: 1260px;
  margin: 0 auto;
  padding: 0 40px;
}

/* CHROME */
.topbar {
  background: var(--navy);
  color: #c5d4e6;
  font-family: var(--mono);
  font-size: 12px;
}
.topbar .wrap {
  display: flex;
  justify-content: space-between;
  align-items: center;
  height: 38px;
}
.topbar .left {
  display: flex;
  gap: 26px;
}
.topbar .left span {
  display: flex;
  align-items: center;
  gap: 7px;
}
.dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--gold);
}
.topbar .right {
  letter-spacing: 0.05em;
  color: var(--gold);
}
nav {
  position: sticky;
  top: 0;
  z-index: 100;
  background: #fff;
  border-bottom: 3px solid var(--navy);
  box-shadow: 0 2px 12px rgba(10, 38, 71, 0.06);
}
nav .wrap {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 78px;
}
.logo {
  display: flex;
  align-items: center;
  gap: 12px;
}
.logo-mark {
  width: 42px;
  height: 42px;
}
.logo-text {
  font-weight: 800;
  font-size: 24px;
  letter-spacing: -0.02em;
  color: var(--navy);
}
.logo-text b {
  color: var(--orange);
}
.logo-text small {
  display: block;
  font-family: var(--mono);
  font-size: 9px;
  letter-spacing: 0.16em;
  color: var(--ink-faint);
  font-weight: 400;
  margin-top: -2px;
}
.nav-links {
  display: flex;
  gap: 32px;
  align-items: center;
}
.nav-links a {
  font-size: 14.5px;
  font-weight: 700;
  color: var(--navy);
  padding: 6px 0;
  letter-spacing: 0.2px;
}
.nav-links a.active {
  color: var(--orange);
  border-bottom: 2px solid var(--orange);
}
.nav-links a:hover {
  color: var(--orange);
}
.nav-cta {
  background: var(--orange);
  color: #fff !important;
  padding: 12px 24px;
  font-weight: 800;
  font-size: 13.5px;
  letter-spacing: 0.5px;
  transition: background 0.15s;
}
.nav-cta:hover {
  background: var(--orange-deep);
}

.breadcrumb {
  background: var(--bg-soft);
  padding: 14px 0;
  border-bottom: 1px solid var(--line);
}
.breadcrumb .wrap {
  display: flex;
  gap: 10px;
  align-items: center;
  font-family: var(--mono);
  font-size: 12px;
  color: var(--ink-soft);
  letter-spacing: 0.05em;
}
.breadcrumb a {
  color: var(--navy);
  font-weight: 600;
}
.breadcrumb a:hover {
  color: var(--orange);
}
.breadcrumb .sep {
  color: var(--ink-faint);
}

/* HERO */
.page-hero {
  background: linear-gradient(135deg, var(--navy) 0%, var(--navy-2) 100%);
  color: #fff;
  padding: 70px 0 90px;
  position: relative;
  overflow: hidden;
}
.page-hero-stripe {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(
    90deg,
    var(--orange) 0%,
    var(--gold) 50%,
    var(--orange) 100%
  );
}
.page-hero .wrap {
  display: grid;
  grid-template-columns: 1.4fr 1fr;
  gap: 60px;
  align-items: end;
  position: relative;
}
.ph-eyebrow {
  display: inline-block;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.15em;
  padding: 8px 14px;
  margin-bottom: 24px;
  text-transform: uppercase;
  font-weight: 500;
}
.formula-display {
  font-family: var(--cond);
  font-size: 92px;
  font-weight: 800;
  line-height: 0.95;
  letter-spacing: -0.025em;
  margin-bottom: 12px;
}
.formula-display sub {
  font-size: 56px;
  font-weight: 600;
}
.page-hero h1 {
  font-family: var(--cond);
  font-size: 52px;
  line-height: 1.05;
  font-weight: 700;
  letter-spacing: -0.02em;
  margin-bottom: 22px;
  color: var(--gold);
}
.ph-sub {
  font-size: 18px;
  color: rgba(255, 255, 255, 0.85);
  line-height: 1.55;
  max-width: 620px;
  margin-bottom: 32px;
}
.ph-sub b {
  color: var(--gold-soft);
  font-weight: 700;
}
.ph-quickstats {
  display: flex;
  gap: 40px;
}
.ph-stat .num {
  font-family: var(--cond);
  font-size: 44px;
  font-weight: 800;
  line-height: 1;
  color: var(--gold);
}
.ph-stat .lbl {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.13em;
  color: rgba(255, 255, 255, 0.7);
  margin-top: 6px;
  text-transform: uppercase;
}
.ph-side {
  padding-bottom: 14px;
}
.grade-cards {
  display: flex;
  flex-direction: column;
  gap: 14px;
}
.grade-card {
  background: rgba(255, 255, 255, 0.06);
  border-left: 3px solid var(--orange);
  padding: 18px 22px;
  transition:
    background 0.15s,
    border-color 0.15s;
  cursor: pointer;
}
.grade-card:hover {
  background: rgba(255, 255, 255, 0.1);
  border-left-color: var(--gold);
}
.grade-card .grade-label {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.15em;
  color: var(--gold);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 4px;
}
.grade-card .grade-title {
  font-family: var(--cond);
  font-size: 22px;
  font-weight: 700;
  color: #fff;
  letter-spacing: -0.005em;
}
.grade-card .grade-use {
  font-family: var(--mono);
  font-size: 10.5px;
  color: rgba(255, 255, 255, 0.6);
  margin-top: 4px;
  letter-spacing: 0.05em;
}

/* CERT STRIP */
.cert-strip {
  background: var(--bg-soft);
  padding: 24px 0;
  border-bottom: 1px solid var(--line);
}
.cert-strip .wrap {
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 22px;
}
.cs-label {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.15em;
  color: var(--navy);
  text-transform: uppercase;
  font-weight: 500;
}
.cs-badges {
  display: flex;
  gap: 28px;
  flex-wrap: wrap;
}
.cs-badges span {
  font-family: var(--cond);
  font-size: 16px;
  font-weight: 700;
  color: var(--navy);
  display: flex;
  align-items: center;
  gap: 7px;
  letter-spacing: 0.3px;
  text-transform: uppercase;
}
.cs-badges span::before {
  content: "✓";
  color: var(--orange);
  font-weight: 700;
}

/* SECTION */
section.s {
  padding: 80px 0;
}
.sec-num {
  font-family: var(--mono);
  font-size: 11.5px;
  letter-spacing: 0.2em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 500;
  margin-bottom: 14px;
}
h2 {
  font-family: var(--cond);
  font-size: 48px;
  font-weight: 800;
  line-height: 1.05;
  letter-spacing: -0.02em;
  color: var(--ink);
  margin-bottom: 16px;
}
h2 .accent {
  color: var(--orange);
}
.sec-lead {
  color: var(--ink-soft);
  font-size: 16.5px;
  max-width: 720px;
  line-height: 1.6;
  margin-bottom: 44px;
}
.sec-lead b {
  color: var(--navy);
  font-weight: 700;
}

/* PROPERTIES TABLE */
.props-wrap {
  background: var(--navy);
  color: #fff;
  padding: 8px;
  overflow-x: auto;
}
table.props {
  width: 100%;
  border-collapse: collapse;
}
table.props th,
table.props td {
  padding: 16px 18px;
  text-align: left;
  font-size: 14px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
}
table.props thead th {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.13em;
  color: var(--gold);
  text-transform: uppercase;
  font-weight: 500;
  padding-top: 22px;
  padding-bottom: 14px;
  border-bottom: 2px solid rgba(255, 255, 255, 0.15);
  background: var(--navy-2);
}
table.props tbody th {
  font-family: var(--cond);
  font-size: 18px;
  font-weight: 700;
  color: #fff;
}
table.props tbody td {
  color: rgba(255, 255, 255, 0.85);
  font-family: var(--cond);
  font-size: 18px;
  font-weight: 700;
}
table.props tbody td.unit {
  font-family: var(--mono);
  font-size: 11px;
  color: rgba(255, 255, 255, 0.55);
  font-weight: 400;
  letter-spacing: 0.05em;
}
table.props tbody tr:hover {
  background: rgba(255, 255, 255, 0.03);
}
.props-legend {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--ink-soft);
  margin-top: 16px;
  letter-spacing: 0.05em;
}

/* APPLICATIONS GRID */
.app-uses {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 22px;
}
.app-use {
  background: #fff;
  border: 1px solid var(--line);
  padding: 28px;
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 14px;
  transition:
    border-color 0.15s,
    transform 0.15s;
}
.app-use:hover {
  border-color: var(--orange);
  transform: translateY(-3px);
  box-shadow: 0 20px 40px -20px rgba(10, 38, 71, 0.15);
}
.app-use-bar {
  position: absolute;
  top: -1px;
  left: 28px;
  width: 40px;
  height: 3px;
  background: var(--orange);
}
.app-use-icon {
  font-size: 32px;
  line-height: 1;
  margin-top: 8px;
}
.app-use-title {
  font-family: var(--cond);
  font-size: 22px;
  font-weight: 700;
  color: var(--navy);
  line-height: 1.15;
  letter-spacing: -0.005em;
}
.app-use-desc {
  font-size: 13.5px;
  color: var(--ink-soft);
  line-height: 1.55;
  flex: 1;
}
.app-use-grade {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--orange);
  letter-spacing: 0.05em;
  padding-top: 12px;
  border-top: 1px solid var(--line);
  font-weight: 600;
}
.app-use-grade b {
  color: var(--navy);
}

/* COMPARISON CARDS */
.vs-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 28px;
}
.vs-card {
  background: #fff;
  border: 1px solid var(--line);
  padding: 32px;
}
.vs-card.win {
  border-color: var(--orange);
  border-width: 2px;
  background: linear-gradient(180deg, #fff 0%, var(--bg-soft) 100%);
}
.vs-card-head {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  margin-bottom: 16px;
  padding-bottom: 16px;
  border-bottom: 1px solid var(--line);
}
.vs-card-head h3 {
  font-family: var(--cond);
  font-size: 26px;
  font-weight: 800;
  color: var(--navy);
  letter-spacing: -0.01em;
}
.vs-tag {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.15em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 700;
  background: rgba(242, 100, 25, 0.08);
  padding: 4px 10px;
}
.vs-tag.alumina-tag {
  background: var(--orange);
  color: #fff;
}
.vs-points {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.vs-points li {
  display: flex;
  gap: 10px;
  align-items: baseline;
  font-size: 14px;
  color: var(--ink-soft);
  line-height: 1.5;
}
.vs-points li::before {
  content: "→";
  color: var(--orange);
  flex-shrink: 0;
  font-weight: 700;
}
.vs-points li b {
  color: var(--navy);
  font-weight: 700;
}

/* CTA BAND */
.casecta {
  background: var(--bg-soft);
  padding: 60px 0;
  border-top: 1px solid var(--line);
  border-bottom: 1px solid var(--line);
}
.casecta .wrap {
  display: grid;
  grid-template-columns: 1.3fr 1fr;
  gap: 50px;
  align-items: center;
}
.casecta-eyebrow {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.18em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 14px;
}
.casecta h2 {
  font-size: 38px;
  line-height: 1.08;
  margin-bottom: 18px;
}
.casecta p {
  font-size: 16px;
  color: var(--ink-soft);
  margin-bottom: 26px;
  line-height: 1.6;
}
.casecta-ctas {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}
.btn-primary {
  background: var(--orange);
  color: #fff;
  padding: 16px 28px;
  font-weight: 800;
  font-size: 14px;
  letter-spacing: 0.2px;
  border: none;
  cursor: pointer;
  transition: background 0.15s;
  display: inline-flex;
  align-items: center;
  gap: 8px;
}
.btn-primary:hover {
  background: var(--orange-deep);
}
.btn-ghost {
  background: transparent;
  color: var(--navy);
  padding: 16px 26px;
  font-weight: 700;
  font-size: 14px;
  letter-spacing: 0.2px;
  border: 2px solid var(--navy);
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 8px;
}
.btn-ghost:hover {
  border-color: var(--orange);
  color: var(--orange);
}
.casecta-side {
  background: #fff;
  border: 1px solid var(--line);
  padding: 28px;
  position: relative;
}
.casecta-side::before {
  content: "";
  position: absolute;
  top: -1px;
  left: 0;
  width: 70px;
  height: 4px;
  background: var(--orange);
}
.casecta-side-tag {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.15em;
  color: var(--orange);
  text-transform: uppercase;
  margin-bottom: 12px;
  font-weight: 600;
}
.casecta-side-title {
  font-family: var(--cond);
  font-size: 22px;
  font-weight: 700;
  color: var(--navy);
  margin-bottom: 16px;
  line-height: 1.2;
}
.casecta-side-list {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.casecta-side-list li {
  font-size: 13.5px;
  color: var(--ink-soft);
  display: flex;
  gap: 10px;
  align-items: baseline;
  padding-bottom: 10px;
  border-bottom: 1px dashed var(--line);
}
.casecta-side-list li:last-child {
  border-bottom: none;
  padding-bottom: 0;
}
.casecta-side-list li b {
  font-family: var(--cond);
  font-size: 15px;
  font-weight: 700;
  color: var(--navy);
  min-width: 70px;
}

/* FAQ */
.faq-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 18px 50px;
}
.faq-item {
  padding: 18px 0;
  border-bottom: 1px solid var(--line);
}
.faq-item:last-child,
.faq-item:nth-last-child(2) {
  border-bottom: none;
}
.faq-q {
  font-family: var(--cond);
  font-size: 19px;
  font-weight: 700;
  color: var(--navy);
  margin-bottom: 10px;
  letter-spacing: -0.005em;
  line-height: 1.25;
}
.faq-q::before {
  content: "Q";
  display: inline-block;
  width: 24px;
  height: 24px;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 12px;
  line-height: 24px;
  text-align: center;
  margin-right: 10px;
  vertical-align: middle;
  font-weight: 700;
}
.faq-a {
  font-size: 14px;
  color: var(--ink-soft);
  line-height: 1.6;
  padding-left: 34px;
}

footer {
  background: var(--navy);
  color: #c5d4e6;
  padding: 70px 0 30px;
}
.foot-grid {
  display: grid;
  grid-template-columns: 1.5fr 1fr 1fr 1fr 1.2fr;
  gap: 40px;
  margin-bottom: 50px;
}
.foot-col h5 {
  font-family: var(--cond);
  font-size: 16px;
  font-weight: 700;
  color: #fff;
  letter-spacing: 0.05em;
  margin-bottom: 16px;
  text-transform: uppercase;
}
.foot-col a {
  display: block;
  font-size: 13.5px;
  color: rgba(255, 255, 255, 0.65);
  margin-bottom: 9px;
  transition: color 0.15s;
}
.foot-col a:hover {
  color: var(--gold);
}
.fb-right p {
  font-size: 13.5px;
  color: rgba(255, 255, 255, 0.65);
  margin-bottom: 10px;
  line-height: 1.55;
}
.fb-right p b {
  color: #fff;
  font-weight: 600;
}
.foot-bottom {
  padding-top: 24px;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  font-family: var(--mono);
  font-size: 11.5px;
  color: rgba(255, 255, 255, 0.5);
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 14px;
}
.foot-bottom span {
  display: flex;
  gap: 22px;
}

@media (max-width: 1024px) {
  .page-hero .wrap {
    grid-template-columns: 1fr;
    gap: 40px;
  }
  .app-uses {
    grid-template-columns: 1fr 1fr;
  }
  .vs-grid {
    grid-template-columns: 1fr;
  }
  .casecta .wrap {
    grid-template-columns: 1fr;
  }
  .faq-grid {
    grid-template-columns: 1fr;
    gap: 0;
  }
  .foot-grid {
    grid-template-columns: 1fr 1fr;
  }
}
@media (max-width: 640px) {
  .wrap {
    padding: 0 20px;
  }
  .formula-display {
    font-size: 72px;
  }
  .page-hero h1 {
    font-size: 38px;
  }
  .app-uses {
    grid-template-columns: 1fr;
  }
  .topbar .left,
  .nav-links {
    display: none;
  }
}

/* ============================================================
     PRODUCT-PAGE TEMPLATE (distinct from material-page template)
     - hero carries an inline Quick-Quote form (core CTR)
     - sub-product catalog lineup replaces the spec datasheet table
     - benefit checklist replaces the vs-comparison grid
     ============================================================ */

/* PRODUCT HERO (quote-led) */
.prod-hero {
  background: linear-gradient(135deg, var(--navy) 0%, var(--navy-2) 100%);
  color: #fff;
  padding: 64px 0 78px;
  position: relative;
  overflow: hidden;
}
.prod-hero-stripe {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(
    90deg,
    var(--orange) 0%,
    var(--gold) 50%,
    var(--orange) 100%
  );
}
.prod-hero .wrap {
  display: grid;
  grid-template-columns: 1.15fr 0.85fr;
  gap: 56px;
  align-items: start;
  position: relative;
}
.ph-eyebrow {
  display: inline-block;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.15em;
  padding: 8px 14px;
  margin-bottom: 22px;
  text-transform: uppercase;
  font-weight: 500;
}
.prod-hero h1 {
  font-family: var(--cond);
  font-size: 56px;
  line-height: 1.02;
  font-weight: 800;
  letter-spacing: -0.02em;
  margin-bottom: 20px;
  color: #fff;
}
.prod-hero h1 .hl {
  color: var(--gold);
}
.prod-hero .ph-sub {
  font-size: 17px;
  color: rgba(255, 255, 255, 0.85);
  line-height: 1.55;
  max-width: 600px;
  margin-bottom: 30px;
}
.prod-hero .ph-sub b {
  color: var(--gold-soft);
  font-weight: 700;
}
.ph-statbar {
  display: flex;
  gap: 36px;
  flex-wrap: wrap;
  padding-top: 26px;
  border-top: 1px solid rgba(255, 255, 255, 0.15);
}
.ph-statbar .num {
  font-family: var(--cond);
  font-size: 38px;
  font-weight: 800;
  line-height: 1;
  color: var(--gold);
}
.ph-statbar .lbl {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.12em;
  color: rgba(255, 255, 255, 0.7);
  margin-top: 6px;
  text-transform: uppercase;
}

/* QUICK-QUOTE FORM CARD (the conversion engine) */
.qquote {
  background: #fff;
  padding: 30px 30px 32px;
  position: relative;
  box-shadow: 0 30px 60px -25px rgba(0, 0, 0, 0.5);
}
.qquote::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: var(--orange);
}
.qquote-tag {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.16em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 8px;
}
.qquote-title {
  font-family: var(--cond);
  font-size: 27px;
  font-weight: 800;
  color: var(--navy);
  line-height: 1.08;
  letter-spacing: -0.01em;
  margin-bottom: 6px;
}
.qquote-note {
  font-size: 12.5px;
  color: var(--ink-soft);
  line-height: 1.5;
  margin-bottom: 20px;
}
.qq-field {
  margin-bottom: 13px;
}
.qq-field label {
  display: block;
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--ink-faint);
  font-weight: 500;
  margin-bottom: 5px;
}
.qq-field input,
.qq-field select,
.qq-field textarea {
  width: 100%;
  font-family: var(--sans);
  font-size: 14px;
  color: var(--ink);
  border: 1px solid var(--line-strong);
  padding: 11px 13px;
  background: var(--bg-soft);
  transition:
    border-color 0.15s,
    background 0.15s;
}
.qq-field input:focus,
.qq-field select:focus,
.qq-field textarea:focus {
  outline: none;
  border-color: var(--orange);
  background: #fff;
}
.qq-field textarea {
  resize: vertical;
  min-height: 64px;
}
.qq-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 13px;
}
.qq-submit {
  width: 100%;
  background: var(--orange);
  color: #fff;
  border: none;
  padding: 15px;
  font-family: var(--sans);
  font-weight: 800;
  font-size: 14.5px;
  letter-spacing: 0.3px;
  cursor: pointer;
  transition: background 0.15s;
  margin-top: 6px;
}
.qq-submit:hover {
  background: var(--orange-deep);
}
.qq-trust {
  font-family: var(--mono);
  font-size: 10.5px;
  color: var(--ink-faint);
  text-align: center;
  margin-top: 12px;
  letter-spacing: 0.03em;
}

/* SUB-PRODUCT LINEUP (catalog — replaces datasheet table) */
.lineup {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24px;
}
.lineup-card {
  background: #fff;
  border: 1px solid var(--line);
  padding: 30px;
  position: relative;
  display: flex;
  flex-direction: column;
  transition:
    border-color 0.15s,
    transform 0.15s,
    box-shadow 0.15s;
}
.lineup-card:hover {
  border-color: var(--orange);
  transform: translateY(-3px);
  box-shadow: 0 24px 46px -24px rgba(10, 38, 71, 0.18);
}
.lineup-card.flagship {
  border-color: var(--orange);
  border-width: 2px;
  background: linear-gradient(180deg, #fff 0%, var(--bg-soft) 100%);
}
.lineup-badge {
  position: absolute;
  top: 0;
  right: 0;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 9.5px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  font-weight: 600;
  padding: 6px 12px;
}
.lineup-head {
  display: flex;
  align-items: flex-start;
  gap: 14px;
  margin-bottom: 14px;
}
.lineup-icon {
  font-size: 30px;
  line-height: 1;
  flex-shrink: 0;
  margin-top: 2px;
}
.lineup-name {
  font-family: var(--cond);
  font-size: 25px;
  font-weight: 800;
  color: var(--navy);
  line-height: 1.08;
  letter-spacing: -0.01em;
}
.lineup-cn {
  font-family: var(--sans);
  font-size: 12.5px;
  color: var(--ink-faint);
  font-weight: 600;
  margin-top: 2px;
}
.lineup-desc {
  font-size: 13.5px;
  color: var(--ink-soft);
  line-height: 1.55;
  margin-bottom: 16px;
  flex: 1;
}
.lineup-specs {
  display: flex;
  flex-wrap: wrap;
  gap: 7px;
  margin-bottom: 18px;
}
.spec-chip {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.03em;
  color: var(--navy);
  background: var(--bg-soft-2);
  padding: 5px 9px;
  font-weight: 500;
}
.spec-chip b {
  color: var(--orange);
  font-weight: 600;
}
.lineup-foot {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 15px;
  border-top: 1px solid var(--line);
}
.lineup-mat {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--ink-soft);
  letter-spacing: 0.04em;
}
.lineup-mat b {
  color: var(--navy);
  font-weight: 600;
}
.lineup-mat a {
  color: var(--orange);
  font-weight: 600;
}
.lineup-link {
  font-family: var(--sans);
  font-size: 13px;
  font-weight: 800;
  color: var(--orange);
  letter-spacing: 0.2px;
  white-space: nowrap;
}
.lineup-link:hover {
  color: var(--orange-deep);
}

/* BENEFIT CHECKLIST (replaces vs-comparison grid) */
.benefits {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 22px 56px;
}
.benefit {
  display: flex;
  gap: 16px;
  align-items: flex-start;
  padding-bottom: 20px;
  border-bottom: 1px solid var(--line);
}
.benefit-ico {
  flex-shrink: 0;
  width: 30px;
  height: 30px;
  background: var(--orange);
  color: #fff;
  font-weight: 800;
  font-size: 15px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 2px;
}
.benefit-ico::before {
  content: "✓";
}
.benefit-body h4 {
  font-family: var(--cond);
  font-size: 20px;
  font-weight: 700;
  color: var(--navy);
  letter-spacing: -0.005em;
  margin-bottom: 5px;
  line-height: 1.15;
}
.benefit-body p {
  font-size: 13.5px;
  color: var(--ink-soft);
  line-height: 1.55;
}

@media (max-width: 1024px) {
  .prod-hero .wrap {
    grid-template-columns: 1fr;
    gap: 40px;
  }
  .lineup {
    grid-template-columns: 1fr;
  }
  .benefits {
    grid-template-columns: 1fr;
    gap: 0;
  }
}
@media (max-width: 640px) {
  .prod-hero h1 {
    font-size: 38px;
  }
  .qq-row {
    grid-template-columns: 1fr;
  }
  .lineup-foot {
    flex-direction: column;
    align-items: flex-start;
    gap: 10px;
  }
}

/* ============================================================
     PRODUCT DETAIL PAGE (CN-300 Si3N4) — extends product template
     ============================================================ */

/* HERO SPOTLIGHT CARD (right column on detail hero) */
.spotlight-tag {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.16em;
  color: var(--gold);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 18px;
}
.spotlight-row {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  padding: 13px 0;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}
.spotlight-row:last-child {
  border-bottom: none;
}
.spotlight-row .k {
  font-family: var(--mono);
  font-size: 11px;
  color: rgba(255, 255, 255, 0.6);
  letter-spacing: 0.05em;
}
.spotlight-row .v {
  font-family: var(--cond);
  font-size: 20px;
  font-weight: 700;
  color: #fff;
}
.spotlight-row .v b {
  color: var(--gold);
}
.hero-ctas {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  margin-top: 30px;
}

/* DOWNLOAD CARDS */
.dl-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20px;
}
.dl-card {
  background: #fff;
  border: 1px solid var(--line);
  padding: 26px;
  display: flex;
  flex-direction: column;
  gap: 12px;
  transition:
    border-color 0.15s,
    transform 0.15s;
  position: relative;
}
.dl-card:hover {
  border-color: var(--orange);
  transform: translateY(-3px);
  box-shadow: 0 20px 40px -22px rgba(10, 38, 71, 0.18);
}
.dl-icon {
  font-size: 30px;
  line-height: 1;
}
.dl-name {
  font-family: var(--cond);
  font-size: 21px;
  font-weight: 700;
  color: var(--navy);
  line-height: 1.12;
}
.dl-meta {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--ink-faint);
  letter-spacing: 0.05em;
}
.dl-btn {
  margin-top: auto;
  font-family: var(--sans);
  font-size: 13px;
  font-weight: 800;
  color: var(--orange);
  letter-spacing: 0.2px;
  padding-top: 10px;
  border-top: 1px solid var(--line);
}
.dl-btn:hover {
  color: var(--orange-deep);
}

/* CERTIFICATION DETAIL CARDS */
.cert-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20px;
}
.cert-card {
  background: #fff;
  border: 1px solid var(--line);
  padding: 24px;
  position: relative;
}
.cert-card::before {
  content: "";
  position: absolute;
  top: -1px;
  left: 0;
  width: 50px;
  height: 4px;
  background: var(--orange);
}
.cert-card .cc-mark {
  font-family: var(--cond);
  font-size: 26px;
  font-weight: 800;
  color: var(--navy);
  letter-spacing: -0.01em;
  margin-bottom: 6px;
}
.cert-card .cc-scope {
  font-size: 13px;
  color: var(--ink-soft);
  line-height: 1.5;
}
.cert-card .cc-tag {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.12em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 600;
  margin-top: 12px;
}

/* RELATED INTERNAL LINKS (Product -> Material + Application) */
.rel-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 22px;
}
.rel-card {
  background: #fff;
  border: 1px solid var(--line);
  padding: 28px;
  display: flex;
  flex-direction: column;
  gap: 10px;
  transition:
    border-color 0.15s,
    transform 0.15s;
}
.rel-card:hover {
  border-color: var(--orange);
  transform: translateY(-3px);
  box-shadow: 0 20px 40px -22px rgba(10, 38, 71, 0.15);
}
.rel-eyebrow {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.14em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 600;
}
.rel-title {
  font-family: var(--cond);
  font-size: 22px;
  font-weight: 700;
  color: var(--navy);
  line-height: 1.15;
}
.rel-desc {
  font-size: 13px;
  color: var(--ink-soft);
  line-height: 1.5;
}
.rel-link {
  font-family: var(--sans);
  font-size: 13px;
  font-weight: 800;
  color: var(--orange);
  margin-top: 4px;
}

/* QUOTE SECTION (full-width, 2-col) */
.quote-sec {
  background: var(--navy);
  color: #fff;
}
.quote-sec .wrap {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 60px;
  align-items: start;
}
.quote-sec .sec-num {
  color: var(--gold);
}
.quote-sec h2 {
  color: #fff;
}
.quote-sec h2 .accent {
  color: var(--gold);
}
.quote-why {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 16px;
  margin-top: 24px;
}
.quote-why li {
  display: flex;
  gap: 13px;
  align-items: flex-start;
  font-size: 15px;
  color: rgba(255, 255, 255, 0.88);
  line-height: 1.5;
}
.quote-why li::before {
  content: "✓";
  color: var(--gold);
  font-weight: 800;
  flex-shrink: 0;
}
.quote-why li b {
  color: #fff;
  font-weight: 700;
}
.quote-form-card {
  background: #fff;
  padding: 32px;
}
.quote-form-card .qquote-title {
  color: var(--navy);
}

@media (max-width: 1024px) {
  .dl-grid {
    grid-template-columns: 1fr;
  }
  .cert-grid {
    grid-template-columns: 1fr 1fr;
  }
  .rel-grid {
    grid-template-columns: 1fr;
  }
  .quote-sec .wrap {
    grid-template-columns: 1fr;
    gap: 36px;
  }
}
@media (max-width: 640px) {
  .cert-grid {
    grid-template-columns: 1fr;
  }
  .spotlight {
    margin-top: 0;
  }
}

/* ============================================================
     APPLICATION VERTICAL DETAIL — v2
     Hero: single-column (no height imbalance) + scenario pill nav
     Scenarios: image-left + content-right full-bleed grid
     ============================================================ */

/* ---- APPLICATION HERO (single-column, no forced 2-col) ---- */
.app-hero {
  background: linear-gradient(135deg, var(--navy) 0%, var(--navy-2) 100%);
  color: #fff;
  padding: 68px 0 0;
  position: relative;
  overflow: hidden;
}
.app-hero-stripe {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3px;
  background: linear-gradient(
    90deg,
    var(--orange) 0%,
    var(--gold) 50%,
    var(--orange) 100%
  );
}
.app-hero-eyebrow {
  display: inline-block;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.15em;
  padding: 8px 14px;
  margin-bottom: 24px;
  text-transform: uppercase;
  font-weight: 500;
}
.app-hero h1 {
  font-family: var(--cond);
  font-size: 60px;
  line-height: 1.02;
  font-weight: 800;
  letter-spacing: -0.025em;
  margin-bottom: 22px;
  color: #fff;
  max-width: 860px;
}
.app-hero h1 .hl {
  color: var(--gold);
}
.app-hero .app-hero-sub {
  font-size: 18px;
  color: rgba(255, 255, 255, 0.82);
  line-height: 1.55;
  max-width: 760px;
  margin-bottom: 32px;
}
.app-hero .app-hero-sub b {
  color: var(--gold-soft);
  font-weight: 700;
}
.app-hero-stats {
  display: flex;
  gap: 44px;
  flex-wrap: wrap;
  margin-bottom: 36px;
}
.app-hero-stats .num {
  font-family: var(--cond);
  font-size: 42px;
  font-weight: 800;
  line-height: 1;
  color: var(--gold);
}
.app-hero-stats .lbl {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.12em;
  color: rgba(255, 255, 255, 0.65);
  margin-top: 6px;
  text-transform: uppercase;
}
.app-hero-ctas {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  margin-bottom: 48px;
}

/* Scenario pill nav (bottom strip inside hero) */
.app-scen-nav {
  border-top: 1px solid rgba(255, 255, 255, 0.14);
  padding: 24px 0 0;
}
.app-scen-nav-label {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.16em;
  color: rgba(255, 255, 255, 0.5);
  text-transform: uppercase;
  margin-bottom: 14px;
}
.app-scen-pills {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 6px;
  padding-bottom: 28px;
}
.app-scen-pill {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 11px 14px;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.1);
  transition:
    background 0.15s,
    border-color 0.15s;
}
.app-scen-pill:hover {
  background: rgba(255, 255, 255, 0.1);
  border-color: var(--orange);
}
.app-scen-pill.flag {
  border-color: var(--orange);
  background: rgba(242, 100, 25, 0.12);
}
.app-scen-pill .pill-num {
  font-family: var(--mono);
  font-size: 11px;
  font-weight: 500;
  color: var(--gold);
  letter-spacing: 0.05em;
  flex-shrink: 0;
}
.app-scen-pill .pill-name {
  font-family: var(--sans);
  font-size: 13px;
  font-weight: 600;
  color: rgba(255, 255, 255, 0.88);
  line-height: 1.25;
}

/* ---- SCENARIO SECTIONS WITH IMAGE ---- */
.scen-img-sec {
  padding: 0;
}
.scen-img-sec.alt {
  background: var(--bg-soft);
}
.scen-full-wrap {
  display: grid;
  grid-template-columns: 420px 1fr;
  min-height: 460px;
  max-width: 1260px;
  margin: 0 auto;
  padding: 0 0 0 40px;
  gap: 0;
  align-items: stretch;
}
.scen-full-wrap.rev {
  grid-template-columns: 1fr 420px;
  padding: 0 40px 0 0;
}
.scen-full-wrap.rev .scen-img-col {
  order: 2;
}
.scen-full-wrap.rev .scen-txt-col {
  order: 1;
  padding: 60px 56px 60px 0;
}

/* Image column — placeholder styled to look like real imagery */
.scen-img-col {
  position: relative;
  overflow: hidden;
  flex-shrink: 0;
  min-height: 460px;
}
.scen-img-placeholder {
  width: 100%;
  height: 100%;
  min-height: 460px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 14px;
  position: relative;
}
.scen-img-placeholder .img-icon {
  font-size: 56px;
  line-height: 1;
  opacity: 0.9;
}
.scen-img-placeholder .img-label {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  opacity: 0.65;
  font-weight: 500;
  text-align: center;
  max-width: 180px;
  line-height: 1.5;
}
.scen-img-placeholder .img-replace {
  position: absolute;
  bottom: 14px;
  left: 0;
  right: 0;
  text-align: center;
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.08em;
  opacity: 0.45;
  text-transform: uppercase;
}
/* Real photo: replace placeholder div with <img> using same .scen-img-col wrapper */
.scen-img-col img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

/* Scenario colors */
.si-biomass {
  background: linear-gradient(145deg, #1a0f05 0%, #3d1a08 40%, #6b2d0e 100%);
  color: #f5d9b8;
}
.si-bbq {
  background: linear-gradient(145deg, #1a0a05 0%, #4a1a08 40%, #7a2e10 100%);
  color: #fad4a8;
}
.si-gas {
  background: linear-gradient(145deg, #0a1a2a 0%, #152840 40%, #1e3d5c 100%);
  color: #b8d4f0;
}
.si-boiler {
  background: linear-gradient(145deg, #0f0f14 0%, #1e1e2e 40%, #2e2e44 100%);
  color: #c4c4e0;
}
.si-water {
  background: linear-gradient(145deg, #051520 0%, #0a2535 40%, #0f3650 100%);
  color: #a8d4f5;
}
.si-hvac {
  background: linear-gradient(145deg, #051a1a 0%, #0a2e2e 40%, #103e3e 100%);
  color: #a8e0e0;
}
.si-ovens {
  background: linear-gradient(145deg, #1a1005 0%, #3d2808 40%, #5c3c0f 100%);
  color: #f0d8a8;
}
.si-hydro {
  background: linear-gradient(145deg, #051a10 0%, #0a3020 40%, #0f4530 100%);
  color: #a8f0c4;
}

/* Text column */
.scen-txt-col {
  padding: 60px 0 60px 56px;
  display: flex;
  flex-direction: column;
  justify-content: center;
}
.scen-eyebrow {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.18em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 500;
  margin-bottom: 10px;
}
.scen-flag {
  display: inline-block;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 9.5px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  font-weight: 600;
  padding: 5px 10px;
  margin-bottom: 12px;
}
.scen-h2 {
  font-family: var(--cond);
  font-size: 36px;
  font-weight: 800;
  line-height: 1.05;
  letter-spacing: -0.02em;
  color: var(--ink);
  margin-bottom: 14px;
}
.scen-h2 .accent {
  color: var(--orange);
}
.scen-intro {
  font-size: 15px;
  color: var(--ink-soft);
  line-height: 1.6;
  margin-bottom: 24px;
}
.scen-intro b {
  color: var(--navy);
  font-weight: 700;
}

/* Tech requirements — clean key-value, NOT dark table */
.tech-req-label {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.13em;
  color: var(--navy);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 10px;
  border-bottom: 2px solid var(--ink);
  padding-bottom: 8px;
}
.tech-row {
  display: grid;
  grid-template-columns: 1fr 1.3fr;
  gap: 6px;
  align-items: baseline;
  padding: 9px 0;
  border-bottom: 1px solid var(--line);
}
.tech-row:last-child {
  border-bottom: none;
}
.tech-k {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--ink-faint);
  letter-spacing: 0.04em;
}
.tech-v {
  font-family: var(--cond);
  font-size: 17px;
  font-weight: 700;
  color: var(--navy);
}
.tech-v .hi {
  color: var(--orange);
}
.tech-v small {
  font-family: var(--mono);
  font-size: 10.5px;
  color: var(--ink-soft);
  font-weight: 400;
  margin-left: 3px;
}

/* Recommended products inline */
.rec-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 12px;
  padding-top: 20px;
  margin-top: 20px;
  border-top: 1px solid var(--line);
}
.rec-links {
  display: flex;
  flex-wrap: wrap;
  gap: 8px 18px;
  font-family: var(--mono);
  font-size: 11.5px;
}
.rec-links .rl {
  color: var(--navy);
  font-weight: 600;
}
.rec-links a {
  color: var(--orange);
  font-weight: 600;
}
.rec-links a:hover {
  color: var(--orange-deep);
}
.rec-cta-sm {
  font-family: var(--sans);
  font-size: 13.5px;
  font-weight: 800;
  color: var(--orange);
  white-space: nowrap;
  border: 2px solid var(--orange);
  padding: 10px 18px;
  transition:
    background 0.15s,
    color 0.15s;
}
.rec-cta-sm:hover {
  background: var(--orange);
  color: #fff;
}

/* Flagship expanded case study (Biomass) */
.flagship-ext {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8px;
  margin-top: 18px;
}
.fe-stat {
  background: var(--navy);
  padding: 14px 16px;
}
.fe-stat .fn {
  font-family: var(--cond);
  font-size: 26px;
  font-weight: 800;
  color: var(--gold);
  line-height: 1;
}
.fe-stat .fl {
  font-family: var(--mono);
  font-size: 10px;
  color: rgba(255, 255, 255, 0.65);
  letter-spacing: 0.04em;
  margin-top: 4px;
}
.fe-case-label {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.14em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 8px;
}

@media (max-width: 1024px) {
  .app-scen-pills {
    grid-template-columns: repeat(2, 1fr);
  }
  .scen-full-wrap {
    grid-template-columns: 1fr;
    padding: 0;
    min-height: auto;
  }
  .scen-full-wrap.rev {
    padding: 0;
  }
  .scen-full-wrap.rev .scen-img-col {
    order: 1;
  }
  .scen-full-wrap.rev .scen-txt-col {
    order: 2;
    padding: 40px 40px;
  }
  .scen-img-col {
    min-height: 260px;
  }
  .scen-img-placeholder {
    min-height: 260px;
  }
  .scen-txt-col {
    padding: 40px 40px;
  }
  .flagship-ext {
    grid-template-columns: repeat(2, 1fr);
  }
}
@media (max-width: 640px) {
  .app-hero h1 {
    font-size: 40px;
  }
  .app-scen-pills {
    grid-template-columns: 1fr;
  }
  .tech-row {
    grid-template-columns: 1fr;
  }
  .rec-row {
    flex-direction: column;
    align-items: flex-start;
  }
  .scen-txt-col {
    padding: 32px 20px;
  }
  .flagship-ext {
    grid-template-columns: 1fr 1fr;
  }
}

/* ============================================================
     BIOMASS & PELLET STOVES — application deep-dive page
     ============================================================ */

/* Hero — application scenario hero (not product, not material) */
.bio-hero {
  background: linear-gradient(
    135deg,
    #1a0f05 0%,
    #2d1a0a 50%,
    var(--navy) 100%
  );
  color: #fff;
  padding: 68px 0 0;
  position: relative;
  overflow: hidden;
}
.bio-hero::after {
  content: "";
  position: absolute;
  top: 0;
  right: 0;
  width: 40%;
  height: 100%;
  background: linear-gradient(
    135deg,
    transparent 0%,
    rgba(242, 100, 25, 0.06) 100%
  );
  pointer-events: none;
}
.bio-hero-stripe {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3px;
  background: linear-gradient(
    90deg,
    var(--orange) 0%,
    var(--gold) 60%,
    var(--orange) 100%
  );
}
.bio-hero .wrap {
  display: grid;
  grid-template-columns: 1.1fr 0.9fr;
  gap: 56px;
  align-items: end;
  position: relative;
}
.bio-hero-eyebrow {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.15em;
  color: var(--gold);
  text-transform: uppercase;
  font-weight: 500;
  margin-bottom: 18px;
}
.bio-hero h1 {
  font-family: var(--cond);
  font-size: 56px;
  line-height: 1.02;
  font-weight: 800;
  letter-spacing: -0.025em;
  margin-bottom: 20px;
  color: #fff;
}
.bio-hero h1 .hl {
  color: var(--gold);
}
.bio-hero-sub {
  font-size: 17px;
  color: rgba(255, 255, 255, 0.82);
  line-height: 1.55;
  max-width: 620px;
  margin-bottom: 30px;
}
.bio-hero-sub b {
  color: var(--gold-soft);
}
.bio-statbar {
  display: flex;
  gap: 36px;
  flex-wrap: wrap;
  margin-bottom: 36px;
}
.bio-statbar .num {
  font-family: var(--cond);
  font-size: 40px;
  font-weight: 800;
  line-height: 1;
  color: var(--gold);
}
.bio-statbar .lbl {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.12em;
  color: rgba(255, 255, 255, 0.65);
  margin-top: 5px;
  text-transform: uppercase;
}
.bio-hero-ctas {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  margin-bottom: 48px;
}

/* Hero right: case proof card */
.bio-proof {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.14);
  border-left: 3px solid var(--orange);
  padding: 26px 28px;
  margin-bottom: 28px;
}
.bio-proof-eyebrow {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.14em;
  color: var(--gold);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 10px;
}
.bio-proof-title {
  font-family: var(--cond);
  font-size: 20px;
  font-weight: 800;
  color: #fff;
  line-height: 1.1;
  margin-bottom: 16px;
}
.bio-proof-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8px;
}
.bio-proof-stat {
  background: rgba(255, 255, 255, 0.05);
  padding: 12px 14px;
}
.bio-proof-stat .bn {
  font-family: var(--cond);
  font-size: 26px;
  font-weight: 800;
  color: var(--gold);
  line-height: 1;
}
.bio-proof-stat .bl {
  font-family: var(--mono);
  font-size: 9.5px;
  color: rgba(255, 255, 255, 0.6);
  letter-spacing: 0.04em;
  margin-top: 4px;
}
.bio-hero-nav-bottom {
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  padding: 20px 0 28px;
  display: flex;
  gap: 28px;
  flex-wrap: wrap;
  align-items: center;
}
.bio-hero-nav-bottom .nav-back {
  font-family: var(--mono);
  font-size: 11px;
  color: rgba(255, 255, 255, 0.55);
  letter-spacing: 0.05em;
}
.bio-hero-nav-bottom .nav-back a {
  color: var(--gold);
  font-weight: 600;
}
.bio-hero-nav-bottom .nav-back a:hover {
  color: #fff;
}

/* Market context band */
.mkt-band {
  background: var(--bg-soft);
  border-top: 1px solid var(--line);
  border-bottom: 1px solid var(--line);
  padding: 44px 0;
}
.mkt-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 32px;
  align-items: start;
}
.mkt-item {
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.mkt-num {
  font-family: var(--cond);
  font-size: 38px;
  font-weight: 800;
  color: var(--navy);
  line-height: 1;
  letter-spacing: -0.01em;
}
.mkt-num span {
  color: var(--orange);
}
.mkt-label {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.08em;
  color: var(--ink-soft);
  line-height: 1.5;
}
.mkt-divider {
  width: 1px;
  background: var(--line);
  align-self: stretch;
  display: none;
}

/* Igniter comparison panel: Si3N4 vs Al2O3 for this specific application */
.comp-band {
  padding: 72px 0;
}
.comp-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24px;
  margin-top: 36px;
}
.comp-card {
  border: 1px solid var(--line);
  padding: 32px;
  position: relative;
}
.comp-card.winner {
  border-color: var(--orange);
  border-width: 2px;
}
.comp-card.winner::before {
  content: "";
  position: absolute;
  top: -1px;
  left: 0;
  width: 80px;
  height: 4px;
  background: var(--orange);
}
.comp-badge {
  display: inline-block;
  font-family: var(--mono);
  font-size: 9.5px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  font-weight: 600;
  padding: 5px 10px;
  margin-bottom: 14px;
}
.comp-badge.win {
  background: var(--orange);
  color: #fff;
}
.comp-badge.alt {
  background: var(--bg-soft-2);
  color: var(--ink-soft);
}
.comp-name {
  font-family: var(--cond);
  font-size: 28px;
  font-weight: 800;
  color: var(--navy);
  letter-spacing: -0.01em;
  margin-bottom: 6px;
}
.comp-tagline {
  font-family: var(--mono);
  font-size: 11.5px;
  color: var(--ink-faint);
  letter-spacing: 0.03em;
  margin-bottom: 22px;
}
.comp-list {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.comp-list li {
  display: flex;
  gap: 10px;
  align-items: flex-start;
  font-size: 14px;
  color: var(--ink-soft);
  line-height: 1.5;
}
.comp-list li .ci {
  flex-shrink: 0;
  font-size: 13px;
  margin-top: 2px;
}
.comp-list li b {
  color: var(--navy);
  font-weight: 700;
}
.comp-cta-row {
  margin-top: 22px;
  padding-top: 18px;
  border-top: 1px solid var(--line);
}

/* Application scenario depth section */
.depth-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 56px;
  align-items: start;
}
.depth-img {
  background: linear-gradient(145deg, #1a0f05 0%, #3d1a08 40%, #6b2d0e 100%);
  min-height: 400px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 14px;
  position: relative;
}
.depth-img .di-icon {
  font-size: 64px;
  line-height: 1;
  opacity: 0.9;
}
.depth-img .di-label {
  font-family: var(--mono);
  font-size: 11px;
  color: rgba(255, 220, 180, 0.7);
  letter-spacing: 0.08em;
  text-transform: uppercase;
  text-align: center;
  max-width: 200px;
  line-height: 1.5;
}
.depth-img .di-replace {
  position: absolute;
  bottom: 14px;
  font-family: var(--mono);
  font-size: 10px;
  color: rgba(255, 220, 180, 0.4);
  letter-spacing: 0.07em;
  text-transform: uppercase;
}

/* Failure analysis band */
.failure-band {
  background: var(--navy);
  color: #fff;
  padding: 64px 0;
}
.failure-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 28px;
  margin-top: 36px;
}
.fail-card {
  background: rgba(255, 255, 255, 0.06);
  border-left: 3px solid var(--orange);
  padding: 22px 24px;
}
.fail-card.worst {
  border-color: var(--gold);
}
.fail-num {
  font-family: var(--cond);
  font-size: 34px;
  font-weight: 800;
  color: var(--gold);
  line-height: 1;
  margin-bottom: 6px;
}
.fail-name {
  font-family: var(--cond);
  font-size: 18px;
  font-weight: 700;
  color: #fff;
  margin-bottom: 8px;
}
.fail-desc {
  font-size: 13px;
  color: rgba(255, 255, 255, 0.7);
  line-height: 1.55;
}
.fail-solution {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--gold);
  letter-spacing: 0.04em;
  margin-top: 10px;
  padding-top: 10px;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
}

@media (max-width: 1024px) {
  .bio-hero .wrap {
    grid-template-columns: 1fr;
    gap: 36px;
  }
  .mkt-grid {
    grid-template-columns: 1fr 1fr;
    gap: 24px;
  }
  .comp-grid {
    grid-template-columns: 1fr;
  }
  .depth-grid {
    grid-template-columns: 1fr;
  }
  .failure-grid {
    grid-template-columns: 1fr;
  }
}
@media (max-width: 640px) {
  .bio-hero h1 {
    font-size: 38px;
  }
  .bio-proof-grid {
    grid-template-columns: 1fr 1fr;
  }
  .mkt-grid {
    grid-template-columns: 1fr 1fr;
  }
}
</style>
