<script setup lang="ts">
useHead({
  title:
    "Advanced Combustion, HVAC & Appliance Ignition — Si₃N₄ & SiC Ceramic Igniters | Ceramitell",
  meta: [{ name: "description", content: "Ceramitell" }],
});
</script>

<template>
  <div>
    <div class="breadcrumb">
      <div class="wrap">
        <a href="/">Home</a>
        <span class="sep">/</span>
        <a href="/applications">Applications</a>
        <span class="sep">/</span>
        <span>Laboratory Equipment</span>
      </div>
    </div>

    <!-- HERO: single column, no height imbalance -->
    <section class="app-hero">
      <div class="app-hero-stripe"></div>
      <div class="wrap">
        <span class="app-hero-eyebrow">Application Vertical 07</span>
        <h1 class="app-hero-h1">
          Ceramic components for<br /><span class="hl"
            >analytical instruments.</span
          >
        </h1>
        <p class="app-hero-sub">
          Al₂O₃ crucibles, Si₃N₄ thermocouple sheaths, AlN heaters, high-purity
          tubes, and precision ceramic components for
          <b
            >analytical chemistry, spectroscopy, electron microscopy, and
            high-temperature research</b
          >.
        </p>
        <div class="app-hero-stats">
          <div class="stat">
            <div class="num">Al₂O₃</div>
            <div class="lbl">Crucibles & Tubes</div>
          </div>
          <div class="stat">
            <div class="num">AlN</div>
            <div class="lbl">Heaters</div>
          </div>
          <div class="stat">
            <div class="num">≥99.99%</div>
            <div class="lbl">Purity</div>
          </div>
          <div class="stat">
            <div class="num">Si₃N₄</div>
            <div class="lbl">Precision Parts</div>
          </div>
        </div>
        <div class="app-hero-ctas">
          <a href="#quote" class="btn-primary">Request a Quote →</a>
          <a
            href="/contact"
            class="btn-ghost"
            style="color: #fff; border-color: rgba(255, 255, 255, 0.4)"
            >Talk to an Engineer</a
          >
        </div>
        <div class="app-scen-nav">
          <div class="app-scen-nav-label">Jump to scenario</div>
          <div class="app-scen-pills">
            <a href="#muffle" class="app-scen-pill flag"
              ><span class="pill-num">01</span
              ><span class="pill-name">Muffle & Tube Furnaces</span></a
            ><a href="#icp-oes" class="app-scen-pill"
              ><span class="pill-num">02</span
              ><span class="pill-name">ICP-OES Atomisation</span></a
            ><a href="#crucibles" class="app-scen-pill"
              ><span class="pill-num">03</span
              ><span class="pill-name">Analytical Crucibles</span></a
            ><a href="#tga-dsc" class="app-scen-pill"
              ><span class="pill-num">04</span
              ><span class="pill-name">TGA & DSC Instruments</span></a
            ><a href="#sem-tem" class="app-scen-pill"
              ><span class="pill-num">05</span
              ><span class="pill-name">SEM & TEM Sample Prep</span></a
            ><a href="#custom-lab" class="app-scen-pill"
              ><span class="pill-num">06</span
              ><span class="pill-name">Custom Lab Ceramics</span></a
            >
          </div>
        </div>
      </div>
    </section>
    <section class="cert-strip">
      <div class="wrap">
        <div class="cs-label">Certifications &amp; Standards</div>
        <div class="cs-badges">
          <span>ISO 9001</span><span>IATF 16949</span><span>CE</span
          ><span>RoHS · REACH</span><span>UL Recognized</span>
        </div>
      </div>
    </section>
    <section class="scen-img-sec" id="muffle">
      <div class="scen-full-wrap">
        <div class="scen-img-col">
          <div
            class="scen-img-placeholder"
            style="
              background: linear-gradient(
                145deg,
                #0d1f33 0%,
                #162d47 40%,
                #1e3d5c 100%
              );
              color: #b8d4f0;
            "
          >
            <div class="img-icon">🧪</div>
            <div class="img-label">Al₂O₃ tube furnace — laboratory setup</div>
            <div class="img-replace">Replace with product photo</div>
          </div>
        </div>
        <div class="scen-txt-col">
          <div class="scen-eyebrow">01 · Muffle & Tube Furnace Components</div>
          <span class="scen-flag">★ P0 Application</span>
          <h2 class="scen-h2">
            Al₂O₃ tubes and boats for
            <span class="accent">laboratory furnaces.</span>.
          </h2>
          <p class="scen-intro">
            High-purity Al₂O₃ tubes, boats, and support rods for muffle furnaces
            and tube furnaces in materials research, ceramics sintering, and
            heat treatment. <b>Purity ≥99.7% Al₂O₃</b> prevents metallic
            contamination of samples at 1600°C.
          </p>
          <div style="margin-bottom: 20px">
            <div class="tech-req-label">Technical Requirements</div>
            <div class="tech-row">
              <span class="tech-k">Al₂O₃ purity</span
              ><span class="tech-v">≥99.7%</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Max temp</span
              ><span class="tech-v">1600°C</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Form</span
              ><span class="tech-v">Tube / boat / rod</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Atmosphere</span
              ><span class="tech-v">Air / N₂ / H₂</span>
            </div>
          </div>
          <div class="rec-row">
            <div class="rec-links">
              <span class="rl">Products →</span
              ><a href="/products/ceramic-structural"
                >Ceramic Structural Parts</a
              ><a href="/products/custom-ceramic">Custom Ceramic DFM</a>
            </div>
            <a href="#quote" class="rec-cta-sm">Request Samples →</a>
          </div>
        </div>
      </div>
    </section>
    <section class="scen-img-sec alt" id="icp-oes">
      <div class="scen-full-wrap rev">
        <div class="scen-img-col">
          <div
            class="scen-img-placeholder"
            style="
              background: linear-gradient(
                145deg,
                #0d1f33 0%,
                #162d47 40%,
                #1e3d5c 100%
              );
              color: #b8d4f0;
            "
          >
            <div class="img-icon">🔬</div>
            <div class="img-label">Al₂O₃ ICP torch — spectral analysis</div>
            <div class="img-replace">Replace with product photo</div>
          </div>
        </div>
        <div class="scen-txt-col">
          <div class="scen-eyebrow">02 · ICP-OES Atomisation Tubes</div>

          <h2 class="scen-h2">
            High-purity Al₂O₃ for
            <span class="accent">plasma atomisation.</span>.
          </h2>
          <p class="scen-intro">
            High-purity Al₂O₃ injector tubes and torch components for ICP-OES
            and ICP-MS spectral analysis instruments. Resistant to HF acid
            cleaning cycles — the most demanding chemical environment in
            analytical laboratory equipment.
          </p>
          <div style="margin-bottom: 20px">
            <div class="tech-req-label">Technical Requirements</div>
            <div class="tech-row">
              <span class="tech-k">Al₂O₃ purity</span
              ><span class="tech-v">≥99.99%</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">HF resistance</span
              ><span class="tech-v">Resistant (unlike quartz)</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Temperature</span
              ><span class="tech-v">Plasma: 6000–8000K</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Form</span
              ><span class="tech-v">Injector / torch tube</span>
            </div>
          </div>
          <div class="rec-row">
            <div class="rec-links">
              <span class="rl">Products →</span
              ><a href="/products/custom-ceramic">Custom Ceramic DFM</a
              ><a href="/products/ceramic-structural"
                >Ceramic Structural Parts</a
              >
            </div>
            <a href="#quote" class="rec-cta-sm">Request Samples →</a>
          </div>
        </div>
      </div>
    </section>
    <section class="scen-img-sec" id="crucibles">
      <div class="scen-full-wrap">
        <div class="scen-img-col">
          <div
            class="scen-img-placeholder"
            style="
              background: linear-gradient(
                145deg,
                #0d1f33 0%,
                #162d47 40%,
                #1e3d5c 100%
              );
              color: #b8d4f0;
            "
          >
            <div class="img-icon">🥣</div>
            <div class="img-label">Al₂O₃ crucible — analytical laboratory</div>
            <div class="img-replace">Replace with product photo</div>
          </div>
        </div>
        <div class="scen-txt-col">
          <div class="scen-eyebrow">03 · Analytical Crucibles & Containers</div>

          <h2 class="scen-h2">
            Al₂O₃ and Pt-lined crucibles for
            <span class="accent">gravimetric analysis.</span>.
          </h2>
          <p class="scen-intro">
            High-purity Al₂O₃ crucibles, evaporating dishes, and combustion
            boats for gravimetric analysis, ignition testing, and sample
            preparation. Precisely known tare weight, no hygroscopic absorption,
            chemically inert to most acids and alkalis.
          </p>
          <div style="margin-bottom: 20px">
            <div class="tech-req-label">Technical Requirements</div>
            <div class="tech-row">
              <span class="tech-k">Material</span
              ><span class="tech-v">Al₂O₃ 99.7%+</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Acid resistance</span
              ><span class="tech-v">Most acids resistant</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Tare weight</span
              ><span class="tech-v">Consistent batch-to-batch</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Form</span
              ><span class="tech-v">Crucible / dish / boat</span>
            </div>
          </div>
          <div class="rec-row">
            <div class="rec-links">
              <span class="rl">Products →</span
              ><a href="/products/custom-ceramic">Custom Ceramic DFM</a
              ><a href="/products/ceramic-structural"
                >Ceramic Structural Parts</a
              >
            </div>
            <a href="#quote" class="rec-cta-sm">Request Samples →</a>
          </div>
        </div>
      </div>
    </section>
    <section class="scen-img-sec alt" id="tga-dsc">
      <div class="scen-full-wrap rev">
        <div class="scen-img-col">
          <div
            class="scen-img-placeholder"
            style="
              background: linear-gradient(
                145deg,
                #0d1f33 0%,
                #162d47 40%,
                #1e3d5c 100%
              );
              color: #b8d4f0;
            "
          >
            <div class="img-icon">📊</div>
            <div class="img-label">
              Al₂O₃ TGA crucible — thermal analysis instrument
            </div>
            <div class="img-replace">Replace with product photo</div>
          </div>
        </div>
        <div class="scen-txt-col">
          <div class="scen-eyebrow">04 · TGA & DSC Instruments</div>

          <h2 class="scen-h2">
            Al₂O₃ and Pt crucibles for
            <span class="accent">thermal analysis.</span>.
          </h2>
          <p class="scen-intro">
            Precision Al₂O₃ sample cups and crucibles for thermogravimetric
            analysis (TGA) and differential scanning calorimetry (DSC).
            Dimensional consistency ±0.05mm — critical for accurate heat flow
            measurement in DSC instruments.
          </p>
          <div style="margin-bottom: 20px">
            <div class="tech-req-label">Technical Requirements</div>
            <div class="tech-row">
              <span class="tech-k">Dimensional tolerance</span
              ><span class="tech-v">±0.05mm</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Thermal mass</span
              ><span class="tech-v">Consistent</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Material</span
              ><span class="tech-v">Al₂O₃ 99.9%</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Instruments</span
              ><span class="tech-v">TA / Mettler / Netzsch compatible</span>
            </div>
          </div>
          <div class="rec-row">
            <div class="rec-links">
              <span class="rl">Products →</span
              ><a href="/products/custom-ceramic">Custom Ceramic DFM</a
              ><a href="/products/ceramic-structural"
                >Ceramic Structural Parts</a
              >
            </div>
            <a href="#quote" class="rec-cta-sm">Request Samples →</a>
          </div>
        </div>
      </div>
    </section>
    <section class="scen-img-sec" id="sem-tem">
      <div class="scen-full-wrap">
        <div class="scen-img-col">
          <div
            class="scen-img-placeholder"
            style="
              background: linear-gradient(
                145deg,
                #0d1f33 0%,
                #162d47 40%,
                #1e3d5c 100%
              );
              color: #b8d4f0;
            "
          >
            <div class="img-icon">🔬</div>
            <div class="img-label">
              Si₃N₄ TEM membrane window — electron microscopy
            </div>
            <div class="img-replace">Replace with product photo</div>
          </div>
        </div>
        <div class="scen-txt-col">
          <div class="scen-eyebrow">05 · SEM & TEM Sample Preparation</div>

          <h2 class="scen-h2">
            Ceramic substrates for
            <span class="accent">electron microscopy.</span>.
          </h2>
          <p class="scen-intro">
            Al₂O₃ and Si₃N₄ membrane windows and substrates for TEM sample
            preparation and SEM mounting. Si₃N₄ TEM membranes — 100nm thick,
            transparent to electrons, mechanically stable under beam
            irradiation.
          </p>
          <div style="margin-bottom: 20px">
            <div class="tech-req-label">Technical Requirements</div>
            <div class="tech-row">
              <span class="tech-k">Si₃N₄ membrane</span
              ><span class="tech-v">100nm thickness</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Electron transparency</span
              ><span class="tech-v">High</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Substrate size</span
              ><span class="tech-v">Custom to drawing</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Surface finish</span
              ><span class="tech-v">Ra ≤0.02µm</span>
            </div>
          </div>
          <div class="rec-row">
            <div class="rec-links">
              <span class="rl">Products →</span
              ><a href="/products/custom-ceramic">Custom Ceramic DFM</a
              ><a href="/products/ceramic-powder">Ceramic Powder</a>
            </div>
            <a href="#quote" class="rec-cta-sm">Request Samples →</a>
          </div>
        </div>
      </div>
    </section>
    <section class="scen-img-sec alt" id="custom-lab">
      <div class="scen-full-wrap rev">
        <div class="scen-img-col">
          <div
            class="scen-img-placeholder"
            style="
              background: linear-gradient(
                145deg,
                #0d1f33 0%,
                #162d47 40%,
                #1e3d5c 100%
              );
              color: #b8d4f0;
            "
          >
            <div class="img-icon">🏺</div>
            <div class="img-label">
              Custom Al₂O₃ laboratory ceramic — research instrument
            </div>
            <div class="img-replace">Replace with product photo</div>
          </div>
        </div>
        <div class="scen-txt-col">
          <div class="scen-eyebrow">06 · Custom Laboratory Ceramics</div>

          <h2 class="scen-h2">
            Custom ceramic parts for
            <span class="accent">specialised instruments.</span>.
          </h2>
          <p class="scen-intro">
            Custom Al₂O₃, Si₃N₄, and ZrO₂ components for bespoke laboratory
            instruments and research apparatus. DFM review in 48 hours,
            prototype in 14 days. Single-unit quantities accepted — no MOQ for
            laboratory research programs.
          </p>
          <div style="margin-bottom: 20px">
            <div class="tech-req-label">Technical Requirements</div>
            <div class="tech-row">
              <span class="tech-k">Lead time</span
              ><span class="tech-v">14 days prototype</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">MOQ</span
              ><span class="tech-v">None (single-unit)</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Materials</span
              ><span class="tech-v">Al₂O₃ / Si₃N₄ / ZrO₂</span>
            </div>
            <div class="tech-row">
              <span class="tech-k">Tolerance</span
              ><span class="tech-v">±0.01mm ground</span>
            </div>
          </div>
          <div class="rec-row">
            <div class="rec-links">
              <span class="rl">Products →</span
              ><a href="/products/custom-ceramic">Custom Ceramic DFM</a
              ><a href="/products/ceramic-structural"
                >Ceramic Structural Parts</a
              >
            </div>
            <a href="#quote" class="rec-cta-sm">Request Samples →</a>
          </div>
        </div>
      </div>
    </section>
    <section class="s quote-sec" id="quote">
      <div class="wrap">
        <div>
          <div class="sec-num">Get a Quote</div>
          <h2>Tell us your <span class="accent">application</span>.</h2>
          <p class="sec-lead" style="color: rgba(255, 255, 255, 0.8)">
            Describe your process environment, operating temperature, chemical
            exposure, and annual volume. Our application engineers match you to
            the right ceramic and respond within one business day.
          </p>
          <ul class="quote-why">
            <li><b>Direct manufacturer pricing</b> — no trading layer.</li>
            <li><b>Free samples</b>, 48-hour dispatch on stock items.</li>
            <li><b>Custom to drawing:</b> DFM in 48h, prototype in 14 days.</li>
            <li>
              <b>ISO 9001 certified</b> — material certs with every shipment.
            </li>
          </ul>
        </div>
        <div class="quote-form-card">
          <div class="qquote-tag">Free Quote · Reply in 24h</div>
          <div class="qquote-title">Request a quote</div>
          <QuoteInquiryForm variant="card" submit-label="Request a Quote →" />
        </div>
      </div>
    </section>
    <section class="s" style="background: var(--bg-soft)">
      <div class="wrap">
        <div class="sec-num">Related</div>
        <h2>Explore <span class="accent">further</span>.</h2>
        <div class="rel-grid">
          <a href="/products/ceramic-structural" class="rel-card"
            ><div class="rel-eyebrow">Product</div>
            <div class="rel-title">Ceramic Structural Parts</div>
            <div class="rel-desc">
              Custom Al₂O₃ and Si₃N₄ laboratory tubes, crucibles, and fixtures.
            </div>
            <div class="rel-link">View →</div></a
          ><a href="/products/custom-ceramic" class="rel-card"
            ><div class="rel-eyebrow">Product</div>
            <div class="rel-title">Custom Ceramic DFM</div>
            <div class="rel-desc">
              Custom ceramic parts to your drawing — 14-day prototype, no MOQ.
            </div>
            <div class="rel-link">View →</div></a
          ><a href="/applications/app-biology-medicine" class="rel-card"
            ><div class="rel-eyebrow">Application</div>
            <div class="rel-title">Biology & Medicine</div>
            <div class="rel-desc">
              Biocompatible ceramic components for medical instruments and
              diagnostics.
            </div>
            <div class="rel-link">View →</div></a
          >
        </div>
      </div>
    </section>
  </div>
</template>

<style>
:root {
  --navy: #0a2647;
  --navy-2: #103a6b;
  --navy-3: #1b4d8a;
  --bg: #ffffff;
  --bg-soft: #f1f5fa;
  --bg-soft-2: #e6eef7;
  --ink: #14233a;
  --ink-soft: #4a5a70;
  --ink-faint: #8794a6;
  --orange: #f26419;
  --orange-deep: #d8530f;
  --gold: #f0a500;
  --gold-soft: #ffd466;
  --line: #dce5ef;
  --line-strong: #c2d0e0;
  --sans: "Barlow", sans-serif;
  --cond: "Barlow Condensed", sans-serif;
  --mono: "IBM Plex Mono", monospace;
}
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}
body {
  background: var(--bg);
  color: var(--ink);
  font-family: var(--sans);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}
a {
  color: inherit;
  text-decoration: none;
  cursor: pointer;
}
.wrap {
  max-width: 1260px;
  margin: 0 auto;
  padding: 0 40px;
}

/* CHROME */
.topbar {
  background: var(--navy);
  color: #c5d4e6;
  font-family: var(--mono);
  font-size: 12px;
}
.topbar .wrap {
  display: flex;
  justify-content: space-between;
  align-items: center;
  height: 38px;
}
.topbar .left {
  display: flex;
  gap: 26px;
}
.topbar .left span {
  display: flex;
  align-items: center;
  gap: 7px;
}
.dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--gold);
}
.topbar .right {
  letter-spacing: 0.05em;
  color: var(--gold);
}
nav {
  position: sticky;
  top: 0;
  z-index: 100;
  background: #fff;
  border-bottom: 3px solid var(--navy);
  box-shadow: 0 2px 12px rgba(10, 38, 71, 0.06);
}
nav .wrap {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 78px;
}
.logo {
  display: flex;
  align-items: center;
  gap: 12px;
}
.logo-mark {
  width: 42px;
  height: 42px;
}
.logo-text {
  font-weight: 800;
  font-size: 24px;
  letter-spacing: -0.02em;
  color: var(--navy);
}
.logo-text b {
  color: var(--orange);
}
.logo-text small {
  display: block;
  font-family: var(--mono);
  font-size: 9px;
  letter-spacing: 0.16em;
  color: var(--ink-faint);
  font-weight: 400;
  margin-top: -2px;
}
.nav-links {
  display: flex;
  gap: 32px;
  align-items: center;
}
.nav-links a {
  font-size: 14.5px;
  font-weight: 700;
  color: var(--navy);
  padding: 6px 0;
  letter-spacing: 0.2px;
}
.nav-links a.active {
  color: var(--orange);
  border-bottom: 2px solid var(--orange);
}
.nav-links a:hover {
  color: var(--orange);
}
.nav-cta {
  background: var(--orange);
  color: #fff !important;
  padding: 12px 24px;
  font-weight: 800;
  font-size: 13.5px;
  letter-spacing: 0.5px;
  transition: background 0.15s;
}
.nav-cta:hover {
  background: var(--orange-deep);
}

.breadcrumb {
  background: var(--bg-soft);
  padding: 14px 0;
  border-bottom: 1px solid var(--line);
}
.breadcrumb .wrap {
  display: flex;
  gap: 10px;
  align-items: center;
  font-family: var(--mono);
  font-size: 12px;
  color: var(--ink-soft);
  letter-spacing: 0.05em;
}
.breadcrumb a {
  color: var(--navy);
  font-weight: 600;
}
.breadcrumb a:hover {
  color: var(--orange);
}
.breadcrumb .sep {
  color: var(--ink-faint);
}

/* HERO */
.page-hero {
  background: linear-gradient(135deg, var(--navy) 0%, var(--navy-2) 100%);
  color: #fff;
  padding: 70px 0 90px;
  position: relative;
  overflow: hidden;
}
.page-hero-stripe {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(
    90deg,
    var(--orange) 0%,
    var(--gold) 50%,
    var(--orange) 100%
  );
}
.page-hero .wrap {
  display: grid;
  grid-template-columns: 1.4fr 1fr;
  gap: 60px;
  align-items: end;
  position: relative;
}
.ph-eyebrow {
  display: inline-block;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.15em;
  padding: 8px 14px;
  margin-bottom: 24px;
  text-transform: uppercase;
  font-weight: 500;
}
.formula-display {
  font-family: var(--cond);
  font-size: 92px;
  font-weight: 800;
  line-height: 0.95;
  letter-spacing: -0.025em;
  margin-bottom: 12px;
}
.formula-display sub {
  font-size: 56px;
  font-weight: 600;
}
.page-hero h1 {
  font-family: var(--cond);
  font-size: 52px;
  line-height: 1.05;
  font-weight: 700;
  letter-spacing: -0.02em;
  margin-bottom: 22px;
  color: var(--gold);
}
.ph-sub {
  font-size: 18px;
  color: rgba(255, 255, 255, 0.85);
  line-height: 1.55;
  max-width: 620px;
  margin-bottom: 32px;
}
.ph-sub b {
  color: var(--gold-soft);
  font-weight: 700;
}
.ph-quickstats {
  display: flex;
  gap: 40px;
}
.ph-stat .num {
  font-family: var(--cond);
  font-size: 44px;
  font-weight: 800;
  line-height: 1;
  color: var(--gold);
}
.ph-stat .lbl {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.13em;
  color: rgba(255, 255, 255, 0.7);
  margin-top: 6px;
  text-transform: uppercase;
}
.ph-side {
  padding-bottom: 14px;
}
.grade-cards {
  display: flex;
  flex-direction: column;
  gap: 14px;
}
.grade-card {
  background: rgba(255, 255, 255, 0.06);
  border-left: 3px solid var(--orange);
  padding: 18px 22px;
  transition:
    background 0.15s,
    border-color 0.15s;
  cursor: pointer;
}
.grade-card:hover {
  background: rgba(255, 255, 255, 0.1);
  border-left-color: var(--gold);
}
.grade-card .grade-label {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.15em;
  color: var(--gold);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 4px;
}
.grade-card .grade-title {
  font-family: var(--cond);
  font-size: 22px;
  font-weight: 700;
  color: #fff;
  letter-spacing: -0.005em;
}
.grade-card .grade-use {
  font-family: var(--mono);
  font-size: 10.5px;
  color: rgba(255, 255, 255, 0.6);
  margin-top: 4px;
  letter-spacing: 0.05em;
}

/* CERT STRIP */
.cert-strip {
  background: var(--bg-soft);
  padding: 24px 0;
  border-bottom: 1px solid var(--line);
}
.cert-strip .wrap {
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 22px;
}
.cs-label {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.15em;
  color: var(--navy);
  text-transform: uppercase;
  font-weight: 500;
}
.cs-badges {
  display: flex;
  gap: 28px;
  flex-wrap: wrap;
}
.cs-badges span {
  font-family: var(--cond);
  font-size: 16px;
  font-weight: 700;
  color: var(--navy);
  display: flex;
  align-items: center;
  gap: 7px;
  letter-spacing: 0.3px;
  text-transform: uppercase;
}
.cs-badges span::before {
  content: "✓";
  color: var(--orange);
  font-weight: 700;
}

/* SECTION */
section.s {
  padding: 80px 0;
}
.sec-num {
  font-family: var(--mono);
  font-size: 11.5px;
  letter-spacing: 0.2em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 500;
  margin-bottom: 14px;
}
h2 {
  font-family: var(--cond);
  font-size: 48px;
  font-weight: 800;
  line-height: 1.05;
  letter-spacing: -0.02em;
  color: var(--ink);
  margin-bottom: 16px;
}
h2 .accent {
  color: var(--orange);
}
.sec-lead {
  color: var(--ink-soft);
  font-size: 16.5px;
  max-width: 720px;
  line-height: 1.6;
  margin-bottom: 44px;
}
.sec-lead b {
  color: var(--navy);
  font-weight: 700;
}

/* PROPERTIES TABLE */
.props-wrap {
  background: var(--navy);
  color: #fff;
  padding: 8px;
  overflow-x: auto;
}
table.props {
  width: 100%;
  border-collapse: collapse;
}
table.props th,
table.props td {
  padding: 16px 18px;
  text-align: left;
  font-size: 14px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
}
table.props thead th {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.13em;
  color: var(--gold);
  text-transform: uppercase;
  font-weight: 500;
  padding-top: 22px;
  padding-bottom: 14px;
  border-bottom: 2px solid rgba(255, 255, 255, 0.15);
  background: var(--navy-2);
}
table.props tbody th {
  font-family: var(--cond);
  font-size: 18px;
  font-weight: 700;
  color: #fff;
}
table.props tbody td {
  color: rgba(255, 255, 255, 0.85);
  font-family: var(--cond);
  font-size: 18px;
  font-weight: 700;
}
table.props tbody td.unit {
  font-family: var(--mono);
  font-size: 11px;
  color: rgba(255, 255, 255, 0.55);
  font-weight: 400;
  letter-spacing: 0.05em;
}
table.props tbody tr:hover {
  background: rgba(255, 255, 255, 0.03);
}
.props-legend {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--ink-soft);
  margin-top: 16px;
  letter-spacing: 0.05em;
}

/* APPLICATIONS GRID */
.app-uses {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 22px;
}
.app-use {
  background: #fff;
  border: 1px solid var(--line);
  padding: 28px;
  position: relative;
  display: flex;
  flex-direction: column;
  gap: 14px;
  transition:
    border-color 0.15s,
    transform 0.15s;
}
.app-use:hover {
  border-color: var(--orange);
  transform: translateY(-3px);
  box-shadow: 0 20px 40px -20px rgba(10, 38, 71, 0.15);
}
.app-use-bar {
  position: absolute;
  top: -1px;
  left: 28px;
  width: 40px;
  height: 3px;
  background: var(--orange);
}
.app-use-icon {
  font-size: 32px;
  line-height: 1;
  margin-top: 8px;
}
.app-use-title {
  font-family: var(--cond);
  font-size: 22px;
  font-weight: 700;
  color: var(--navy);
  line-height: 1.15;
  letter-spacing: -0.005em;
}
.app-use-desc {
  font-size: 13.5px;
  color: var(--ink-soft);
  line-height: 1.55;
  flex: 1;
}
.app-use-grade {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--orange);
  letter-spacing: 0.05em;
  padding-top: 12px;
  border-top: 1px solid var(--line);
  font-weight: 600;
}
.app-use-grade b {
  color: var(--navy);
}

/* COMPARISON CARDS */
.vs-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 28px;
}
.vs-card {
  background: #fff;
  border: 1px solid var(--line);
  padding: 32px;
}
.vs-card.win {
  border-color: var(--orange);
  border-width: 2px;
  background: linear-gradient(180deg, #fff 0%, var(--bg-soft) 100%);
}
.vs-card-head {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  margin-bottom: 16px;
  padding-bottom: 16px;
  border-bottom: 1px solid var(--line);
}
.vs-card-head h3 {
  font-family: var(--cond);
  font-size: 26px;
  font-weight: 800;
  color: var(--navy);
  letter-spacing: -0.01em;
}
.vs-tag {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.15em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 700;
  background: rgba(242, 100, 25, 0.08);
  padding: 4px 10px;
}
.vs-tag.alumina-tag {
  background: var(--orange);
  color: #fff;
}
.vs-points {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.vs-points li {
  display: flex;
  gap: 10px;
  align-items: baseline;
  font-size: 14px;
  color: var(--ink-soft);
  line-height: 1.5;
}
.vs-points li::before {
  content: "→";
  color: var(--orange);
  flex-shrink: 0;
  font-weight: 700;
}
.vs-points li b {
  color: var(--navy);
  font-weight: 700;
}

/* CTA BAND */
.casecta {
  background: var(--bg-soft);
  padding: 60px 0;
  border-top: 1px solid var(--line);
  border-bottom: 1px solid var(--line);
}
.casecta .wrap {
  display: grid;
  grid-template-columns: 1.3fr 1fr;
  gap: 50px;
  align-items: center;
}
.casecta-eyebrow {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.18em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 14px;
}
.casecta h2 {
  font-size: 38px;
  line-height: 1.08;
  margin-bottom: 18px;
}
.casecta p {
  font-size: 16px;
  color: var(--ink-soft);
  margin-bottom: 26px;
  line-height: 1.6;
}
.casecta-ctas {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}
.btn-primary {
  background: var(--orange);
  color: #fff;
  padding: 16px 28px;
  font-weight: 800;
  font-size: 14px;
  letter-spacing: 0.2px;
  border: none;
  cursor: pointer;
  transition: background 0.15s;
  display: inline-flex;
  align-items: center;
  gap: 8px;
}
.btn-primary:hover {
  background: var(--orange-deep);
}
.btn-ghost {
  background: transparent;
  color: var(--navy);
  padding: 16px 26px;
  font-weight: 700;
  font-size: 14px;
  letter-spacing: 0.2px;
  border: 2px solid var(--navy);
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 8px;
}
.btn-ghost:hover {
  border-color: var(--orange);
  color: var(--orange);
}
.casecta-side {
  background: #fff;
  border: 1px solid var(--line);
  padding: 28px;
  position: relative;
}
.casecta-side::before {
  content: "";
  position: absolute;
  top: -1px;
  left: 0;
  width: 70px;
  height: 4px;
  background: var(--orange);
}
.casecta-side-tag {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.15em;
  color: var(--orange);
  text-transform: uppercase;
  margin-bottom: 12px;
  font-weight: 600;
}
.casecta-side-title {
  font-family: var(--cond);
  font-size: 22px;
  font-weight: 700;
  color: var(--navy);
  margin-bottom: 16px;
  line-height: 1.2;
}
.casecta-side-list {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.casecta-side-list li {
  font-size: 13.5px;
  color: var(--ink-soft);
  display: flex;
  gap: 10px;
  align-items: baseline;
  padding-bottom: 10px;
  border-bottom: 1px dashed var(--line);
}
.casecta-side-list li:last-child {
  border-bottom: none;
  padding-bottom: 0;
}
.casecta-side-list li b {
  font-family: var(--cond);
  font-size: 15px;
  font-weight: 700;
  color: var(--navy);
  min-width: 70px;
}

/* FAQ */
.faq-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 18px 50px;
}
.faq-item {
  padding: 18px 0;
  border-bottom: 1px solid var(--line);
}
.faq-item:last-child,
.faq-item:nth-last-child(2) {
  border-bottom: none;
}
.faq-q {
  font-family: var(--cond);
  font-size: 19px;
  font-weight: 700;
  color: var(--navy);
  margin-bottom: 10px;
  letter-spacing: -0.005em;
  line-height: 1.25;
}
.faq-q::before {
  content: "Q";
  display: inline-block;
  width: 24px;
  height: 24px;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 12px;
  line-height: 24px;
  text-align: center;
  margin-right: 10px;
  vertical-align: middle;
  font-weight: 700;
}
.faq-a {
  font-size: 14px;
  color: var(--ink-soft);
  line-height: 1.6;
  padding-left: 34px;
}

footer {
  background: var(--navy);
  color: #c5d4e6;
  padding: 70px 0 30px;
}
.foot-grid {
  display: grid;
  grid-template-columns: 1.5fr 1fr 1fr 1fr 1.2fr;
  gap: 40px;
  margin-bottom: 50px;
}
.foot-col h5 {
  font-family: var(--cond);
  font-size: 16px;
  font-weight: 700;
  color: #fff;
  letter-spacing: 0.05em;
  margin-bottom: 16px;
  text-transform: uppercase;
}
.foot-col a {
  display: block;
  font-size: 13.5px;
  color: rgba(255, 255, 255, 0.65);
  margin-bottom: 9px;
  transition: color 0.15s;
}
.foot-col a:hover {
  color: var(--gold);
}
.fb-right p {
  font-size: 13.5px;
  color: rgba(255, 255, 255, 0.65);
  margin-bottom: 10px;
  line-height: 1.55;
}
.fb-right p b {
  color: #fff;
  font-weight: 600;
}
.foot-bottom {
  padding-top: 24px;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  font-family: var(--mono);
  font-size: 11.5px;
  color: rgba(255, 255, 255, 0.5);
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 14px;
}
.foot-bottom span {
  display: flex;
  gap: 22px;
}

@media (max-width: 1024px) {
  .page-hero .wrap {
    grid-template-columns: 1fr;
    gap: 40px;
  }
  .app-uses {
    grid-template-columns: 1fr 1fr;
  }
  .vs-grid {
    grid-template-columns: 1fr;
  }
  .casecta .wrap {
    grid-template-columns: 1fr;
  }
  .faq-grid {
    grid-template-columns: 1fr;
    gap: 0;
  }
  .foot-grid {
    grid-template-columns: 1fr 1fr;
  }
}
@media (max-width: 640px) {
  .wrap {
    padding: 0 20px;
  }
  .formula-display {
    font-size: 72px;
  }
  .page-hero h1 {
    font-size: 38px;
  }
  .app-uses {
    grid-template-columns: 1fr;
  }
  .topbar .left,
  .nav-links {
    display: none;
  }
}

/* ============================================================
     PRODUCT-PAGE TEMPLATE (distinct from material-page template)
     - hero carries an inline Quick-Quote form (core CTR)
     - sub-product catalog lineup replaces the spec datasheet table
     - benefit checklist replaces the vs-comparison grid
     ============================================================ */

/* PRODUCT HERO (quote-led) */
.prod-hero {
  background: linear-gradient(135deg, var(--navy) 0%, var(--navy-2) 100%);
  color: #fff;
  padding: 64px 0 78px;
  position: relative;
  overflow: hidden;
}
.prod-hero-stripe {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: linear-gradient(
    90deg,
    var(--orange) 0%,
    var(--gold) 50%,
    var(--orange) 100%
  );
}
.prod-hero .wrap {
  display: grid;
  grid-template-columns: 1.15fr 0.85fr;
  gap: 56px;
  align-items: start;
  position: relative;
}
.ph-eyebrow {
  display: inline-block;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.15em;
  padding: 8px 14px;
  margin-bottom: 22px;
  text-transform: uppercase;
  font-weight: 500;
}
.prod-hero h1 {
  font-family: var(--cond);
  font-size: 56px;
  line-height: 1.02;
  font-weight: 800;
  letter-spacing: -0.02em;
  margin-bottom: 20px;
  color: #fff;
}
.prod-hero h1 .hl {
  color: var(--gold);
}
.prod-hero .ph-sub {
  font-size: 17px;
  color: rgba(255, 255, 255, 0.85);
  line-height: 1.55;
  max-width: 600px;
  margin-bottom: 30px;
}
.prod-hero .ph-sub b {
  color: var(--gold-soft);
  font-weight: 700;
}
.ph-statbar {
  display: flex;
  gap: 36px;
  flex-wrap: wrap;
  padding-top: 26px;
  border-top: 1px solid rgba(255, 255, 255, 0.15);
}
.ph-statbar .num {
  font-family: var(--cond);
  font-size: 38px;
  font-weight: 800;
  line-height: 1;
  color: var(--gold);
}
.ph-statbar .lbl {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.12em;
  color: rgba(255, 255, 255, 0.7);
  margin-top: 6px;
  text-transform: uppercase;
}

/* QUICK-QUOTE FORM CARD (the conversion engine) */
.qquote {
  background: #fff;
  padding: 30px 30px 32px;
  position: relative;
  box-shadow: 0 30px 60px -25px rgba(0, 0, 0, 0.5);
}
.qquote::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 4px;
  background: var(--orange);
}
.qquote-tag {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.16em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 8px;
}
.qquote-title {
  font-family: var(--cond);
  font-size: 27px;
  font-weight: 800;
  color: var(--navy);
  line-height: 1.08;
  letter-spacing: -0.01em;
  margin-bottom: 6px;
}
.qquote-note {
  font-size: 12.5px;
  color: var(--ink-soft);
  line-height: 1.5;
  margin-bottom: 20px;
}
.qq-field {
  margin-bottom: 13px;
}
.qq-field label {
  display: block;
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--ink-faint);
  font-weight: 500;
  margin-bottom: 5px;
}
.qq-field input,
.qq-field select,
.qq-field textarea {
  width: 100%;
  font-family: var(--sans);
  font-size: 14px;
  color: var(--ink);
  border: 1px solid var(--line-strong);
  padding: 11px 13px;
  background: var(--bg-soft);
  transition:
    border-color 0.15s,
    background 0.15s;
}
.qq-field input:focus,
.qq-field select:focus,
.qq-field textarea:focus {
  outline: none;
  border-color: var(--orange);
  background: #fff;
}
.qq-field textarea {
  resize: vertical;
  min-height: 64px;
}
.qq-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 13px;
}
.qq-submit {
  width: 100%;
  background: var(--orange);
  color: #fff;
  border: none;
  padding: 15px;
  font-family: var(--sans);
  font-weight: 800;
  font-size: 14.5px;
  letter-spacing: 0.3px;
  cursor: pointer;
  transition: background 0.15s;
  margin-top: 6px;
}
.qq-submit:hover {
  background: var(--orange-deep);
}
.qq-trust {
  font-family: var(--mono);
  font-size: 10.5px;
  color: var(--ink-faint);
  text-align: center;
  margin-top: 12px;
  letter-spacing: 0.03em;
}

/* SUB-PRODUCT LINEUP (catalog — replaces datasheet table) */
.lineup {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 24px;
}
.lineup-card {
  background: #fff;
  border: 1px solid var(--line);
  padding: 30px;
  position: relative;
  display: flex;
  flex-direction: column;
  transition:
    border-color 0.15s,
    transform 0.15s,
    box-shadow 0.15s;
}
.lineup-card:hover {
  border-color: var(--orange);
  transform: translateY(-3px);
  box-shadow: 0 24px 46px -24px rgba(10, 38, 71, 0.18);
}
.lineup-card.flagship {
  border-color: var(--orange);
  border-width: 2px;
  background: linear-gradient(180deg, #fff 0%, var(--bg-soft) 100%);
}
.lineup-badge {
  position: absolute;
  top: 0;
  right: 0;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 9.5px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  font-weight: 600;
  padding: 6px 12px;
}
.lineup-head {
  display: flex;
  align-items: flex-start;
  gap: 14px;
  margin-bottom: 14px;
}
.lineup-icon {
  font-size: 30px;
  line-height: 1;
  flex-shrink: 0;
  margin-top: 2px;
}
.lineup-name {
  font-family: var(--cond);
  font-size: 25px;
  font-weight: 800;
  color: var(--navy);
  line-height: 1.08;
  letter-spacing: -0.01em;
}
.lineup-cn {
  font-family: var(--sans);
  font-size: 12.5px;
  color: var(--ink-faint);
  font-weight: 600;
  margin-top: 2px;
}
.lineup-desc {
  font-size: 13.5px;
  color: var(--ink-soft);
  line-height: 1.55;
  margin-bottom: 16px;
  flex: 1;
}
.lineup-specs {
  display: flex;
  flex-wrap: wrap;
  gap: 7px;
  margin-bottom: 18px;
}
.spec-chip {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.03em;
  color: var(--navy);
  background: var(--bg-soft-2);
  padding: 5px 9px;
  font-weight: 500;
}
.spec-chip b {
  color: var(--orange);
  font-weight: 600;
}
.lineup-foot {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 15px;
  border-top: 1px solid var(--line);
}
.lineup-mat {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--ink-soft);
  letter-spacing: 0.04em;
}
.lineup-mat b {
  color: var(--navy);
  font-weight: 600;
}
.lineup-mat a {
  color: var(--orange);
  font-weight: 600;
}
.lineup-link {
  font-family: var(--sans);
  font-size: 13px;
  font-weight: 800;
  color: var(--orange);
  letter-spacing: 0.2px;
  white-space: nowrap;
}
.lineup-link:hover {
  color: var(--orange-deep);
}

/* BENEFIT CHECKLIST (replaces vs-comparison grid) */
.benefits {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 22px 56px;
}
.benefit {
  display: flex;
  gap: 16px;
  align-items: flex-start;
  padding-bottom: 20px;
  border-bottom: 1px solid var(--line);
}
.benefit-ico {
  flex-shrink: 0;
  width: 30px;
  height: 30px;
  background: var(--orange);
  color: #fff;
  font-weight: 800;
  font-size: 15px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-top: 2px;
}
.benefit-ico::before {
  content: "✓";
}
.benefit-body h4 {
  font-family: var(--cond);
  font-size: 20px;
  font-weight: 700;
  color: var(--navy);
  letter-spacing: -0.005em;
  margin-bottom: 5px;
  line-height: 1.15;
}
.benefit-body p {
  font-size: 13.5px;
  color: var(--ink-soft);
  line-height: 1.55;
}

@media (max-width: 1024px) {
  .prod-hero .wrap {
    grid-template-columns: 1fr;
    gap: 40px;
  }
  .lineup {
    grid-template-columns: 1fr;
  }
  .benefits {
    grid-template-columns: 1fr;
    gap: 0;
  }
}
@media (max-width: 640px) {
  .prod-hero h1 {
    font-size: 38px;
  }
  .qq-row {
    grid-template-columns: 1fr;
  }
  .lineup-foot {
    flex-direction: column;
    align-items: flex-start;
    gap: 10px;
  }
}

/* ============================================================
     PRODUCT DETAIL PAGE (BLK Si3N4) — extends product template
     ============================================================ */

/* HERO SPOTLIGHT CARD (right column on detail hero) */
.spotlight-tag {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.16em;
  color: var(--gold);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 18px;
}
.spotlight-row {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  padding: 13px 0;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}
.spotlight-row:last-child {
  border-bottom: none;
}
.spotlight-row .k {
  font-family: var(--mono);
  font-size: 11px;
  color: rgba(255, 255, 255, 0.6);
  letter-spacing: 0.05em;
}
.spotlight-row .v {
  font-family: var(--cond);
  font-size: 20px;
  font-weight: 700;
  color: #fff;
}
.spotlight-row .v b {
  color: var(--gold);
}
.hero-ctas {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  margin-top: 30px;
}

/* DOWNLOAD CARDS */
.dl-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 20px;
}
.dl-card {
  background: #fff;
  border: 1px solid var(--line);
  padding: 26px;
  display: flex;
  flex-direction: column;
  gap: 12px;
  transition:
    border-color 0.15s,
    transform 0.15s;
  position: relative;
}
.dl-card:hover {
  border-color: var(--orange);
  transform: translateY(-3px);
  box-shadow: 0 20px 40px -22px rgba(10, 38, 71, 0.18);
}
.dl-icon {
  font-size: 30px;
  line-height: 1;
}
.dl-name {
  font-family: var(--cond);
  font-size: 21px;
  font-weight: 700;
  color: var(--navy);
  line-height: 1.12;
}
.dl-meta {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--ink-faint);
  letter-spacing: 0.05em;
}
.dl-btn {
  margin-top: auto;
  font-family: var(--sans);
  font-size: 13px;
  font-weight: 800;
  color: var(--orange);
  letter-spacing: 0.2px;
  padding-top: 10px;
  border-top: 1px solid var(--line);
}
.dl-btn:hover {
  color: var(--orange-deep);
}

/* CERTIFICATION DETAIL CARDS */
.cert-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20px;
}
.cert-card {
  background: #fff;
  border: 1px solid var(--line);
  padding: 24px;
  position: relative;
}
.cert-card::before {
  content: "";
  position: absolute;
  top: -1px;
  left: 0;
  width: 50px;
  height: 4px;
  background: var(--orange);
}
.cert-card .cc-mark {
  font-family: var(--cond);
  font-size: 26px;
  font-weight: 800;
  color: var(--navy);
  letter-spacing: -0.01em;
  margin-bottom: 6px;
}
.cert-card .cc-scope {
  font-size: 13px;
  color: var(--ink-soft);
  line-height: 1.5;
}
.cert-card .cc-tag {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.12em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 600;
  margin-top: 12px;
}

/* RELATED INTERNAL LINKS (Product -> Material + Application) */
.rel-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 22px;
}
.rel-card {
  background: #fff;
  border: 1px solid var(--line);
  padding: 28px;
  display: flex;
  flex-direction: column;
  gap: 10px;
  transition:
    border-color 0.15s,
    transform 0.15s;
}
.rel-card:hover {
  border-color: var(--orange);
  transform: translateY(-3px);
  box-shadow: 0 20px 40px -22px rgba(10, 38, 71, 0.15);
}
.rel-eyebrow {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.14em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 600;
}
.rel-title {
  font-family: var(--cond);
  font-size: 22px;
  font-weight: 700;
  color: var(--navy);
  line-height: 1.15;
}
.rel-desc {
  font-size: 13px;
  color: var(--ink-soft);
  line-height: 1.5;
}
.rel-link {
  font-family: var(--sans);
  font-size: 13px;
  font-weight: 800;
  color: var(--orange);
  margin-top: 4px;
}

/* QUOTE SECTION (full-width, 2-col) */
.quote-sec {
  background: var(--navy);
  color: #fff;
}
.quote-sec .wrap {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 60px;
  align-items: start;
}
.quote-sec .sec-num {
  color: var(--gold);
}
.quote-sec h2 {
  color: #fff;
}
.quote-sec h2 .accent {
  color: var(--gold);
}
.quote-why {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 16px;
  margin-top: 24px;
}
.quote-why li {
  display: flex;
  gap: 13px;
  align-items: flex-start;
  font-size: 15px;
  color: rgba(255, 255, 255, 0.88);
  line-height: 1.5;
}
.quote-why li::before {
  content: "✓";
  color: var(--gold);
  font-weight: 800;
  flex-shrink: 0;
}
.quote-why li b {
  color: #fff;
  font-weight: 700;
}
.quote-form-card {
  background: #fff;
  padding: 32px;
}
.quote-form-card .qquote-title {
  color: var(--navy);
}

@media (max-width: 1024px) {
  .dl-grid {
    grid-template-columns: 1fr;
  }
  .cert-grid {
    grid-template-columns: 1fr 1fr;
  }
  .rel-grid {
    grid-template-columns: 1fr;
  }
  .quote-sec .wrap {
    grid-template-columns: 1fr;
    gap: 36px;
  }
}
@media (max-width: 640px) {
  .cert-grid {
    grid-template-columns: 1fr;
  }
  .spotlight {
    margin-top: 0;
  }
}

/* ============================================================
     APPLICATION VERTICAL DETAIL — v2
     Hero: single-column (no height imbalance) + scenario pill nav
     Scenarios: image-left + content-right full-bleed grid
     ============================================================ */

/* ---- APPLICATION HERO (single-column, no forced 2-col) ---- */
.app-hero {
  background: linear-gradient(135deg, var(--navy) 0%, var(--navy-2) 100%);
  color: #fff;
  padding: 68px 0 0;
  position: relative;
  overflow: hidden;
}
.app-hero-stripe {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3px;
  background: linear-gradient(
    90deg,
    var(--orange) 0%,
    var(--gold) 50%,
    var(--orange) 100%
  );
}
.app-hero-eyebrow {
  display: inline-block;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.15em;
  padding: 8px 14px;
  margin-bottom: 24px;
  text-transform: uppercase;
  font-weight: 500;
}
.app-hero h1 {
  font-family: var(--cond);
  font-size: 60px;
  line-height: 1.02;
  font-weight: 800;
  letter-spacing: -0.025em;
  margin-bottom: 22px;
  color: #fff;
  max-width: 860px;
}
.app-hero h1 .hl {
  color: var(--gold);
}
.app-hero .app-hero-sub {
  font-size: 18px;
  color: rgba(255, 255, 255, 0.82);
  line-height: 1.55;
  max-width: 760px;
  margin-bottom: 32px;
}
.app-hero .app-hero-sub b {
  color: var(--gold-soft);
  font-weight: 700;
}
.app-hero-stats {
  display: flex;
  gap: 44px;
  flex-wrap: wrap;
  margin-bottom: 36px;
}
.app-hero-stats .num {
  font-family: var(--cond);
  font-size: 42px;
  font-weight: 800;
  line-height: 1;
  color: var(--gold);
}
.app-hero-stats .lbl {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.12em;
  color: rgba(255, 255, 255, 0.65);
  margin-top: 6px;
  text-transform: uppercase;
}
.app-hero-ctas {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  margin-bottom: 48px;
}

/* Scenario pill nav (bottom strip inside hero) */
.app-scen-nav {
  border-top: 1px solid rgba(255, 255, 255, 0.14);
  padding: 24px 0 0;
}
.app-scen-nav-label {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.16em;
  color: rgba(255, 255, 255, 0.5);
  text-transform: uppercase;
  margin-bottom: 14px;
}
.app-scen-pills {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 6px;
  padding-bottom: 28px;
}
.app-scen-pill {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 11px 14px;
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.1);
  transition:
    background 0.15s,
    border-color 0.15s;
}
.app-scen-pill:hover {
  background: rgba(255, 255, 255, 0.1);
  border-color: var(--orange);
}
.app-scen-pill.flag {
  border-color: var(--orange);
  background: rgba(242, 100, 25, 0.12);
}
.app-scen-pill .pill-num {
  font-family: var(--mono);
  font-size: 11px;
  font-weight: 500;
  color: var(--gold);
  letter-spacing: 0.05em;
  flex-shrink: 0;
}
.app-scen-pill .pill-name {
  font-family: var(--sans);
  font-size: 13px;
  font-weight: 600;
  color: rgba(255, 255, 255, 0.88);
  line-height: 1.25;
}

/* ---- SCENARIO SECTIONS WITH IMAGE ---- */
.scen-img-sec {
  padding: 0;
}
.scen-img-sec.alt {
  background: var(--bg-soft);
}
.scen-full-wrap {
  display: grid;
  grid-template-columns: 420px 1fr;
  min-height: 460px;
  max-width: 1260px;
  margin: 0 auto;
  padding: 0 0 0 40px;
  gap: 0;
  align-items: stretch;
}
.scen-full-wrap.rev {
  grid-template-columns: 1fr 420px;
  padding: 0 40px 0 0;
}
.scen-full-wrap.rev .scen-img-col {
  order: 2;
}
.scen-full-wrap.rev .scen-txt-col {
  order: 1;
  padding: 60px 56px 60px 0;
}

/* Image column — placeholder styled to look like real imagery */
.scen-img-col {
  position: relative;
  overflow: hidden;
  flex-shrink: 0;
  min-height: 460px;
}
.scen-img-placeholder {
  width: 100%;
  height: 100%;
  min-height: 460px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 14px;
  position: relative;
}
.scen-img-placeholder .img-icon {
  font-size: 56px;
  line-height: 1;
  opacity: 0.9;
}
.scen-img-placeholder .img-label {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  opacity: 0.65;
  font-weight: 500;
  text-align: center;
  max-width: 180px;
  line-height: 1.5;
}
.scen-img-placeholder .img-replace {
  position: absolute;
  bottom: 14px;
  left: 0;
  right: 0;
  text-align: center;
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.08em;
  opacity: 0.45;
  text-transform: uppercase;
}
/* Real photo: replace placeholder div with <img> using same .scen-img-col wrapper */
.scen-img-col img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

/* Scenario colors */
.si-biomass {
  background: linear-gradient(145deg, #1a0f05 0%, #3d1a08 40%, #6b2d0e 100%);
  color: #f5d9b8;
}
.si-bbq {
  background: linear-gradient(145deg, #1a0a05 0%, #4a1a08 40%, #7a2e10 100%);
  color: #fad4a8;
}
.si-gas {
  background: linear-gradient(145deg, #0a1a2a 0%, #152840 40%, #1e3d5c 100%);
  color: #b8d4f0;
}
.si-boiler {
  background: linear-gradient(145deg, #0f0f14 0%, #1e1e2e 40%, #2e2e44 100%);
  color: #c4c4e0;
}
.si-water {
  background: linear-gradient(145deg, #051520 0%, #0a2535 40%, #0f3650 100%);
  color: #a8d4f5;
}
.si-hvac {
  background: linear-gradient(145deg, #051a1a 0%, #0a2e2e 40%, #103e3e 100%);
  color: #a8e0e0;
}
.si-ovens {
  background: linear-gradient(145deg, #1a1005 0%, #3d2808 40%, #5c3c0f 100%);
  color: #f0d8a8;
}
.si-hydro {
  background: linear-gradient(145deg, #051a10 0%, #0a3020 40%, #0f4530 100%);
  color: #a8f0c4;
}

/* Text column */
.scen-txt-col {
  padding: 60px 0 60px 56px;
  display: flex;
  flex-direction: column;
  justify-content: center;
}
.scen-eyebrow {
  font-family: var(--mono);
  font-size: 11px;
  letter-spacing: 0.18em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 500;
  margin-bottom: 10px;
}
.scen-flag {
  display: inline-block;
  background: var(--orange);
  color: #fff;
  font-family: var(--mono);
  font-size: 9.5px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  font-weight: 600;
  padding: 5px 10px;
  margin-bottom: 12px;
}
.scen-h2 {
  font-family: var(--cond);
  font-size: 36px;
  font-weight: 800;
  line-height: 1.05;
  letter-spacing: -0.02em;
  color: var(--ink);
  margin-bottom: 14px;
}
.scen-h2 .accent {
  color: var(--orange);
}
.scen-intro {
  font-size: 15px;
  color: var(--ink-soft);
  line-height: 1.6;
  margin-bottom: 24px;
}
.scen-intro b {
  color: var(--navy);
  font-weight: 700;
}

/* Tech requirements — clean key-value, NOT dark table */
.tech-req-label {
  font-family: var(--mono);
  font-size: 10.5px;
  letter-spacing: 0.13em;
  color: var(--navy);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 10px;
  border-bottom: 2px solid var(--ink);
  padding-bottom: 8px;
}
.tech-row {
  display: grid;
  grid-template-columns: 1fr 1.3fr;
  gap: 6px;
  align-items: baseline;
  padding: 9px 0;
  border-bottom: 1px solid var(--line);
}
.tech-row:last-child {
  border-bottom: none;
}
.tech-k {
  font-family: var(--mono);
  font-size: 11px;
  color: var(--ink-faint);
  letter-spacing: 0.04em;
}
.tech-v {
  font-family: var(--cond);
  font-size: 17px;
  font-weight: 700;
  color: var(--navy);
}
.tech-v .hi {
  color: var(--orange);
}
.tech-v small {
  font-family: var(--mono);
  font-size: 10.5px;
  color: var(--ink-soft);
  font-weight: 400;
  margin-left: 3px;
}

/* Recommended products inline */
.rec-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 12px;
  padding-top: 20px;
  margin-top: 20px;
  border-top: 1px solid var(--line);
}
.rec-links {
  display: flex;
  flex-wrap: wrap;
  gap: 8px 18px;
  font-family: var(--mono);
  font-size: 11.5px;
}
.rec-links .rl {
  color: var(--navy);
  font-weight: 600;
}
.rec-links a {
  color: var(--orange);
  font-weight: 600;
}
.rec-links a:hover {
  color: var(--orange-deep);
}
.rec-cta-sm {
  font-family: var(--sans);
  font-size: 13.5px;
  font-weight: 800;
  color: var(--orange);
  white-space: nowrap;
  border: 2px solid var(--orange);
  padding: 10px 18px;
  transition:
    background 0.15s,
    color 0.15s;
}
.rec-cta-sm:hover {
  background: var(--orange);
  color: #fff;
}

/* Flagship expanded case study (Biomass) */
.flagship-ext {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8px;
  margin-top: 18px;
}
.fe-stat {
  background: var(--navy);
  padding: 14px 16px;
}
.fe-stat .fn {
  font-family: var(--cond);
  font-size: 26px;
  font-weight: 800;
  color: var(--gold);
  line-height: 1;
}
.fe-stat .fl {
  font-family: var(--mono);
  font-size: 10px;
  color: rgba(255, 255, 255, 0.65);
  letter-spacing: 0.04em;
  margin-top: 4px;
}
.fe-case-label {
  font-family: var(--mono);
  font-size: 10px;
  letter-spacing: 0.14em;
  color: var(--orange);
  text-transform: uppercase;
  font-weight: 600;
  margin-bottom: 8px;
}

@media (max-width: 1024px) {
  .app-scen-pills {
    grid-template-columns: repeat(2, 1fr);
  }
  .scen-full-wrap {
    grid-template-columns: 1fr;
    padding: 0;
    min-height: auto;
  }
  .scen-full-wrap.rev {
    padding: 0;
  }
  .scen-full-wrap.rev .scen-img-col {
    order: 1;
  }
  .scen-full-wrap.rev .scen-txt-col {
    order: 2;
    padding: 40px 40px;
  }
  .scen-img-col {
    min-height: 260px;
  }
  .scen-img-placeholder {
    min-height: 260px;
  }
  .scen-txt-col {
    padding: 40px 40px;
  }
  .flagship-ext {
    grid-template-columns: repeat(2, 1fr);
  }
}
@media (max-width: 640px) {
  .app-hero h1 {
    font-size: 40px;
  }
  .app-scen-pills {
    grid-template-columns: 1fr;
  }
  .tech-row {
    grid-template-columns: 1fr;
  }
  .rec-row {
    flex-direction: column;
    align-items: flex-start;
  }
  .scen-txt-col {
    padding: 32px 20px;
  }
  .flagship-ext {
    grid-template-columns: 1fr 1fr;
  }
}

/* Column alignment fix: equal columns for all scenarios */
.scen-full-wrap {
  grid-template-columns: 1fr 1fr !important;
  padding: 0 !important;
}
.scen-full-wrap.rev {
  grid-template-columns: 1fr 1fr !important;
  padding: 0 !important;
}
.scen-txt-col {
  padding: 60px 64px !important;
}
.scen-full-wrap.rev .scen-txt-col {
  padding: 60px 64px 60px 56px !important;
  order: 1 !important;
}
.scen-full-wrap.rev .scen-img-col {
  order: 2 !important;
}
@media (max-width: 900px) {
  .scen-full-wrap,
  .scen-full-wrap.rev {
    grid-template-columns: 1fr !important;
  }
  .scen-full-wrap.rev .scen-img-col {
    order: 1 !important;
  }
  .scen-full-wrap.rev .scen-txt-col {
    order: 2 !important;
    padding: 40px 32px !important;
  }
  .scen-txt-col {
    padding: 40px 32px !important;
  }
}
</style>
