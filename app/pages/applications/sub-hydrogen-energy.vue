<script setup lang="ts">
useHead({
  title: 'Biomass & Pellet Stove Igniters — Si₃N₄ CN-300 for High-Cycle Ignition | Ceramitell',
  meta: [{ name: 'description', content: 'Silicon nitride (Si₃N₄) hot surface igniters for biomass and pellet stoves. 100K+ cycle life, 3-second heat-up, UL/CE certified. Italian OEM case: 180,000 units/year, −38% warranty returns. Free samples, 48h dispatch.' }],
})
</script>

<template>
  <div>
    <div class="breadcrumb">
      <div class="wrap">
        <a href="/">Home</a>
        <span class="sep">/</span>
        <a href="/applications">Applications</a>
        <span class="sep">/</span>
        <a href="/applications/advanced-combustion-fixed">Advanced Combustion</a>
        <span class="sep">/</span>
        <span>Hydrogen Energy</span>
      </div>
    </div>

    <!-- HERO -->
    <section class="bio-hero" style="background:linear-gradient(135deg,#05101a 0%,#0a1e30 50%,var(--navy) 100%);">
      <div class="bio-hero-stripe"></div>
      <div class="wrap">
        <div>
          <div class="bio-hero-eyebrow">Advanced Combustion · Scenario 08</div>
          <h1>Hydrogen combustion<br/><span class="hl">ignition systems.</span></h1>
          <p class="bio-hero-sub">Ceramic igniters for <b>hydrogen burners, H₂-blended gas appliances, and hydrogen fuel cells with combustion backup</b>. H₂ flames are invisible — reliable first-attempt ignition is a critical safety requirement, not just a performance metric.</p>
          <div class="bio-statbar"><div><div class="num">H₂-rated</div><div class="lbl">100% H₂ compatible</div></div><div><div class="num">3 sec</div><div class="lbl">Heat-Up</div></div><div><div class="num">First attempt</div><div class="lbl">Ignition reliability</div></div><div><div class="num">Safety-critical</div><div class="lbl">Application</div></div></div>
          <div class="bio-hero-ctas">
            <a class="btn-primary" href="#quote">Request Free Samples →</a>
            <a class="btn-ghost" style="color:#fff;border-color:rgba(255,255,255,0.4);" href="/products/silicon-nitride-igniters-final">View CN-300 Specs →</a>
          </div>
          <div class="bio-hero-nav-bottom">
            <span class="nav-back">← <a href="/applications/advanced-combustion-fixed">Back to Advanced Combustion</a></span>
            <span class="nav-back">↑ <a href="/applications">All 8 Application Verticals</a></span>
          </div>
        </div>
        <div>
          <div class="bio-proof">
            <div class="bio-proof-eyebrow">★ EU hydrogen transition — ignition impact</div>
            <div class="bio-proof-title">What 20% H₂ grid blending means for appliances</div>
            <div class="bio-proof-grid"><div class="bio-proof-stat"><div class="bn">20%</div><div class="bl">EU H₂ blending target by 2030</div></div><div class="bio-proof-stat"><div class="bn">300M+</div><div class="bl">Existing EU appliances requiring H₂ compatibility</div></div><div class="bio-proof-stat"><div class="bn">Invisible</div><div class="bl">H₂ flame — missed ignition is safety event</div></div><div class="bio-proof-stat"><div class="bn">H2Ready</div><div class="bl">Certification program active</div></div></div>
          </div>
        </div>
      </div>
    </section><section class="cert-strip">
      <div class="wrap">
        <div class="cs-label">Certifications &amp; Standards</div>
        <div class="cs-badges"><span>ISO 9001</span><span>IATF 16949</span><span>CE</span><span>RoHS · REACH</span><span>UL Recognized</span></div>
      </div>
    </section><section class="mkt-band"><div class="wrap"><div class="mkt-grid"><div class="mkt-item"><div class="mkt-num">20%</div><div class="mkt-label">Hydrogen blending target in EU gas grids by 2030 — all existing appliance igniters must be compatible</div></div><div class="mkt-item"><div class="mkt-num">300M+</div><div class="mkt-label">Existing gas appliances in Europe requiring H₂-compatible ignition as grid blending increases</div></div><div class="mkt-item"><div class="mkt-num">50–100×</div><div class="mkt-label">Wider flammability range of H₂ vs natural gas — demanding faster, more reliable ignition</div></div><div class="mkt-item"><div class="mkt-num">2026</div><div class="mkt-label">Target date for CN-300 H2Ready certification data publication</div></div></div></div></section><section class="s" style="padding:72px 0;">
      <div class="wrap">
        <div class="sec-num">01 · The Application</div>
        <h2>Hydrogen demands <span class="accent">faster, more reliable ignition</span>.</h2>
        <div class="depth-grid">
          <div>
            <div class="depth-img" style="background:linear-gradient(145deg,#05101a 0%,#0a1e30 40%,#0d2640 100%);">
              <div class="di-icon">⚗️</div>
              <div class="di-label">CN-300 igniter in hydrogen burner test rig</div>
              <div class="di-replace">Replace with product photo</div>
            </div>
          </div>
          <div>
            <p style="font-size:16px;color:var(--ink-soft);line-height:1.65;margin-bottom:24px;">Hydrogen has different combustion properties to natural gas: invisible flame, wider flammability range, 3× faster flame speed, and lower minimum ignition energy. These properties demand igniters that fire reliably on every attempt — missed ignitions allow H₂ to accumulate, creating explosion risk.</p>
             <p style="font-size:16px;color:var(--ink-soft);line-height:1.65;">As EU gas grids increase H₂ blending from 0% to 20% (and potentially 100% in some grids by 2035), all gas appliances must verify igniter compatibility. First-attempt ignition reliability is the critical parameter.</p>
            <div class="tech-req-label" style="margin-top:24px;">Technical Requirements</div>
            <div class="tech-row"><span class="tech-k">Ignition reliability</span><span class="tech-v">First-attempt mandatory — safety requirement</span></div><div class="tech-row"><span class="tech-k">Heat-up time</span><span class="tech-v">&lt;3 seconds to 1,100°C</span></div><div class="tech-row"><span class="tech-k">H₂ compatibility</span><span class="tech-v">100% H₂ and blended gas rated</span></div><div class="tech-row"><span class="tech-k">Cycle life</span><span class="tech-v">100,000+ at 20,000 cycles/yr</span></div>
          </div>
        </div>
      </div>
    </section><section class="comp-band" style="background:var(--bg-soft);">
      <div class="wrap">
        <div class="sec-num">02 · Igniter Technology Selection</div>
        <h2>Hot surface vs spark for</h2>
        <p class="sec-lead">hydrogen ignition</p>
        <div class="comp-grid">
          <div class="comp-card winner">
            <span class="comp-badge win">★ Recommended</span>
            <div class="comp-name">Si₃N₄ Hot Surface (CN-300)</div>
            <div class="comp-tagline">100K+ cycles · 3 sec · highest first-attempt reliability</div>
            <ul class="comp-list"><li><span class="ci">✓</span><div><b>Large surface ignition zone</b> — vs point spark, ensures H₂ ignition across wider stoichiometric range</div></li><li><span class="ci">✓</span><div><b>3-second heat-up</b> — fires before H₂ accumulates in combustion chamber</div></li><li><span class="ci">✓</span><div><b>100,000+ cycle life</b> — reliable across seasonal on/off cycling</div></li><li><span class="ci">✓</span><div><b>H₂-compatible material chemistry</b> — Si₃N₄ inert to H₂ at operating temperature</div></li><li><span class="ci">✓</span><div><b>H2Ready certification</b> in progress — data available Q3 2026</div></li></ul>
            <div class="comp-cta-row"><a class="btn-primary" href="/products/silicon-nitride-igniters-final">View Full Specs →</a></div>
          </div>
          <div class="comp-card">
            <span class="comp-badge alt">Also Available</span>
            <div class="comp-name">Piezoelectric Spark Ignition</div>
            <div class="comp-tagline">Unlimited mechanical life · lower reliability</div>
            <ul class="comp-list"><li><span class="ci">→</span><div>No electrical heater required — simpler circuit</div></li><li><span class="ci">→</span><div>Works on natural gas reliably</div></li><li><span class="ci" style="color:var(--orange);">⚠</span><div><b>Lower first-attempt reliability on H₂</b> — point spark vs wide ignition surface</div></li><li><span class="ci" style="color:var(--orange);">⚠</span><div><b>H₂ leak risk</b> — lower reliability means more missed ignitions and H₂ accumulation</div></li></ul>
          </div>
        </div>
      </div>
    </section><section class="casecta">
      <div class="wrap">
        <div>
          <div class="casecta-eyebrow">H₂-Ready appliance certification support</div>
          <h2>Ready for Europe's<br/><span style="color:var(--gold);">hydrogen transition.</span></h2>
          <div class="casecta-ctas">
            <a class="casecta-primary" href="#quote">Request Engineering Data →</a>
            <a class="casecta-secondary" href="/contact">Talk to an Engineer</a>
          </div>
        </div>
        <div class="casecta-side">
          <div class="casecta-side-tag">H₂ program status</div>
          <ul class="casecta-side-list"><li><b>20% H₂/CH₄ blend:</b> compatibility testing completed</li><li><b>100% H₂:</b> combustion testing in progress</li><li><b>H2Ready cert:</b> target data publication Q3 2026</li><li><b>OEM engineering:</b> ignition timing review and control board support</li><li><b>NDA standard:</b> before any H₂ program discussion</li></ul>
        </div>
      </div>
    </section><section class="failure-band">
      <div class="wrap">
        <div class="sec-num" style="color:var(--gold);">03 · H₂ Ignition Failure Risks</div>
        <h2 style="color:#fff;">Why reliable ignition is safety-critical in H₂.</h2>
        <p style="color:rgba(255,255,255,0.75);font-size:16px;max-width:680px;margin-top:10px;line-height:1.6;">In natural gas, a missed ignition is a nuisance. In hydrogen, it is a safety incident. Understanding the failure modes is essential for H₂ appliance design.</p>
        <div class="failure-grid"><div class="fail-card worst"><div class="fail-num">01</div><div class="fail-name">Missed ignition — H₂ accumulation</div><div class="fail-desc">In hydrogen appliances, H₂ accumulation in the combustion chamber before the next ignition attempt creates an explosive mixture. First-attempt ignition reliability is a safety requirement, not a performance preference.</div><div class="fail-solution">Si₃N₄ fix → large surface ignition zone provides reliable first-attempt ignition across H₂/air stoichiometric range.</div></div><div class="fail-card"><div class="fail-num">02</div><div class="fail-name">Faster flame speed — timing sensitivity</div><div class="fail-desc">H₂ flame speed is 10× faster than natural gas. Hot surface igniters must reach ignition temperature before the gas valve opens — control board sequencing must be verified for H₂ operation.</div><div class="fail-solution">Si₃N₄ fix → 3-second heat-up ensures ignition temperature before gas valve opens with standard control board timing.</div></div><div class="fail-card"><div class="fail-num">03</div><div class="fail-name">Higher NOx at hot surfaces</div><div class="fail-desc">H₂ combustion at high temperatures produces NOx at higher rates than methane. Uneven igniter surface temperature creates local NOx hotspots.</div><div class="fail-solution">Si₃N₄ fix → uniform surface temperature from high thermal conductivity reduces local NOx formation at the ignition point.</div></div></div>
      </div>
    </section><section class="s" style="background:var(--bg-soft);">
      <div class="wrap">
        <div class="sec-num">04 · Engineering FAQ</div>
        <h2>Questions from hydrogen appliance engineers.</h2>
        <div class="faq-grid"><div class="faq-item"><div class="faq-q">Is CN-300 compatible with 20% H₂ blended gas?</div><div class="faq-a">Compatibility testing with 20% H₂/80% CH₄ blend completed — no ignition performance degradation. Full H2Ready certification data targeted Q3 2026.</div></div><div class="faq-item"><div class="faq-q">What about 100% H₂ operation?</div><div class="faq-a">100% H₂ testing in progress. Si₃N₄ element is chemically compatible with H₂ at operating temperatures. Ignition timing control board parameters require adjustment — we provide application engineering support.</div></div><div class="faq-item"><div class="faq-q">Do you provide H₂ appliance conversion engineering support?</div><div class="faq-a">Yes — ignition system design review, H₂/CH₄ blend compatibility data, control board timing recommendations, and first-article testing for OEMs undergoing H₂Ready certification.</div></div><div class="faq-item"><div class="faq-q">When will H2Ready certification data be available?</div><div class="faq-a">Target Q3 2026. Contact us for the engineering data pack currently available for appliance design review.</div></div></div>
      </div>
    </section><section class="s quote-sec" id="quote">
      <div class="wrap">
        <div>
          <div class="sec-num">Request a Quote</div>
          <h2>Ready to spec <span class="accent">your H₂-compatible igniter</span>?</h2>
          <p class="sec-lead" style="color:rgba(255,255,255,0.8);">Tell us your application, current igniter, and development timeline. We send H₂ compatibility data and a quote within 24h.</p>
          <ul class="quote-why"><li><b>20% H₂ blend compatibility</b> — test data available now.</li><li><b>H2Ready certification</b> — data targeted Q3 2026.</li><li><b>Engineering support</b> for H₂ appliance OEM programs.</li><li><b>NDA standard</b> before any H₂ program discussion.</li></ul>
        </div>
        <div class="quote-form-card">
          <div class="qquote-tag">Free Samples · Reply in 24h</div>
          <div class="qquote-title">Request H₂ igniter compatibility data</div>
          <div class="qquote-note">Describe your application, H₂ blend ratio, and development timeline.</div>
          <div class="qq-field"><label>Name <span style="color:var(--orange)">*</span></label><input type="text" placeholder="Your name" required /></div>
          <div class="qq-field"><label>Email <span style="color:var(--orange)">*</span></label><input type="email" placeholder="you@company.com" required /></div>
          <div class="qq-field"><label>Phone <span style="color:var(--ink-faint);font-weight:400;letter-spacing:0">(optional)</span></label><input type="tel" placeholder="+1 / +39 / +86 ..." /></div>
          <div class="qq-field"><label>Application / Part Number <span style="color:var(--orange)">*</span></label><textarea placeholder="Describe your application, current part number, annual volume, or upload a drawing" required></textarea></div>
          <button class="qq-submit" onclick="alert('Demo form');return false;">Request Samples →</button>
          <div class="qq-trust">🔒 Reply within 24h · NDA available · No spam</div>
        </div>
      </div>
    </section><section class="s">
      <div class="wrap">
        <div class="sec-num">Related</div>
        <h2>Explore <span class="accent">further</span>.</h2>
        <div class="rel-grid"><a href="/products/silicon-nitride-igniters-final" class="rel-card"><div class="rel-eyebrow">Product</div><div class="rel-title">CN-300 Si₃N₄ Igniter</div><div class="rel-desc">Full specifications and H₂ compatibility data.</div><div class="rel-link">View →</div></a><a href="/applications/sub-commercial-boilers" class="rel-card"><div class="rel-eyebrow">Application</div><div class="rel-title">Commercial & Industrial Boilers</div><div class="rel-desc">H₂-blended boiler ignition at commercial scale.</div><div class="rel-link">View →</div></a><a href="/applications/app-clean-energy" class="rel-card"><div class="rel-eyebrow">Application</div><div class="rel-title">Clean Energy & Battery</div><div class="rel-desc">H₂ fuel cells, SOFC, and clean energy ceramic applications.</div><div class="rel-link">View →</div></a></div>
      </div>
    </section>
  </div>
</template>

<style>
:root {
    --navy: #0A2647; --navy-2: #103A6B; --navy-3: #1B4D8A;
    --bg: #FFFFFF; --bg-soft: #F1F5FA; --bg-soft-2: #E6EEF7;
    --ink: #14233A; --ink-soft: #4A5A70; --ink-faint: #8794A6;
    --orange: #F26419; --orange-deep: #D8530F;
    --gold: #F0A500; --gold-soft: #FFD466;
    --line: #DCE5EF; --line-strong: #C2D0E0;
    --sans: 'Barlow', sans-serif;
    --cond: 'Barlow Condensed', sans-serif;
    --mono: 'IBM Plex Mono', monospace;
  }
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body { background: var(--bg); color: var(--ink); font-family: var(--sans); line-height: 1.6; -webkit-font-smoothing: antialiased; }
  a { color: inherit; text-decoration: none; cursor: pointer; }
  .wrap { max-width: 1260px; margin: 0 auto; padding: 0 40px; }

  /* CHROME */
  .topbar { background: var(--navy); color: #C5D4E6; font-family: var(--mono); font-size: 12px; }
  .topbar .wrap { display: flex; justify-content: space-between; align-items: center; height: 38px; }
  .topbar .left { display: flex; gap: 26px; }
  .topbar .left span { display: flex; align-items: center; gap: 7px; }
  .dot { width: 6px; height: 6px; border-radius: 50%; background: var(--gold); }
  .topbar .right { letter-spacing: 0.05em; color: var(--gold); }
  nav { position: sticky; top: 0; z-index: 100; background: #fff; border-bottom: 3px solid var(--navy); box-shadow: 0 2px 12px rgba(10,38,71,0.06); }
  nav .wrap { display: flex; align-items: center; justify-content: space-between; height: 78px; }
  .logo { display: flex; align-items: center; gap: 12px; }
  .logo-mark { width: 42px; height: 42px; }
  .logo-text { font-weight: 800; font-size: 24px; letter-spacing: -0.02em; color: var(--navy); }
  .logo-text b { color: var(--orange); }
  .logo-text small { display: block; font-family: var(--mono); font-size: 9px; letter-spacing: 0.16em; color: var(--ink-faint); font-weight: 400; margin-top: -2px; }
  .nav-links { display: flex; gap: 32px; align-items: center; }
  .nav-links a { font-size: 14.5px; font-weight: 700; color: var(--navy); padding: 6px 0; letter-spacing: 0.2px; }
  .nav-links a.active { color: var(--orange); border-bottom: 2px solid var(--orange); }
  .nav-links a:hover { color: var(--orange); }
  .nav-cta { background: var(--orange); color: #fff !important; padding: 12px 24px; font-weight: 800; font-size: 13.5px; letter-spacing: 0.5px; transition: background 0.15s; }
  .nav-cta:hover { background: var(--orange-deep); }

  .breadcrumb { background: var(--bg-soft); padding: 14px 0; border-bottom: 1px solid var(--line); }
  .breadcrumb .wrap { display: flex; gap: 10px; align-items: center; font-family: var(--mono); font-size: 12px; color: var(--ink-soft); letter-spacing: 0.05em; }
  .breadcrumb a { color: var(--navy); font-weight: 600; }
  .breadcrumb a:hover { color: var(--orange); }
  .breadcrumb .sep { color: var(--ink-faint); }

  /* HERO */
  .page-hero { background: linear-gradient(135deg, var(--navy) 0%, var(--navy-2) 100%); color: #fff; padding: 70px 0 90px; position: relative; overflow: hidden; }
  .page-hero-stripe { position: absolute; bottom: 0; left: 0; right: 0; height: 4px; background: linear-gradient(90deg, var(--orange) 0%, var(--gold) 50%, var(--orange) 100%); }
  .page-hero .wrap { display: grid; grid-template-columns: 1.4fr 1fr; gap: 60px; align-items: end; position: relative; }
  .ph-eyebrow { display: inline-block; background: var(--orange); color: #fff; font-family: var(--mono); font-size: 11px; letter-spacing: 0.15em; padding: 8px 14px; margin-bottom: 24px; text-transform: uppercase; font-weight: 500; }
  .formula-display { font-family: var(--cond); font-size: 92px; font-weight: 800; line-height: 0.95; letter-spacing: -0.025em; margin-bottom: 12px; }
  .formula-display sub { font-size: 56px; font-weight: 600; }
  .page-hero h1 { font-family: var(--cond); font-size: 52px; line-height: 1.05; font-weight: 700; letter-spacing: -0.02em; margin-bottom: 22px; color: var(--gold); }
  .ph-sub { font-size: 18px; color: rgba(255,255,255,0.85); line-height: 1.55; max-width: 620px; margin-bottom: 32px; }
  .ph-sub b { color: var(--gold-soft); font-weight: 700; }
  .ph-quickstats { display: flex; gap: 40px; }
  .ph-stat .num { font-family: var(--cond); font-size: 44px; font-weight: 800; line-height: 1; color: var(--gold); }
  .ph-stat .lbl { font-family: var(--mono); font-size: 10.5px; letter-spacing: 0.13em; color: rgba(255,255,255,0.7); margin-top: 6px; text-transform: uppercase; }
  .ph-side { padding-bottom: 14px; }
  .grade-cards { display: flex; flex-direction: column; gap: 14px; }
  .grade-card { background: rgba(255,255,255,0.06); border-left: 3px solid var(--orange); padding: 18px 22px; transition: background 0.15s, border-color 0.15s; cursor: pointer; }
  .grade-card:hover { background: rgba(255,255,255,0.1); border-left-color: var(--gold); }
  .grade-card .grade-label { font-family: var(--mono); font-size: 10px; letter-spacing: 0.15em; color: var(--gold); text-transform: uppercase; font-weight: 600; margin-bottom: 4px; }
  .grade-card .grade-title { font-family: var(--cond); font-size: 22px; font-weight: 700; color: #fff; letter-spacing: -0.005em; }
  .grade-card .grade-use { font-family: var(--mono); font-size: 10.5px; color: rgba(255,255,255,0.6); margin-top: 4px; letter-spacing: 0.05em; }

  /* CERT STRIP */
  .cert-strip { background: var(--bg-soft); padding: 24px 0; border-bottom: 1px solid var(--line); }
  .cert-strip .wrap { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 22px; }
  .cs-label { font-family: var(--mono); font-size: 11px; letter-spacing: 0.15em; color: var(--navy); text-transform: uppercase; font-weight: 500; }
  .cs-badges { display: flex; gap: 28px; flex-wrap: wrap; }
  .cs-badges span { font-family: var(--cond); font-size: 16px; font-weight: 700; color: var(--navy); display: flex; align-items: center; gap: 7px; letter-spacing: 0.3px; text-transform: uppercase; }
  .cs-badges span::before { content: '✓'; color: var(--orange); font-weight: 700; }

  /* SECTION */
  section.s { padding: 80px 0; }
  .sec-num { font-family: var(--mono); font-size: 11.5px; letter-spacing: 0.2em; color: var(--orange); text-transform: uppercase; font-weight: 500; margin-bottom: 14px; }
  h2 { font-family: var(--cond); font-size: 48px; font-weight: 800; line-height: 1.05; letter-spacing: -0.02em; color: var(--ink); margin-bottom: 16px; }
  h2 .accent { color: var(--orange); }
  .sec-lead { color: var(--ink-soft); font-size: 16.5px; max-width: 720px; line-height: 1.6; margin-bottom: 44px; }
  .sec-lead b { color: var(--navy); font-weight: 700; }

  /* PROPERTIES TABLE */
  .props-wrap { background: var(--navy); color: #fff; padding: 8px; overflow-x: auto; }
  table.props { width: 100%; border-collapse: collapse; }
  table.props th, table.props td { padding: 16px 18px; text-align: left; font-size: 14px; border-bottom: 1px solid rgba(255,255,255,0.08); }
  table.props thead th { font-family: var(--mono); font-size: 11px; letter-spacing: 0.13em; color: var(--gold); text-transform: uppercase; font-weight: 500; padding-top: 22px; padding-bottom: 14px; border-bottom: 2px solid rgba(255,255,255,0.15); background: var(--navy-2); }
  table.props tbody th { font-family: var(--cond); font-size: 18px; font-weight: 700; color: #fff; }
  table.props tbody td { color: rgba(255,255,255,0.85); font-family: var(--cond); font-size: 18px; font-weight: 700; }
  table.props tbody td.unit { font-family: var(--mono); font-size: 11px; color: rgba(255,255,255,0.55); font-weight: 400; letter-spacing: 0.05em; }
  table.props tbody tr:hover { background: rgba(255,255,255,0.03); }
  .props-legend { font-family: var(--mono); font-size: 11px; color: var(--ink-soft); margin-top: 16px; letter-spacing: 0.05em; }

  /* APPLICATIONS GRID */
  .app-uses { display: grid; grid-template-columns: repeat(3, 1fr); gap: 22px; }
  .app-use { background: #fff; border: 1px solid var(--line); padding: 28px; position: relative; display: flex; flex-direction: column; gap: 14px; transition: border-color 0.15s, transform 0.15s; }
  .app-use:hover { border-color: var(--orange); transform: translateY(-3px); box-shadow: 0 20px 40px -20px rgba(10,38,71,0.15); }
  .app-use-bar { position: absolute; top: -1px; left: 28px; width: 40px; height: 3px; background: var(--orange); }
  .app-use-icon { font-size: 32px; line-height: 1; margin-top: 8px; }
  .app-use-title { font-family: var(--cond); font-size: 22px; font-weight: 700; color: var(--navy); line-height: 1.15; letter-spacing: -0.005em; }
  .app-use-desc { font-size: 13.5px; color: var(--ink-soft); line-height: 1.55; flex: 1; }
  .app-use-grade { font-family: var(--mono); font-size: 11px; color: var(--orange); letter-spacing: 0.05em; padding-top: 12px; border-top: 1px solid var(--line); font-weight: 600; }
  .app-use-grade b { color: var(--navy); }

  /* COMPARISON CARDS */
  .vs-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 28px; }
  .vs-card { background: #fff; border: 1px solid var(--line); padding: 32px; }
  .vs-card.win { border-color: var(--orange); border-width: 2px; background: linear-gradient(180deg, #fff 0%, var(--bg-soft) 100%); }
  .vs-card-head { display: flex; justify-content: space-between; align-items: baseline; margin-bottom: 16px; padding-bottom: 16px; border-bottom: 1px solid var(--line); }
  .vs-card-head h3 { font-family: var(--cond); font-size: 26px; font-weight: 800; color: var(--navy); letter-spacing: -0.01em; }
  .vs-tag { font-family: var(--mono); font-size: 10px; letter-spacing: 0.15em; color: var(--orange); text-transform: uppercase; font-weight: 700; background: rgba(242,100,25,0.08); padding: 4px 10px; }
  .vs-tag.alumina-tag { background: var(--orange); color: #fff; }
  .vs-points { list-style: none; display: flex; flex-direction: column; gap: 10px; }
  .vs-points li { display: flex; gap: 10px; align-items: baseline; font-size: 14px; color: var(--ink-soft); line-height: 1.5; }
  .vs-points li::before { content: '→'; color: var(--orange); flex-shrink: 0; font-weight: 700; }
  .vs-points li b { color: var(--navy); font-weight: 700; }

  /* CTA BAND */
  .casecta { background: var(--bg-soft); padding: 60px 0; border-top: 1px solid var(--line); border-bottom: 1px solid var(--line); }
  .casecta .wrap { display: grid; grid-template-columns: 1.3fr 1fr; gap: 50px; align-items: center; }
  .casecta-eyebrow { font-family: var(--mono); font-size: 11px; letter-spacing: 0.18em; color: var(--orange); text-transform: uppercase; font-weight: 600; margin-bottom: 14px; }
  .casecta h2 { font-size: 38px; line-height: 1.08; margin-bottom: 18px; }
  .casecta p { font-size: 16px; color: var(--ink-soft); margin-bottom: 26px; line-height: 1.6; }
  .casecta-ctas { display: flex; gap: 12px; flex-wrap: wrap; }
  .btn-primary { background: var(--orange); color: #fff; padding: 16px 28px; font-weight: 800; font-size: 14px; letter-spacing: 0.2px; border: none; cursor: pointer; transition: background 0.15s; display: inline-flex; align-items: center; gap: 8px; }
  .btn-primary:hover { background: var(--orange-deep); }
  .btn-ghost { background: transparent; color: var(--navy); padding: 16px 26px; font-weight: 700; font-size: 14px; letter-spacing: 0.2px; border: 2px solid var(--navy); cursor: pointer; display: inline-flex; align-items: center; gap: 8px; }
  .btn-ghost:hover { border-color: var(--orange); color: var(--orange); }
  .casecta-side { background: #fff; border: 1px solid var(--line); padding: 28px; position: relative; }
  .casecta-side::before { content: ''; position: absolute; top: -1px; left: 0; width: 70px; height: 4px; background: var(--orange); }
  .casecta-side-tag { font-family: var(--mono); font-size: 10px; letter-spacing: 0.15em; color: var(--orange); text-transform: uppercase; margin-bottom: 12px; font-weight: 600; }
  .casecta-side-title { font-family: var(--cond); font-size: 22px; font-weight: 700; color: var(--navy); margin-bottom: 16px; line-height: 1.2; }
  .casecta-side-list { list-style: none; display: flex; flex-direction: column; gap: 10px; }
  .casecta-side-list li { font-size: 13.5px; color: var(--ink-soft); display: flex; gap: 10px; align-items: baseline; padding-bottom: 10px; border-bottom: 1px dashed var(--line); }
  .casecta-side-list li:last-child { border-bottom: none; padding-bottom: 0; }
  .casecta-side-list li b { font-family: var(--cond); font-size: 15px; font-weight: 700; color: var(--navy); min-width: 70px; }

  /* FAQ */
  .faq-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 18px 50px; }
  .faq-item { padding: 18px 0; border-bottom: 1px solid var(--line); }
  .faq-item:last-child, .faq-item:nth-last-child(2) { border-bottom: none; }
  .faq-q { font-family: var(--cond); font-size: 19px; font-weight: 700; color: var(--navy); margin-bottom: 10px; letter-spacing: -0.005em; line-height: 1.25; }
  .faq-q::before { content: 'Q'; display: inline-block; width: 24px; height: 24px; background: var(--orange); color: #fff; font-family: var(--mono); font-size: 12px; line-height: 24px; text-align: center; margin-right: 10px; vertical-align: middle; font-weight: 700; }
  .faq-a { font-size: 14px; color: var(--ink-soft); line-height: 1.6; padding-left: 34px; }

  footer { background: var(--navy); color: #C5D4E6; padding: 70px 0 30px; }
  .foot-grid { display: grid; grid-template-columns: 1.5fr 1fr 1fr 1fr 1.2fr; gap: 40px; margin-bottom: 50px; }
  .foot-col h5 { font-family: var(--cond); font-size: 16px; font-weight: 700; color: #fff; letter-spacing: 0.05em; margin-bottom: 16px; text-transform: uppercase; }
  .foot-col a { display: block; font-size: 13.5px; color: rgba(255,255,255,0.65); margin-bottom: 9px; transition: color 0.15s; }
  .foot-col a:hover { color: var(--gold); }
  .fb-right p { font-size: 13.5px; color: rgba(255,255,255,0.65); margin-bottom: 10px; line-height: 1.55; }
  .fb-right p b { color: #fff; font-weight: 600; }
  .foot-bottom { padding-top: 24px; border-top: 1px solid rgba(255,255,255,0.1); font-family: var(--mono); font-size: 11.5px; color: rgba(255,255,255,0.5); display: flex; justify-content: space-between; flex-wrap: wrap; gap: 14px; }
  .foot-bottom span { display: flex; gap: 22px; }

  @media (max-width: 1024px) {
    .page-hero .wrap { grid-template-columns: 1fr; gap: 40px; }
    .app-uses { grid-template-columns: 1fr 1fr; }
    .vs-grid { grid-template-columns: 1fr; }
    .casecta .wrap { grid-template-columns: 1fr; }
    .faq-grid { grid-template-columns: 1fr; gap: 0; }
    .foot-grid { grid-template-columns: 1fr 1fr; }
  }
  @media (max-width: 640px) {
    .wrap { padding: 0 20px; }
    .formula-display { font-size: 72px; }
    .page-hero h1 { font-size: 38px; }
    .app-uses { grid-template-columns: 1fr; }
    .topbar .left, .nav-links { display: none; }
  }

  /* ============================================================
     PRODUCT-PAGE TEMPLATE (distinct from material-page template)
     - hero carries an inline Quick-Quote form (core CTR)
     - sub-product catalog lineup replaces the spec datasheet table
     - benefit checklist replaces the vs-comparison grid
     ============================================================ */

  /* PRODUCT HERO (quote-led) */
  .prod-hero { background: linear-gradient(135deg, var(--navy) 0%, var(--navy-2) 100%); color: #fff; padding: 64px 0 78px; position: relative; overflow: hidden; }
  .prod-hero-stripe { position: absolute; bottom: 0; left: 0; right: 0; height: 4px; background: linear-gradient(90deg, var(--orange) 0%, var(--gold) 50%, var(--orange) 100%); }
  .prod-hero .wrap { display: grid; grid-template-columns: 1.15fr 0.85fr; gap: 56px; align-items: start; position: relative; }
  .ph-eyebrow { display: inline-block; background: var(--orange); color: #fff; font-family: var(--mono); font-size: 11px; letter-spacing: 0.15em; padding: 8px 14px; margin-bottom: 22px; text-transform: uppercase; font-weight: 500; }
  .prod-hero h1 { font-family: var(--cond); font-size: 56px; line-height: 1.02; font-weight: 800; letter-spacing: -0.02em; margin-bottom: 20px; color: #fff; }
  .prod-hero h1 .hl { color: var(--gold); }
  .prod-hero .ph-sub { font-size: 17px; color: rgba(255,255,255,0.85); line-height: 1.55; max-width: 600px; margin-bottom: 30px; }
  .prod-hero .ph-sub b { color: var(--gold-soft); font-weight: 700; }
  .ph-statbar { display: flex; gap: 36px; flex-wrap: wrap; padding-top: 26px; border-top: 1px solid rgba(255,255,255,0.15); }
  .ph-statbar .num { font-family: var(--cond); font-size: 38px; font-weight: 800; line-height: 1; color: var(--gold); }
  .ph-statbar .lbl { font-family: var(--mono); font-size: 10px; letter-spacing: 0.12em; color: rgba(255,255,255,0.7); margin-top: 6px; text-transform: uppercase; }

  /* QUICK-QUOTE FORM CARD (the conversion engine) */
  .qquote { background: #fff; padding: 30px 30px 32px; position: relative; box-shadow: 0 30px 60px -25px rgba(0,0,0,0.5); }
  .qquote::before { content: ''; position: absolute; top: 0; left: 0; right: 0; height: 4px; background: var(--orange); }
  .qquote-tag { font-family: var(--mono); font-size: 10.5px; letter-spacing: 0.16em; color: var(--orange); text-transform: uppercase; font-weight: 600; margin-bottom: 8px; }
  .qquote-title { font-family: var(--cond); font-size: 27px; font-weight: 800; color: var(--navy); line-height: 1.08; letter-spacing: -0.01em; margin-bottom: 6px; }
  .qquote-note { font-size: 12.5px; color: var(--ink-soft); line-height: 1.5; margin-bottom: 20px; }
  .qq-field { margin-bottom: 13px; }
  .qq-field label { display: block; font-family: var(--mono); font-size: 10px; letter-spacing: 0.1em; text-transform: uppercase; color: var(--ink-faint); font-weight: 500; margin-bottom: 5px; }
  .qq-field input, .qq-field select, .qq-field textarea { width: 100%; font-family: var(--sans); font-size: 14px; color: var(--ink); border: 1px solid var(--line-strong); padding: 11px 13px; background: var(--bg-soft); transition: border-color 0.15s, background 0.15s; }
  .qq-field input:focus, .qq-field select:focus, .qq-field textarea:focus { outline: none; border-color: var(--orange); background: #fff; }
  .qq-field textarea { resize: vertical; min-height: 64px; }
  .qq-row { display: grid; grid-template-columns: 1fr 1fr; gap: 13px; }
  .qq-submit { width: 100%; background: var(--orange); color: #fff; border: none; padding: 15px; font-family: var(--sans); font-weight: 800; font-size: 14.5px; letter-spacing: 0.3px; cursor: pointer; transition: background 0.15s; margin-top: 6px; }
  .qq-submit:hover { background: var(--orange-deep); }
  .qq-trust { font-family: var(--mono); font-size: 10.5px; color: var(--ink-faint); text-align: center; margin-top: 12px; letter-spacing: 0.03em; }

  /* SUB-PRODUCT LINEUP (catalog — replaces datasheet table) */
  .lineup { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; }
  .lineup-card { background: #fff; border: 1px solid var(--line); padding: 30px; position: relative; display: flex; flex-direction: column; transition: border-color 0.15s, transform 0.15s, box-shadow 0.15s; }
  .lineup-card:hover { border-color: var(--orange); transform: translateY(-3px); box-shadow: 0 24px 46px -24px rgba(10,38,71,0.18); }
  .lineup-card.flagship { border-color: var(--orange); border-width: 2px; background: linear-gradient(180deg, #fff 0%, var(--bg-soft) 100%); }
  .lineup-badge { position: absolute; top: 0; right: 0; background: var(--orange); color: #fff; font-family: var(--mono); font-size: 9.5px; letter-spacing: 0.14em; text-transform: uppercase; font-weight: 600; padding: 6px 12px; }
  .lineup-head { display: flex; align-items: flex-start; gap: 14px; margin-bottom: 14px; }
  .lineup-icon { font-size: 30px; line-height: 1; flex-shrink: 0; margin-top: 2px; }
  .lineup-name { font-family: var(--cond); font-size: 25px; font-weight: 800; color: var(--navy); line-height: 1.08; letter-spacing: -0.01em; }
  .lineup-cn { font-family: var(--sans); font-size: 12.5px; color: var(--ink-faint); font-weight: 600; margin-top: 2px; }
  .lineup-desc { font-size: 13.5px; color: var(--ink-soft); line-height: 1.55; margin-bottom: 16px; flex: 1; }
  .lineup-specs { display: flex; flex-wrap: wrap; gap: 7px; margin-bottom: 18px; }
  .spec-chip { font-family: var(--mono); font-size: 10.5px; letter-spacing: 0.03em; color: var(--navy); background: var(--bg-soft-2); padding: 5px 9px; font-weight: 500; }
  .spec-chip b { color: var(--orange); font-weight: 600; }
  .lineup-foot { display: flex; justify-content: space-between; align-items: center; padding-top: 15px; border-top: 1px solid var(--line); }
  .lineup-mat { font-family: var(--mono); font-size: 11px; color: var(--ink-soft); letter-spacing: 0.04em; }
  .lineup-mat b { color: var(--navy); font-weight: 600; }
  .lineup-mat a { color: var(--orange); font-weight: 600; }
  .lineup-link { font-family: var(--sans); font-size: 13px; font-weight: 800; color: var(--orange); letter-spacing: 0.2px; white-space: nowrap; }
  .lineup-link:hover { color: var(--orange-deep); }

  /* BENEFIT CHECKLIST (replaces vs-comparison grid) */
  .benefits { display: grid; grid-template-columns: 1fr 1fr; gap: 22px 56px; }
  .benefit { display: flex; gap: 16px; align-items: flex-start; padding-bottom: 20px; border-bottom: 1px solid var(--line); }
  .benefit-ico { flex-shrink: 0; width: 30px; height: 30px; background: var(--orange); color: #fff; font-weight: 800; font-size: 15px; display: flex; align-items: center; justify-content: center; margin-top: 2px; }
  .benefit-ico::before { content: '✓'; }
  .benefit-body h4 { font-family: var(--cond); font-size: 20px; font-weight: 700; color: var(--navy); letter-spacing: -0.005em; margin-bottom: 5px; line-height: 1.15; }
  .benefit-body p { font-size: 13.5px; color: var(--ink-soft); line-height: 1.55; }

  @media (max-width: 1024px) {
    .prod-hero .wrap { grid-template-columns: 1fr; gap: 40px; }
    .lineup { grid-template-columns: 1fr; }
    .benefits { grid-template-columns: 1fr; gap: 0; }
  }
  @media (max-width: 640px) {
    .prod-hero h1 { font-size: 38px; }
    .qq-row { grid-template-columns: 1fr; }
    .lineup-foot { flex-direction: column; align-items: flex-start; gap: 10px; }
  }

  /* ============================================================
     PRODUCT DETAIL PAGE (CN-300 Si3N4) — extends product template
     ============================================================ */

  /* HERO SPOTLIGHT CARD (right column on detail hero) */
  .spotlight { background: rgba(255,255,255,0.06); border: 1px solid rgba(255,255,255,0.14); padding: 26px 28px; }
  .spotlight-tag { font-family: var(--mono); font-size: 10.5px; letter-spacing: 0.16em; color: var(--gold); text-transform: uppercase; font-weight: 600; margin-bottom: 18px; }
  .spotlight-row { display: flex; justify-content: space-between; align-items: baseline; padding: 13px 0; border-bottom: 1px solid rgba(255,255,255,0.1); }
  .spotlight-row:last-child { border-bottom: none; }
  .spotlight-row .k { font-family: var(--mono); font-size: 11px; color: rgba(255,255,255,0.6); letter-spacing: 0.05em; }
  .spotlight-row .v { font-family: var(--cond); font-size: 20px; font-weight: 700; color: #fff; }
  .spotlight-row .v b { color: var(--gold); }
  .hero-ctas { display: flex; gap: 12px; flex-wrap: wrap; margin-top: 30px; }

  /* DOWNLOAD CARDS */
  .dl-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; }
  .dl-card { background: #fff; border: 1px solid var(--line); padding: 26px; display: flex; flex-direction: column; gap: 12px; transition: border-color 0.15s, transform 0.15s; position: relative; }
  .dl-card:hover { border-color: var(--orange); transform: translateY(-3px); box-shadow: 0 20px 40px -22px rgba(10,38,71,0.18); }
  .dl-icon { font-size: 30px; line-height: 1; }
  .dl-name { font-family: var(--cond); font-size: 21px; font-weight: 700; color: var(--navy); line-height: 1.12; }
  .dl-meta { font-family: var(--mono); font-size: 11px; color: var(--ink-faint); letter-spacing: 0.05em; }
  .dl-btn { margin-top: auto; font-family: var(--sans); font-size: 13px; font-weight: 800; color: var(--orange); letter-spacing: 0.2px; padding-top: 10px; border-top: 1px solid var(--line); }
  .dl-btn:hover { color: var(--orange-deep); }

  /* CERTIFICATION DETAIL CARDS */
  .cert-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; }
  .cert-card { background: #fff; border: 1px solid var(--line); padding: 24px; position: relative; }
  .cert-card::before { content: ''; position: absolute; top: -1px; left: 0; width: 50px; height: 4px; background: var(--orange); }
  .cert-card .cc-mark { font-family: var(--cond); font-size: 26px; font-weight: 800; color: var(--navy); letter-spacing: -0.01em; margin-bottom: 6px; }
  .cert-card .cc-scope { font-size: 13px; color: var(--ink-soft); line-height: 1.5; }
  .cert-card .cc-tag { font-family: var(--mono); font-size: 10px; letter-spacing: 0.12em; color: var(--orange); text-transform: uppercase; font-weight: 600; margin-top: 12px; }

  /* RELATED INTERNAL LINKS (Product -> Material + Application) */
  .rel-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 22px; }
  .rel-card { background: #fff; border: 1px solid var(--line); padding: 28px; display: flex; flex-direction: column; gap: 10px; transition: border-color 0.15s, transform 0.15s; }
  .rel-card:hover { border-color: var(--orange); transform: translateY(-3px); box-shadow: 0 20px 40px -22px rgba(10,38,71,0.15); }
  .rel-eyebrow { font-family: var(--mono); font-size: 10px; letter-spacing: 0.14em; color: var(--orange); text-transform: uppercase; font-weight: 600; }
  .rel-title { font-family: var(--cond); font-size: 22px; font-weight: 700; color: var(--navy); line-height: 1.15; }
  .rel-desc { font-size: 13px; color: var(--ink-soft); line-height: 1.5; }
  .rel-link { font-family: var(--sans); font-size: 13px; font-weight: 800; color: var(--orange); margin-top: 4px; }

  /* QUOTE SECTION (full-width, 2-col) */
  .quote-sec { background: var(--navy); color: #fff; }
  .quote-sec .wrap { display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: start; }
  .quote-sec .sec-num { color: var(--gold); }
  .quote-sec h2 { color: #fff; }
  .quote-sec h2 .accent { color: var(--gold); }
  .quote-why { list-style: none; display: flex; flex-direction: column; gap: 16px; margin-top: 24px; }
  .quote-why li { display: flex; gap: 13px; align-items: flex-start; font-size: 15px; color: rgba(255,255,255,0.88); line-height: 1.5; }
  .quote-why li::before { content: '✓'; color: var(--gold); font-weight: 800; flex-shrink: 0; }
  .quote-why li b { color: #fff; font-weight: 700; }
  .quote-form-card { background: #fff; padding: 32px; }
  .quote-form-card .qquote-title { color: var(--navy); }

  @media (max-width: 1024px) {
    .dl-grid { grid-template-columns: 1fr; }
    .cert-grid { grid-template-columns: 1fr 1fr; }
    .rel-grid { grid-template-columns: 1fr; }
    .quote-sec .wrap { grid-template-columns: 1fr; gap: 36px; }
  }
  @media (max-width: 640px) {
    .cert-grid { grid-template-columns: 1fr; }
    .spotlight { margin-top: 0; }
  }

  /* ============================================================
     APPLICATION VERTICAL DETAIL — v2
     Hero: single-column (no height imbalance) + scenario pill nav
     Scenarios: image-left + content-right full-bleed grid
     ============================================================ */

  /* ---- APPLICATION HERO (single-column, no forced 2-col) ---- */
  .app-hero { background: linear-gradient(135deg, var(--navy) 0%, var(--navy-2) 100%); color: #fff; padding: 68px 0 0; position: relative; overflow: hidden; }
  .app-hero-stripe { position: absolute; top: 0; left: 0; right: 0; height: 3px; background: linear-gradient(90deg, var(--orange) 0%, var(--gold) 50%, var(--orange) 100%); }
  .app-hero-eyebrow { display: inline-block; background: var(--orange); color: #fff; font-family: var(--mono); font-size: 11px; letter-spacing: 0.15em; padding: 8px 14px; margin-bottom: 24px; text-transform: uppercase; font-weight: 500; }
  .app-hero h1 { font-family: var(--cond); font-size: 60px; line-height: 1.02; font-weight: 800; letter-spacing: -0.025em; margin-bottom: 22px; color: #fff; max-width: 860px; }
  .app-hero h1 .hl { color: var(--gold); }
  .app-hero .app-hero-sub { font-size: 18px; color: rgba(255,255,255,0.82); line-height: 1.55; max-width: 760px; margin-bottom: 32px; }
  .app-hero .app-hero-sub b { color: var(--gold-soft); font-weight: 700; }
  .app-hero-stats { display: flex; gap: 44px; flex-wrap: wrap; margin-bottom: 36px; }
  .app-hero-stats .num { font-family: var(--cond); font-size: 42px; font-weight: 800; line-height: 1; color: var(--gold); }
  .app-hero-stats .lbl { font-family: var(--mono); font-size: 10.5px; letter-spacing: 0.12em; color: rgba(255,255,255,0.65); margin-top: 6px; text-transform: uppercase; }
  .app-hero-ctas { display: flex; gap: 12px; flex-wrap: wrap; margin-bottom: 48px; }

  /* Scenario pill nav (bottom strip inside hero) */
  .app-scen-nav { border-top: 1px solid rgba(255,255,255,0.14); padding: 24px 0 0; }
  .app-scen-nav-label { font-family: var(--mono); font-size: 10px; letter-spacing: 0.16em; color: rgba(255,255,255,0.5); text-transform: uppercase; margin-bottom: 14px; }
  .app-scen-pills { display: grid; grid-template-columns: repeat(4, 1fr); gap: 6px; padding-bottom: 28px; }
  .app-scen-pill { display: flex; align-items: center; gap: 10px; padding: 11px 14px; background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.1); transition: background 0.15s, border-color 0.15s; }
  .app-scen-pill:hover { background: rgba(255,255,255,0.1); border-color: var(--orange); }
  .app-scen-pill.flag { border-color: var(--orange); background: rgba(242,100,25,0.12); }
  .app-scen-pill .pill-num { font-family: var(--mono); font-size: 11px; font-weight: 500; color: var(--gold); letter-spacing: 0.05em; flex-shrink: 0; }
  .app-scen-pill .pill-name { font-family: var(--sans); font-size: 13px; font-weight: 600; color: rgba(255,255,255,0.88); line-height: 1.25; }

  /* ---- SCENARIO SECTIONS WITH IMAGE ---- */
  .scen-img-sec { padding: 0; }
  .scen-img-sec.alt { background: var(--bg-soft); }
  .scen-full-wrap { display: grid; grid-template-columns: 420px 1fr; min-height: 460px; max-width: 1260px; margin: 0 auto; padding: 0 0 0 40px; gap: 0; align-items: stretch; }
  .scen-full-wrap.rev { grid-template-columns: 1fr 420px; padding: 0 40px 0 0; }
  .scen-full-wrap.rev .scen-img-col { order: 2; }
  .scen-full-wrap.rev .scen-txt-col { order: 1; padding: 60px 56px 60px 0; }

  /* Image column — placeholder styled to look like real imagery */
  .scen-img-col { position: relative; overflow: hidden; flex-shrink: 0; min-height: 460px; }
  .scen-img-placeholder { width: 100%; height: 100%; min-height: 460px; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 14px; position: relative; }
  .scen-img-placeholder .img-icon { font-size: 56px; line-height: 1; opacity: 0.9; }
  .scen-img-placeholder .img-label { font-family: var(--mono); font-size: 11px; letter-spacing: 0.1em; text-transform: uppercase; opacity: 0.65; font-weight: 500; text-align: center; max-width: 180px; line-height: 1.5; }
  .scen-img-placeholder .img-replace { position: absolute; bottom: 14px; left: 0; right: 0; text-align: center; font-family: var(--mono); font-size: 10px; letter-spacing: 0.08em; opacity: 0.45; text-transform: uppercase; }
  /* Real photo: replace placeholder div with <img> using same .scen-img-col wrapper */
  .scen-img-col img { width: 100%; height: 100%; object-fit: cover; display: block; }

  /* Scenario colors */
  .si-biomass { background: linear-gradient(145deg, #1a0f05 0%, #3d1a08 40%, #6b2d0e 100%); color: #f5d9b8; }
  .si-bbq    { background: linear-gradient(145deg, #1a0a05 0%, #4a1a08 40%, #7a2e10 100%); color: #fad4a8; }
  .si-gas    { background: linear-gradient(145deg, #0a1a2a 0%, #152840 40%, #1e3d5c 100%); color: #b8d4f0; }
  .si-boiler { background: linear-gradient(145deg, #0f0f14 0%, #1e1e2e 40%, #2e2e44 100%); color: #c4c4e0; }
  .si-water  { background: linear-gradient(145deg, #051520 0%, #0a2535 40%, #0f3650 100%); color: #a8d4f5; }
  .si-hvac   { background: linear-gradient(145deg, #051a1a 0%, #0a2e2e 40%, #103e3e 100%); color: #a8e0e0; }
  .si-ovens  { background: linear-gradient(145deg, #1a1005 0%, #3d2808 40%, #5c3c0f 100%); color: #f0d8a8; }
  .si-hydro  { background: linear-gradient(145deg, #051a10 0%, #0a3020 40%, #0f4530 100%); color: #a8f0c4; }

  /* Text column */
  .scen-txt-col { padding: 60px 0 60px 56px; display: flex; flex-direction: column; justify-content: center; }
  .scen-eyebrow { font-family: var(--mono); font-size: 11px; letter-spacing: 0.18em; color: var(--orange); text-transform: uppercase; font-weight: 500; margin-bottom: 10px; }
  .scen-flag { display: inline-block; background: var(--orange); color: #fff; font-family: var(--mono); font-size: 9.5px; letter-spacing: 0.14em; text-transform: uppercase; font-weight: 600; padding: 5px 10px; margin-bottom: 12px; }
  .scen-h2 { font-family: var(--cond); font-size: 36px; font-weight: 800; line-height: 1.05; letter-spacing: -0.02em; color: var(--ink); margin-bottom: 14px; }
  .scen-h2 .accent { color: var(--orange); }
  .scen-intro { font-size: 15px; color: var(--ink-soft); line-height: 1.6; margin-bottom: 24px; }
  .scen-intro b { color: var(--navy); font-weight: 700; }

  /* Tech requirements — clean key-value, NOT dark table */
  .tech-req-label { font-family: var(--mono); font-size: 10.5px; letter-spacing: 0.13em; color: var(--navy); text-transform: uppercase; font-weight: 600; margin-bottom: 10px; border-bottom: 2px solid var(--ink); padding-bottom: 8px; }
  .tech-row { display: grid; grid-template-columns: 1fr 1.3fr; gap: 6px; align-items: baseline; padding: 9px 0; border-bottom: 1px solid var(--line); }
  .tech-row:last-child { border-bottom: none; }
  .tech-k { font-family: var(--mono); font-size: 11px; color: var(--ink-faint); letter-spacing: 0.04em; }
  .tech-v { font-family: var(--cond); font-size: 17px; font-weight: 700; color: var(--navy); }
  .tech-v .hi { color: var(--orange); }
  .tech-v small { font-family: var(--mono); font-size: 10.5px; color: var(--ink-soft); font-weight: 400; margin-left: 3px; }

  /* Recommended products inline */
  .rec-row { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 12px; padding-top: 20px; margin-top: 20px; border-top: 1px solid var(--line); }
  .rec-links { display: flex; flex-wrap: wrap; gap: 8px 18px; font-family: var(--mono); font-size: 11.5px; }
  .rec-links .rl { color: var(--navy); font-weight: 600; }
  .rec-links a { color: var(--orange); font-weight: 600; }
  .rec-links a:hover { color: var(--orange-deep); }
  .rec-cta-sm { font-family: var(--sans); font-size: 13.5px; font-weight: 800; color: var(--orange); white-space: nowrap; border: 2px solid var(--orange); padding: 10px 18px; transition: background 0.15s, color 0.15s; }
  .rec-cta-sm:hover { background: var(--orange); color: #fff; }

  /* Flagship expanded case study (Biomass) */
  .flagship-ext { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; margin-top: 18px; }
  .fe-stat { background: var(--navy); padding: 14px 16px; }
  .fe-stat .fn { font-family: var(--cond); font-size: 26px; font-weight: 800; color: var(--gold); line-height: 1; }
  .fe-stat .fl { font-family: var(--mono); font-size: 10px; color: rgba(255,255,255,0.65); letter-spacing: 0.04em; margin-top: 4px; }
  .fe-case-label { font-family: var(--mono); font-size: 10px; letter-spacing: 0.14em; color: var(--orange); text-transform: uppercase; font-weight: 600; margin-bottom: 8px; }

  @media (max-width: 1024px) {
    .app-scen-pills { grid-template-columns: repeat(2, 1fr); }
    .scen-full-wrap { grid-template-columns: 1fr; padding: 0; min-height: auto; }
    .scen-full-wrap.rev { padding: 0; }
    .scen-full-wrap.rev .scen-img-col { order: 1; }
    .scen-full-wrap.rev .scen-txt-col { order: 2; padding: 40px 40px; }
    .scen-img-col { min-height: 260px; }
    .scen-img-placeholder { min-height: 260px; }
    .scen-txt-col { padding: 40px 40px; }
    .flagship-ext { grid-template-columns: repeat(2, 1fr); }
  }
  @media (max-width: 640px) {
    .app-hero h1 { font-size: 40px; }
    .app-scen-pills { grid-template-columns: 1fr; }
    .tech-row { grid-template-columns: 1fr; }
    .rec-row { flex-direction: column; align-items: flex-start; }
    .scen-txt-col { padding: 32px 20px; }
    .flagship-ext { grid-template-columns: 1fr 1fr; }
  }

  /* ============================================================
     BIOMASS & PELLET STOVES — application deep-dive page
     ============================================================ */

  /* Hero — application scenario hero (not product, not material) */
  .bio-hero { background: linear-gradient(135deg, #1a0f05 0%, #2d1a0a 50%, var(--navy) 100%); color: #fff; padding: 68px 0 0; position: relative; overflow: hidden; }
  .bio-hero::after { content: ''; position: absolute; top: 0; right: 0; width: 40%; height: 100%; background: linear-gradient(135deg, transparent 0%, rgba(242,100,25,0.06) 100%); pointer-events: none; }
  .bio-hero-stripe { position: absolute; top: 0; left: 0; right: 0; height: 3px; background: linear-gradient(90deg, var(--orange) 0%, var(--gold) 60%, var(--orange) 100%); }
  .bio-hero .wrap { display: grid; grid-template-columns: 1.1fr 0.9fr; gap: 56px; align-items: end; position: relative; }
  .bio-hero-eyebrow { font-family: var(--mono); font-size: 11px; letter-spacing: 0.15em; color: var(--gold); text-transform: uppercase; font-weight: 500; margin-bottom: 18px; }
  .bio-hero h1 { font-family: var(--cond); font-size: 56px; line-height: 1.02; font-weight: 800; letter-spacing: -0.025em; margin-bottom: 20px; color: #fff; }
  .bio-hero h1 .hl { color: var(--gold); }
  .bio-hero-sub { font-size: 17px; color: rgba(255,255,255,0.82); line-height: 1.55; max-width: 620px; margin-bottom: 30px; }
  .bio-hero-sub b { color: var(--gold-soft); }
  .bio-statbar { display: flex; gap: 36px; flex-wrap: wrap; margin-bottom: 36px; }
  .bio-statbar .num { font-family: var(--cond); font-size: 40px; font-weight: 800; line-height: 1; color: var(--gold); }
  .bio-statbar .lbl { font-family: var(--mono); font-size: 10px; letter-spacing: 0.12em; color: rgba(255,255,255,0.65); margin-top: 5px; text-transform: uppercase; }
  .bio-hero-ctas { display: flex; gap: 12px; flex-wrap: wrap; margin-bottom: 48px; }

  /* Hero right: case proof card */
  .bio-proof { background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.14); border-left: 3px solid var(--orange); padding: 26px 28px; margin-bottom: 28px; }
  .bio-proof-eyebrow { font-family: var(--mono); font-size: 10px; letter-spacing: 0.14em; color: var(--gold); text-transform: uppercase; font-weight: 600; margin-bottom: 10px; }
  .bio-proof-title { font-family: var(--cond); font-size: 20px; font-weight: 800; color: #fff; line-height: 1.1; margin-bottom: 16px; }
  .bio-proof-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; }
  .bio-proof-stat { background: rgba(255,255,255,0.05); padding: 12px 14px; }
  .bio-proof-stat .bn { font-family: var(--cond); font-size: 26px; font-weight: 800; color: var(--gold); line-height: 1; }
  .bio-proof-stat .bl { font-family: var(--mono); font-size: 9.5px; color: rgba(255,255,255,0.6); letter-spacing: 0.04em; margin-top: 4px; }
  .bio-hero-nav-bottom { border-top: 1px solid rgba(255,255,255,0.1); padding: 20px 0 28px; display: flex; gap: 28px; flex-wrap: wrap; align-items: center; }
  .bio-hero-nav-bottom .nav-back { font-family: var(--mono); font-size: 11px; color: rgba(255,255,255,0.55); letter-spacing: 0.05em; }
  .bio-hero-nav-bottom .nav-back a { color: var(--gold); font-weight: 600; }
  .bio-hero-nav-bottom .nav-back a:hover { color: #fff; }

  /* Market context band */
  .mkt-band { background: var(--bg-soft); border-top: 1px solid var(--line); border-bottom: 1px solid var(--line); padding: 44px 0; }
  .mkt-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 32px; align-items: start; }
  .mkt-item { display: flex; flex-direction: column; gap: 8px; }
  .mkt-num { font-family: var(--cond); font-size: 38px; font-weight: 800; color: var(--navy); line-height: 1; letter-spacing: -0.01em; }
  .mkt-num span { color: var(--orange); }
  .mkt-label { font-family: var(--mono); font-size: 11px; letter-spacing: 0.08em; color: var(--ink-soft); line-height: 1.5; }
  .mkt-divider { width: 1px; background: var(--line); align-self: stretch; display: none; }

  /* Igniter comparison panel: Si3N4 vs Al2O3 for this specific application */
  .comp-band { padding: 72px 0; }
  .comp-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; margin-top: 36px; }
  .comp-card { border: 1px solid var(--line); padding: 32px; position: relative; }
  .comp-card.winner { border-color: var(--orange); border-width: 2px; }
  .comp-card.winner::before { content: ''; position: absolute; top: -1px; left: 0; width: 80px; height: 4px; background: var(--orange); }
  .comp-badge { display: inline-block; font-family: var(--mono); font-size: 9.5px; letter-spacing: 0.14em; text-transform: uppercase; font-weight: 600; padding: 5px 10px; margin-bottom: 14px; }
  .comp-badge.win { background: var(--orange); color: #fff; }
  .comp-badge.alt { background: var(--bg-soft-2); color: var(--ink-soft); }
  .comp-name { font-family: var(--cond); font-size: 28px; font-weight: 800; color: var(--navy); letter-spacing: -0.01em; margin-bottom: 6px; }
  .comp-tagline { font-family: var(--mono); font-size: 11.5px; color: var(--ink-faint); letter-spacing: 0.03em; margin-bottom: 22px; }
  .comp-list { list-style: none; display: flex; flex-direction: column; gap: 10px; }
  .comp-list li { display: flex; gap: 10px; align-items: flex-start; font-size: 14px; color: var(--ink-soft); line-height: 1.5; }
  .comp-list li .ci { flex-shrink: 0; font-size: 13px; margin-top: 2px; }
  .comp-list li b { color: var(--navy); font-weight: 700; }
  .comp-cta-row { margin-top: 22px; padding-top: 18px; border-top: 1px solid var(--line); }

  /* Application scenario depth section */
  .depth-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 56px; align-items: start; }
  .depth-img { background: linear-gradient(145deg, #1a0f05 0%, #3d1a08 40%, #6b2d0e 100%); min-height: 400px; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 14px; position: relative; }
  .depth-img .di-icon { font-size: 64px; line-height: 1; opacity: 0.9; }
  .depth-img .di-label { font-family: var(--mono); font-size: 11px; color: rgba(255,220,180,0.7); letter-spacing: 0.08em; text-transform: uppercase; text-align: center; max-width: 200px; line-height: 1.5; }
  .depth-img .di-replace { position: absolute; bottom: 14px; font-family: var(--mono); font-size: 10px; color: rgba(255,220,180,0.4); letter-spacing: 0.07em; text-transform: uppercase; }

  /* Failure analysis band */
  .failure-band { background: var(--navy); color: #fff; padding: 64px 0; }
  .failure-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 28px; margin-top: 36px; }
  .fail-card { background: rgba(255,255,255,0.06); border-left: 3px solid var(--orange); padding: 22px 24px; }
  .fail-card.worst { border-color: var(--gold); }
  .fail-num { font-family: var(--cond); font-size: 34px; font-weight: 800; color: var(--gold); line-height: 1; margin-bottom: 6px; }
  .fail-name { font-family: var(--cond); font-size: 18px; font-weight: 700; color: #fff; margin-bottom: 8px; }
  .fail-desc { font-size: 13px; color: rgba(255,255,255,0.7); line-height: 1.55; }
  .fail-solution { font-family: var(--mono); font-size: 11px; color: var(--gold); letter-spacing: 0.04em; margin-top: 10px; padding-top: 10px; border-top: 1px solid rgba(255,255,255,0.1); }

  @media (max-width: 1024px) {
    .bio-hero .wrap { grid-template-columns: 1fr; gap: 36px; }
    .mkt-grid { grid-template-columns: 1fr 1fr; gap: 24px; }
    .comp-grid { grid-template-columns: 1fr; }
    .depth-grid { grid-template-columns: 1fr; }
    .failure-grid { grid-template-columns: 1fr; }
  }
  @media (max-width: 640px) {
    .bio-hero h1 { font-size: 38px; }
    .bio-proof-grid { grid-template-columns: 1fr 1fr; }
    .mkt-grid { grid-template-columns: 1fr 1fr; }
  }
</style>
